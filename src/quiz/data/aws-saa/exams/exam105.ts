import type { RawQuestion } from '@/quiz/quiz.types';

/**
 * AWS SAA-C03 practice set 4 (53 questions), authored by Chabe.
 */
export const exam105: RawQuestion[] = [
  {
    n: 1,
    d: 'RES',
    m: 1,
    q: 'Which two statements are true about the EC2 user data feature? (Choose two.)',
    o: [
      'By default, user data is executed every time an EC2 instance is restarted',
      'While an instance is running, you can update its user data using root user credentials',
      'By default, scripts entered as user data are executed with root user privileges',
      'By default, scripts entered as user data do not have root user privileges',
      'By default, user data runs only during the boot cycle when you first launch an instance',
    ],
    a: [2, 4],
    x: 'Los scripts de user data se ejecutan como root por defecto (sin necesitar sudo), y solo corren durante el primer arranque tras el lanzamiento, salvo que se configure explicitamente para que se repita en cada reinicio. No puedes modificar el user data mientras la instancia esta en ejecucion, ni siquiera con credenciales root, solo puedes verlo.',
  },
  {
    n: 2,
    d: 'COST',
    m: 0,
    q: 'You want to mount a network file system on Linux instances where files are accessed frequently at first and then infrequently. What is the most cost-effective solution?',
    o: ['Glacier Deep Archive', 'S3 Intelligent-Tiering', 'FSx for Lustre', 'EFS Infrequent Access (EFS IA)'],
    a: [3],
    x: 'EFS IA reduce hasta un 92% el coste de almacenamiento para archivos poco accedidos, manteniendo la disponibilidad y semantica de sistema de archivos NFS que se necesita montar en Linux. S3 y Glacier son almacenamiento de objetos (no se montan como sistema de archivos de red), y FSx for Lustre esta pensado para HPC y es mucho mas caro para este caso.',
  },
  {
    n: 3,
    d: 'PERF',
    m: 0,
    q: 'An analytics company wants to improve the performance of big data processing workflows running on Amazon EFS. Which EFS performance mode should be used?',
    o: ['Provisioned Throughput', 'Max I/O', 'Bursting Throughput', 'General Purpose'],
    a: [1],
    x: 'Max I/O escala a niveles mas altos de throughput y operaciones por segundo agregados, a costa de una latencia ligeramente mayor en metadatos, encajando con cargas muy paralelizadas como el analisis de big data. Provisioned Throughput y Bursting Throughput son modos de throughput (no de performance), y General Purpose esta pensado para casos sensibles a la latencia, no para maximizar el throughput agregado.',
  },
  {
    n: 4,
    d: 'COST',
    m: 0,
    q: 'A studio wants a scalable, serverless static website with lots of animations for a movie trailer launch. What is the most cost-optimal and high-performance solution?',
    o: [
      'Host the site on an on-premises instance, with CloudFront using it as a custom origin',
      'Build the site as a static site on S3, with CloudFront as the origin and a Route 53 alias record',
      'Host the site on Lambda, with CloudFront using Lambda as the origin',
      'Host the site on an EC2 instance, with CloudFront using it as a custom origin',
    ],
    a: [1],
    x: 'S3 aloja contenido estatico de forma barata y serverless, y CloudFront lo distribuye globalmente con baja latencia, con Route 53 apuntando via alias record a la distribucion. Lambda no aloja sitios web ni puede ser origen de CloudFront, y usar EC2 o un servidor on-premises va en contra del requisito explicito de solucion serverless.',
  },
  {
    n: 5,
    d: 'SEC',
    m: 0,
    q: 'A Lambda function in AWS account A needs to access an S3 bucket in AWS account B. What is the correct way to grant this access?',
    o: [
      'Create an IAM role granting S3 access and set it as the Lambda execution role; that alone gives cross-account access',
      'Create an IAM role granting S3 access, set it as the Lambda execution role, and make sure the bucket policy also grants access to that role',
      'Lambda cannot access resources in other AWS accounts; use identity federation instead',
      'The bucket owner should make the bucket public so the Lambda function can access it',
    ],
    a: [1],
    x: 'Cuando el rol de ejecucion de Lambda y el bucket estan en cuentas distintas, hay que conceder permisos tanto en el rol IAM como en la bucket policy del bucket de destino para que el acceso funcione. Confiar solo en el rol IAM no basta entre cuentas distintas, hacer publico el bucket es una mala practica de seguridad, y Lambda si puede acceder a recursos de otras cuentas sin necesitar federacion de identidades.',
  },
  {
    n: 6,
    d: 'PERF',
    m: 0,
    q: 'IoT data flows into Kinesis Data Streams, which is the source of a Kinesis Firehose delivery stream. After configuring a Kinesis Agent on another set of devices to send data to the same Firehose stream, data is not arriving as expected. What is the most likely root cause?',
    o: [
      'Kinesis Agent can only write to Kinesis Data Streams, not to Firehose',
      'The Firehose delivery stream has reached its limit and needs manual scaling',
      'Kinesis Agent cannot write to a Firehose stream whose source is already set to a Kinesis Data Stream',
      'The data sent by Kinesis Agent was lost due to a configuration error',
    ],
    a: [2],
    x: 'Cuando un Kinesis Data Stream esta configurado como origen de un Firehose delivery stream, las operaciones directas PutRecord/PutRecordBatch hacia Firehose quedan deshabilitadas; los datos deben entrar por el Data Stream, no directamente por Firehose. Kinesis Agent si puede escribir en Firehose en otros escenarios, y Firehose escala automaticamente sin intervencion manual.',
  },
  {
    n: 7,
    d: 'SEC',
    m: 0,
    q: 'A company wants to authorize API Gateway calls with a solution that offers built-in user management. What do you recommend?',
    o: [
      'API Gateway Lambda authorizer',
      'AWS_IAM authorization',
      'Amazon Cognito User Pools',
      'Amazon Cognito Identity Pools',
    ],
    a: [2],
    x: 'Cognito User Pools ofrece gestion de usuarios integrada (registro, inicio de sesion, MFA, verificacion) ademas de integracion con proveedores sociales o SAML, y se conecta a API Gateway como un authorizer COGNITO_USER_POOLS. AWS_IAM sirve para consumidores dentro del entorno AWS, el Lambda authorizer requiere validar contra un IdP propio existente, y los Identity Pools solo dan credenciales temporales de AWS, no gestion de usuarios.',
  },
  {
    n: 8,
    d: 'RES',
    m: 1,
    q: 'A video backup service on EC2 behind an ALB stores videos on per-instance EBS volumes, so users only see a subset of their uploads depending on which instance answers. Which two changes ensure users see all their videos? (Choose two.)',
    o: [
      'Copy the videos from all EBS volumes to S3 Standard and switch the app to store videos in S3',
      'Mount EFS on all instances, copy the videos from the EBS volumes to EFS, and switch the app to use EFS',
      'Copy the videos to DynamoDB and switch the app to store videos there',
      'Copy the videos to RDS and switch the app to store videos there',
      'Copy the videos to S3 Glacier Deep Archive and switch the app to store videos there',
    ],
    a: [0, 1],
    x: 'S3 y EFS son almacenamiento compartido accesible desde todas las instancias, resolviendo el problema de fondo de que cada EBS es independiente y solo visible desde su propia instancia. DynamoDB y RDS no son adecuados para archivos binarios grandes como videos, y Glacier Deep Archive tiene tiempos de recuperacion de horas, incompatibles con un servicio de acceso inmediato.',
  },
  {
    n: 9,
    d: 'SEC',
    m: 0,
    q: 'Third-party SSL/TLS certificates imported into ACM must be renewed before expiry, notifying the security team 30 days beforehand with minimal scripting. What do you recommend?',
    o: [
      'Monitor a CloudWatch "days to expiry" metric for imported certificates and alarm on it',
      'Use an AWS Config managed rule to flag third-party certificates imported into ACM expiring within 30 days, notifying via SNS',
      'Use an AWS Config managed rule to flag ACM-issued certificates expiring within 30 days, notifying via SNS',
      'Monitor a CloudWatch "days to expiry" metric for ACM-issued certificates and alarm on it',
    ],
    a: [1],
    x: 'El AWS Config managed rule para expiracion de certificados ACM evalua automaticamente los certificados importados (incluidos los de terceros) y se integra con SNS para notificar, con minimo esfuerzo de configuracion. ACM no publica una metrica nativa de "dias para expirar" en CloudWatch, y los certificados emitidos por ACM se renuevan automaticamente, asi que el problema real esta en los certificados importados de terceros, no en los emitidos por ACM.',
  },
  {
    n: 10,
    d: 'PERF',
    m: 0,
    q: 'An application using RDS for MySQL with read replicas has performance issues, and the company wants to keep the relational schema while operating at global scale for branch offices worldwide. What is the most cost-effective, high-performance solution?',
    o: [
      'DynamoDB global tables for fast local reads and writes in each region',
      'EC2 with MySQL installed in each region, migrating existing data',
      'Aurora Global Database for fast local reads with low latency in each region',
      'A Redshift cluster in each region, migrating existing data',
    ],
    a: [2],
    x: 'Aurora Global Database mantiene una sola region primaria para escrituras y replica a regiones secundarias con menos de un segundo de latencia tipica, dando lecturas locales rapidas mientras conserva el esquema relacional. DynamoDB no es relacional (rompe el requisito), gestionar MySQL manualmente en EC2 por region anade mucha carga operativa, y Redshift es para analitica, no para el workload transaccional descrito.',
  },
  {
    n: 11,
    d: 'SEC',
    m: 0,
    q: 'A social media app runs on EC2 behind an ALB, fronted by CloudFront. The team wants to decouple user authentication so app servers focus only on business logic, with minimal development effort. What do you recommend?',
    o: [
      'Cognito authentication via Cognito User Pools for the CloudFront distribution',
      'Cognito authentication via Cognito Identity Pools for the CloudFront distribution',
      'Cognito authentication via Cognito User Pools for the Application Load Balancer',
      'Cognito authentication via Cognito Identity Pools for the Application Load Balancer',
    ],
    a: [2],
    x: 'El ALB soporta una accion de autenticacion integrada con Cognito User Pools directamente en las reglas del listener, delegando la autenticacion al load balancer sin tocar el codigo de la aplicacion. Los Identity Pools no gestionan autenticacion de usuarios (solo credenciales temporales de AWS), y para usar User Pools con CloudFront haria falta desarrollar una funcion Lambda@Edge adicional, mas esfuerzo de desarrollo que con el ALB.',
  },
  {
    n: 12,
    d: 'COST',
    m: 0,
    q: 'What is correct regarding data transfer charges for RDS Read Replicas?',
    o: [
      'There are data transfer charges for replicating across AWS Regions',
      'There are data transfer charges for replicating within the same AWS Region',
      'There are data transfer charges for replicating within the same Availability Zone',
      'There are no data transfer charges for replicating across AWS Regions',
    ],
    a: [0],
    x: 'La replicacion dentro de la misma region no genera cargos de transferencia de datos entre la instancia origen y el read replica, pero la replicacion cross-region si conlleva cargos, ya que atraviesa la red publica entre regiones de AWS.',
  },
  {
    n: 13,
    d: 'PERF',
    m: 0,
    q: 'An e-commerce app uses Aurora Multi-AZ, and database reads are causing high I/O that adds latency to write requests. What would you recommend to separate reads from writes?',
    o: [
      'Set up a read replica and modify the app to use the appropriate endpoint',
      'Activate read-through caching on the Aurora database',
      'Configure the app to read from the Multi-AZ standby instance',
      'Provision another Aurora database and link it as a read replica',
    ],
    a: [0],
    x: 'Un read replica de Aurora ofrece un endpoint dedicado para las lecturas, separando ese trafico del primario y evitando que agregue latencia a las escrituras. Aurora no ofrece caching read-through nativo, el standby de Multi-AZ solo se usa para failover (no sirve lecturas en operacion normal), y los replicas se crean dentro del mismo cluster, no vinculando una segunda base de datos separada.',
  },
  {
    n: 14,
    d: 'PERF',
    m: 0,
    q: 'An application performs a lot of overwrites and deletes on data, and every read query must reflect the latest information. Which database technology fits?',
    o: ['Amazon Neptune', 'Amazon RDS', 'Amazon ElastiCache', 'Amazon S3'],
    a: [1],
    x: 'RDS ofrece bases relacionales con cumplimiento ACID y consistencia fuerte, garantizando que las consultas devuelvan siempre el dato mas reciente tras sobrescrituras y eliminaciones frecuentes. Neptune es para grafos, ElastiCache es una cache en memoria (no la base de datos principal), y S3 es almacenamiento de objetos, no una base de datos transaccional.',
  },
  {
    n: 15,
    d: 'SEC',
    m: 0,
    q: 'CloudFront (with an ALB origin) plus WAF sees a surge of malicious attacks from a specific IP trying to steal data from the EC2 instances. What action stops the attacks?',
    o: [
      'Create a deny rule for the malicious IP in each instance\u2019s security group',
      'Open a ticket with AWS support to take action against the malicious IP',
      'Create an IP match condition in WAF to block the malicious IP',
      'Create a deny rule for the malicious IP in each instance\u2019s network ACL',
    ],
    a: [2],
    x: 'Una condicion de coincidencia de IP en WAF bloquea las peticiones de esa direccion antes de que lleguen a la aplicacion, siendo la herramienta pensada para esto en la capa donde ya esta desplegado WAF. Los security groups no admiten reglas deny, las NACL no estan asociadas a instancias individuales, y AWS no gestiona la seguridad de tu aplicacion en tu nombre.',
  },
  {
    n: 16,
    d: 'RES',
    m: 0,
    q: 'A manufacturing company wants a failover environment on AWS in case its on-premises data center fails, keeping data consistent and minimizing downtime. Which solution provides the least downtime?',
    o: [
      'Route 53 failover + a script running CloudFormation to provision EC2 behind an ALB + Storage Gateway stored volumes backing up to S3',
      'Route 53 failover + application servers already running on EC2 behind an ALB in an Auto Scaling group + Storage Gateway stored volumes backing up to S3',
      'Route 53 failover + Lambda running CloudFormation to launch two EC2 instances + Storage Gateway stored volumes + Direct Connect',
      'Route 53 failover + Direct Connect + EC2 Auto Scaling + Lambda running CloudFormation to create an ALB',
    ],
    a: [1],
    x: 'Tener los servidores de aplicacion ya desplegados y en marcha en un Auto Scaling group detras de un ALB, con Storage Gateway manteniendo los datos sincronizados, permite un failover casi inmediato via Route 53. Las opciones que aprovisionan EC2, el ALB o la infraestructura en el momento del fallo (via script, Lambda o CloudFormation) introducen retraso justo cuando se necesita la menor demora posible.',
  },
  {
    n: 17,
    d: 'SEC',
    m: 0,
    q: 'You want to store a database password securely and rotate it automatically every 90 days. What do you recommend?',
    o: ['CloudHSM', 'Secrets Manager', 'SSM Parameter Store', 'Key Management Service (KMS)'],
    a: [1],
    x: 'Secrets Manager esta pensado para guardar secretos como credenciales de base de datos y soporta rotacion automatica programada, integrandose de forma nativa con RDS. CloudHSM gestiona claves criptograficas (no secretos de aplicacion), Parameter Store puede guardar secretos cifrados pero no rota automaticamente sin scripts propios, y KMS gestiona claves de cifrado, no contrasenas.',
  },
  {
    n: 18,
    d: 'SEC',
    m: 0,
    q: 'A security review found several unencrypted RDS databases. What steps must be taken to encrypt them?',
    o: [
      'Enable encryption on the RDS database directly from the AWS Console',
      'Create a read replica of the database, encrypt the replica, then promote it and terminate the original',
      'Take a snapshot of the database, copy it as an encrypted snapshot, restore a new database from it, then terminate the old one',
      'Enable Multi-AZ, ensure the standby is encrypted, fail over to it, then disable Multi-AZ',
    ],
    a: [2],
    x: 'RDS no permite activar cifrado directamente sobre una instancia existente; el camino soportado es tomar un snapshot, copiarlo cifrandolo, restaurar una nueva instancia cifrada a partir de ese snapshot, y luego eliminar la instancia original sin cifrar. No se puede crear un replica cifrado desde un master sin cifrar, y el standby de Multi-AZ hereda el estado de cifrado del primario, asi que tampoco resuelve el problema.',
  },
  {
    n: 19,
    d: 'SEC',
    m: 0,
    q: 'A Redshift cluster writes data via UNLOAD to an S3 bucket in a different AWS account, but the bucket owner cannot access those files. What is the most likely reason?',
    o: [
      'Both accounts must share bucket policies; an erroneous policy caused this',
      'The bucket owner has implicit access, but permissions are set only after the copy finishes, so the write may still be in progress',
      'The bucket owner gets implicit permissions on cross-account uploads; this looks like an upload error fixable from the console',
      'By default, an S3 object is owned by the account that uploaded it, so the bucket owner does not automatically get access',
    ],
    a: [3],
    x: 'En S3, la propiedad del objeto pertenece a la cuenta que lo subio, no al propietario del bucket; por eso el propietario del bucket no obtiene acceso automatico a objetos escritos por otra cuenta salvo que se conceda explicitamente via politica o ACL. No es un problema de que la escritura este en progreso, ni existe un mecanismo de permisos implicitos automaticos para el propietario del bucket en este escenario.',
  },
  {
    n: 20,
    d: 'PERF',
    m: 0,
    q: 'ETL workloads are being migrated from an on-premises Hadoop cluster to AWS, needing high availability with about 50 EC2 instances per Availability Zone. Which placement group fits this distributed workload?',
    o: [
      'Both spread and partition placement groups',
      'Partition placement group',
      'Cluster placement group',
      'Spread placement group',
    ],
    a: [1],
    x: 'El partition placement group reparte las instancias en particiones logicas sin compartir hardware entre particiones, el patron recomendado para cargas distribuidas y replicadas como Hadoop. El cluster placement group prioriza baja latencia dentro de una AZ (no aislamiento de fallos), y el spread placement group limita a 7 instancias por AZ, insuficiente para 50 instancias.',
  },
  {
    n: 21,
    d: 'COST',
    m: 0,
    q: 'You need the most cost-effective file storage that can be concurrently accessed by hundreds of EC2 instances, for less frequently accessed files, with immediate access whenever needed. What fits?',
    o: [
      'EFS Standard-IA storage class',
      'EFS Standard storage class',
      'Amazon EBS',
      'S3 Standard-Infrequent Access storage class',
    ],
    a: [0],
    x: 'EFS Standard-IA reduce el coste de almacenamiento para archivos poco accedidos sin sacrificar la disponibilidad ni el acceso inmediato del sistema de archivos EFS compartido entre miles de instancias. EFS Standard es mas caro para datos poco accedidos, EBS no se puede compartir entre cientos de instancias a la vez, y S3 es almacenamiento de objetos, no un sistema de archivos.',
  },
  {
    n: 22,
    d: 'SEC',
    m: 0,
    q: 'What does this IAM policy do?\n\n{"Effect":"Allow","Action":["ec2:RunInstances"],"Resource":"*","Condition":{"IpAddress":{"aws:SourceIp":"34.50.31.0/24"}}}',
    o: [
      'It allows launching EC2 instances only when the API call originates from an IP in 34.50.31.0/24',
      'It allows launching EC2 instances only when they get a public IP within 34.50.31.0/24',
      'It allows launching EC2 instances only when they get an Elastic IP within 34.50.31.0/24',
      'It allows launching EC2 instances only when they get a private IP within 34.50.31.0/24',
    ],
    a: [0],
    x: 'La condicion aws:SourceIp evalua la IP de origen de quien realiza la llamada a la API, no ninguna propiedad de la instancia resultante. Por tanto la politica solo permite RunInstances si la peticion viene de esa CIDR; no restringe la IP publica, privada ni elastica que reciba la instancia lanzada.',
  },
  {
    n: 23,
    d: 'COST',
    m: 0,
    q: 'A workload needs at least 20 instances 24/7, up to 300 during spikes, and 80 instances 80% of the time. What is the most cost-optimal solution for steady-state demand?',
    o: [
      'Purchase 80 Spot Instances, using an ASG for the rest as On-Demand',
      'Purchase 80 On-Demand Instances, using an ASG for a mix of On-Demand and Spot for the rest',
      'Purchase 80 Reserved Instances, using an ASG for a mix of On-Demand and Spot for the rest',
      'Purchase 20 On-Demand Instances, using an ASG for the rest as Spot',
    ],
    a: [2],
    x: 'Como el workload necesita 80 instancias el 80% del tiempo, las Reserved Instances dan el mayor ahorro para esa base predecible, mientras que un Auto Scaling group con una mezcla de On-Demand y Spot cubre los picos hasta 300. Confiar en Spot para la base estable es arriesgado por posibles interrupciones, y usar On-Demand para toda la base predecible es mas caro de lo necesario.',
  },
  {
    n: 24,
    d: 'PERF',
    m: 1,
    q: 'A web+Aurora app runs entirely in us-east-1, but 90% of customers are in the US and Europe, and European customers report high application load time. Which two changes would you recommend? (Choose two.)',
    o: [
      'Create Aurora read replicas in eu-west-1',
      'Add a web-tier EC2 fleet in eu-west-1 and enable Route 53 geolocation routing',
      'Add a web-tier EC2 fleet in eu-west-1 and enable Route 53 latency-based routing',
      'Add a web-tier EC2 fleet in eu-west-1 and enable Route 53 failover routing',
      'Create an Aurora Multi-AZ standby instance in eu-west-1',
    ],
    a: [0, 2],
    x: 'Los read replicas de Aurora en eu-west-1 acercan las lecturas a los usuarios europeos, y el enrutamiento por latencia de Route 53 dirige a cada usuario hacia la region con menor latencia, mejorando el rendimiento medido. El enrutamiento geografico no esta pensado para minimizar latencia, el failover routing es para alta disponibilidad (no reduce latencia), y el standby de Multi-AZ no puede servir lecturas ni escalar el rendimiento de lectura.',
  },
  {
    n: 25,
    d: 'SEC',
    m: 0,
    q: 'What is true about RDS Read Replica encryption?',
    o: [
      'If the master database is encrypted, the read replicas are encrypted',
      'If the master database is unencrypted, the replicas can be either encrypted or unencrypted',
      'If the master database is encrypted, the replicas can be either encrypted or unencrypted',
      'If the master database is unencrypted, the replicas are encrypted',
    ],
    a: [0],
    x: 'Los read replicas heredan el estado de cifrado de la instancia primaria: si el master esta cifrado, todos sus replicas tambien lo estan de forma automatica. No es posible crear un replica cifrado desde un master sin cifrar, ni tampoco crear un replica sin cifrar desde un master cifrado.',
  },
  {
    n: 26,
    d: 'PERF',
    m: 1,
    q: 'The infrastructure team wants an email notification whenever CPU utilization for any EC2 instance breaches a threshold. Which two services require the least development effort for this? (Choose two.)',
    o: ['AWS Lambda', 'Amazon SQS', 'AWS Step Functions', 'Amazon SNS', 'Amazon CloudWatch'],
    a: [3, 4],
    x: 'CloudWatch puede crear una alarma sobre la metrica de CPU de las instancias EC2 y, al dispararse, notificar via un topic de SNS que envie el email al equipo, sin necesidad de escribir codigo adicional. Lambda, SQS y Step Functions no monitorizan CPU ni envian correos por si mismos y anadirian complejidad innecesaria a un caso tan directo.',
  },
  {
    n: 27,
    d: 'COST',
    m: 1,
    q: 'A data lake has raw and refined zones in S3, growing 1TB daily each. Source data must be kept 5+ years for compliance; analysts run ad-hoc Athena queries only on the refined zone. Which two changes are the most cost-optimal? (Choose two.)',
    o: [
      'Delete the raw zone data with a Lambda job after 1 day',
      'Have the Glue ETL job write the refined zone data in a compressed file format',
      'Transition the raw zone data to Glacier Deep Archive via lifecycle policy after 1 day',
      'Have the Glue ETL job write the refined zone data in CSV format',
      'Transition the refined zone data to Glacier Deep Archive via lifecycle policy after 1 day',
    ],
    a: [1, 2],
    x: 'La zona raw no se puede borrar por requisitos de cumplimiento, asi que archivarla a Glacier Deep Archive tras 1 dia es la forma mas barata de retenerla; y escribir la zona refined en formato comprimido reduce su coste de almacenamiento sin afectar las consultas de Athena. La zona refined no se puede archivar porque los analistas la consultan activamente, y escribirla en CSV (sin comprimir) desperdicia espacio frente a un formato comprimido.',
  },
  {
    n: 28,
    d: 'SEC',
    m: 0,
    q: 'Which IAM policy provides read-only access to the S3 bucket "mybucket" and its content?',
    o: [
      'Allow s3:ListBucket and s3:GetObject on Resource "arn:aws:s3:::mybucket/*"',
      'Allow s3:ListBucket on Resource "arn:aws:s3:::mybucket", and separately allow s3:GetObject on Resource "arn:aws:s3:::mybucket/*"',
      'Allow s3:ListBucket and s3:GetObject on Resource "arn:aws:s3:::mybucket"',
      'Allow s3:ListBucket on Resource "arn:aws:s3:::mybucket/*", and separately allow s3:GetObject on Resource "arn:aws:s3:::mybucket"',
    ],
    a: [1],
    x: 's3:ListBucket se aplica al propio bucket (ARN sin barra final) y s3:GetObject se aplica a los objetos dentro de el (ARN con /*), asi que hace falta un statement para cada recurso con la accion correcta. Aplicar ambas acciones al mismo ARN (bucket u objetos) deja sin cubrir el otro nivel, y invertir los ARN entre las dos acciones tampoco produce el resultado correcto.',
  },
  {
    n: 29,
    d: 'RES',
    m: 0,
    q: 'By default, the root volume of an EBS-backed EC2 instance is deleted when the instance terminates. Which setting changes this so the volume persists?',
    o: [
      'Set the DeleteOnTermination attribute to true',
      'Set the TerminateOnDelete attribute to false',
      'Set the DeleteOnTermination attribute to false',
      'Set the TerminateOnDelete attribute to true',
    ],
    a: [2],
    x: 'El atributo DeleteOnTermination controla si el volumen raiz se elimina al terminar la instancia; ponerlo en false hace que el volumen persista tras la terminacion. No existe un atributo llamado TerminateOnDelete, y dejar DeleteOnTermination en true mantiene el comportamiento por defecto de eliminacion.',
  },
  {
    n: 30,
    d: 'PERF',
    m: 0,
    q: 'A financial services company wants a single serverless log processing model for system, application, and database logs, that auto-scales to match throughput and requires no ongoing administration, before storing durably for downstream analytics. Which service fits?',
    o: ['AWS Lambda', 'Amazon EMR', 'Kinesis Data Streams', 'Kinesis Data Firehose'],
    a: [3],
    x: 'Kinesis Data Firehose es un servicio totalmente gestionado que escala automaticamente para igualar el throughput de los datos y entrega de forma duradera a destinos como S3, sin necesitar administracion continua. Lambda es solo computo, EMR exige gestionar un cluster, y Kinesis Data Streams necesita consumidores propios en vez de entregar directamente a almacenamiento duradero.',
  },
  {
    n: 31,
    d: 'SEC',
    m: 0,
    q: 'A financial services company must keep its encryption key stored in a custom on-premises application, while offloading data storage and the encryption process itself to S3. Which S3 encryption option fits?',
    o: [
      'Server-Side Encryption with Customer-Provided Keys (SSE-C)',
      'Server-Side Encryption with S3-Managed Keys (SSE-S3)',
      'Client-Side Encryption, with encryption done before sending to S3',
      'Server-Side Encryption with CMKs Stored in KMS (SSE-KMS)',
    ],
    a: [0],
    x: 'SSE-C permite aportar la propia clave de cifrado en cada peticion PUT/GET a S3, manteniendo la clave gestionada por la aplicacion on-premises mientras S3 realiza el cifrado en si. SSE-S3 y SSE-KMS gestionan las claves dentro de AWS (sin control del cliente), y el cifrado del lado del cliente haria que la aplicacion gestione todo el proceso de cifrado, no S3.',
  },
  {
    n: 32,
    d: 'PERF',
    m: 0,
    q: 'A mobile gaming app on RDS MySQL is growing fast, and the team is worried about running out of database storage. What is the solution that requires the least development and administration effort?',
    o: [
      'Migrate the RDS MySQL database to Aurora, which offers storage auto-scaling',
      'Create a read replica for the RDS MySQL database',
      'Migrate the RDS MySQL database to DynamoDB, which allocates storage automatically',
      'Enable storage auto-scaling for the RDS MySQL database',
    ],
    a: [3],
    x: 'RDS for MySQL soporta storage auto-scaling directamente, incrementando la capacidad automaticamente cuando se acerca al limite asignado, sin necesitar migracion. Migrar a Aurora o a DynamoDB implica un esfuerzo de migracion significativo, y un read replica mejora el rendimiento de lectura, no resuelve la falta de espacio de almacenamiento.',
  },
  {
    n: 33,
    d: 'SEC',
    m: 0,
    q: 'A photo-sharing app on EC2 behind an ELB needs to access S3 (for uploads) and DynamoDB (for a leaderboard). What is the most secure way to grant this access?',
    o: [
      'Save the AWS access key and secret key in a config file within the app code on the instances',
      'Configure the AWS CLI on the instances with a valid IAM user\u2019s credentials for the app to invoke via shell scripts',
      'Attach the appropriate IAM role to the EC2 instance profile',
      'Encrypt the AWS credentials with a custom library and save them in a secret directory on the instances',
    ],
    a: [2],
    x: 'Un rol de IAM asociado al perfil de instancia entrega credenciales temporales automaticamente sin distribuir claves de larga duracion, evitando la gestion y rotacion manual de credenciales. Guardar claves de acceso en texto plano, cifradas con librerias propias, o configuradas via CLI con credenciales de usuario son todas practicas de seguridad deficientes frente al uso de roles.',
  },
  {
    n: 34,
    d: 'SEC',
    m: 0,
    q: 'A set of users from the development environment need delegated access to some resources in a separate production AWS account. What do you recommend?',
    o: [
      'Create new IAM user credentials in the production account and share them with the dev users',
      'Both IAM roles and IAM users can be used interchangeably for cross-account access',
      'It is not possible to access cross-account resources',
      'Create an IAM role with the required permissions in the production account, which the users can assume',
    ],
    a: [3],
    x: 'Los roles de IAM estan pensados exactamente para delegar acceso entre cuentas: los usuarios asumen el rol y obtienen credenciales temporales, sin necesitar credenciales de larga duracion propias en la otra cuenta. Crear usuarios IAM nuevos en produccion es innecesario y menos seguro, y IAM si permite el acceso entre cuentas mediante roles (los roles y los usuarios no son intercambiables para este fin).',
  },
  {
    n: 35,
    d: 'RES',
    m: 0,
    q: 'A private hosted zone was associated with a VPC, but DNS queries for it remain unresolved. Which VPC settings need to be checked?',
    o: [
      'Remove any overlapping namespaces between private and public hosted zones',
      'Enable DNS hostnames and DNS resolution for the VPC',
      'Fix conflicts between the private hosted zone and any Resolver rule for the same domain',
      'Fix the NS and SOA records that may have wrong configurations',
    ],
    a: [1],
    x: 'Las private hosted zones solo se resuelven a traves del servidor DNS de la VPC, por lo que las opciones DNS hostnames y DNS resolution deben estar habilitadas en la VPC para que funcionen; si estan deshabilitadas, las consultas quedan sin resolver. Los namespaces superpuestos, los conflictos con reglas del Resolver, o registros NS/SOA incorrectos son otros posibles problemas de DNS, pero no explican una falta total de resolucion en una zona privada recien asociada.',
  },
  {
    n: 36,
    d: 'PERF',
    m: 0,
    q: 'An on-premises server, connected via Direct Connect, generates video files daily on an NFS file system. After migrating the app to EC2 with a mounted EFS file system, you need to replicate newly created on-premises video files to EFS before cutover. What is the most operationally efficient way?',
    o: [
      'DataSync agent on-premises, transferring over Direct Connect to an S3 bucket via an S3 VPC endpoint, with Lambda copying from S3 to EFS on event notifications',
      'DataSync agent on-premises, transferring over Direct Connect to an S3 bucket via a public VIF, with Lambda copying from S3 to EFS on event notifications',
      'DataSync agent on-premises, transferring over Direct Connect to a VPC peering endpoint for EFS via a private VIF, with a scheduled DataSync task every 24 hours',
      'DataSync agent on-premises, transferring over Direct Connect to a PrivateLink interface VPC endpoint for EFS via a private VIF, with a scheduled DataSync task every 24 hours',
    ],
    a: [3],
    x: 'DataSync puede enviar los datos directamente a EFS a traves de un interface VPC endpoint de PrivateLink, usando una private VIF de Direct Connect, con una tarea programada cada 24 horas, sin pasos intermedios por S3 ni funciones Lambda. VPC peering no aplica a un endpoint de servicio como EFS, y las rutas que pasan primero por S3 con una funcion Lambda anaden pasos y latencia innecesarios frente a la integracion directa de DataSync con EFS.',
  },
  {
    n: 37,
    d: 'PERF',
    m: 0,
    q: 'An application on an Auto Scaling group behind an internal ALB performs best at around 50% CPU utilization. What do you recommend so the application runs near peak performance?',
    o: [
      'A CloudWatch alarm at 50% CPU utilization',
      'A target tracking policy with a CPU utilization target of 50%',
      'A simple scaling policy with a CPU utilization target of 50%',
      'A step scaling policy with a CPU utilization target of 50%',
    ],
    a: [1],
    x: 'Una politica de target tracking mantiene el numero de instancias ajustado automaticamente para que la utilizacion de CPU se mantenga cerca del 50% objetivo, anadiendo o quitando capacidad segun haga falta. Un simple CloudWatch alarm no gestiona el escalado por si solo, y tanto simple scaling como step scaling reaccionan a umbrales predefinidos en vez de mantener un valor objetivo continuo.',
  },
  {
    n: 38,
    d: 'COST',
    m: 0,
    q: 'A company is evaluating pricing for ECS with the EC2 launch type versus ECS with the Fargate launch type. What is correct regarding their pricing?',
    o: [
      'Both are charged based on EC2 instances and EBS volumes used',
      'Both are charged based on vCPU and memory resources the containerized app requests',
      'Both are just charged based on ECS usage per hour',
      'ECS with EC2 is charged based on EC2 instances and EBS volumes used; ECS with Fargate is charged based on vCPU and memory resources requested',
    ],
    a: [3],
    x: 'Con el tipo de lanzamiento EC2, pagas por los recursos de AWS que aprovisionas (instancias EC2, volumenes EBS), mientras que con Fargate pagas por los recursos de vCPU y memoria que solicita la tarea contenedorizada, calculados desde que se descargan las imagenes hasta que termina la tarea. No hay un cargo unico por "ECS usado por hora" comun a ambos modelos, ya que sus mecanismos de facturacion son distintos.',
  },
  {
    n: 39,
    d: 'RES',
    m: 0,
    q: 'A blue-green deployment must be rolled out and tested within the next 48 hours for a global app, where most users are on mobile phones prone to DNS caching. What do you recommend to test across as many users as possible?',
    o: [
      'AWS Global Accelerator to distribute a portion of traffic to a particular deployment',
      'AWS CodeDeploy deployment options to choose the right deployment',
      'An Elastic Load Balancer to distribute traffic across deployments',
      'Route 53 weighted routing to spread traffic across deployments',
    ],
    a: [0],
    x: 'Global Accelerator usa direcciones IP estaticas y enruta en el edge, evitando el problema de la cache DNS en moviles y permitiendo repartir trafico de forma fina entre blue y green casi al instante. CodeDeploy gestiona el despliegue de la aplicacion (no el enrutamiento de trafico global), un ELB solo distribuye dentro de una region, y el weighted routing de Route 53 sigue dependiendo de la resolucion DNS, que se vera retrasada por la cache en moviles dentro de las 48 horas.',
  },
  {
    n: 40,
    d: 'SEC',
    m: 0,
    q: 'A developer configured inbound rules on both the EC2 security group and the subnet\u2019s network ACL, but still cannot connect to the service on the instance. What explains this?',
    o: [
      'NACL rules should never be modified from the command line, or they get blocked',
      'Security groups are stateful (allowing inbound enables the return traffic); NACLs are stateless, so both inbound and outbound must be allowed',
      'The IAM role in the security group differs from the one given access in the NACL',
      'NACLs are stateful and security groups are stateless, so both inbound and outbound must be allowed in the security group',
    ],
    a: [1],
    x: 'Los security groups son con estado (basta con permitir el trafico entrante, la respuesta se permite automaticamente), pero las NACL son sin estado y requieren reglas explicitas tanto de entrada como de salida; probablemente falta la regla outbound en la NACL. No hay restriccion sobre modificar NACL por linea de comandos, IAM no tiene relacion con security groups o NACL, y la afirmacion de que las NACL son con estado y los security groups sin estado es justo lo contrario de la realidad.',
  },
  {
    n: 41,
    d: 'COST',
    m: 0,
    q: 'The team wants to mix On-Demand and Spot Instances across multiple instance types in the same Auto Scaling group. What allows this?',
    o: [
      'Neither a launch configuration nor a launch template supports this',
      'Only a launch configuration supports this',
      'Either a launch configuration or a launch template supports this',
      'Only a launch template supports this',
    ],
    a: [3],
    x: 'Los launch templates soportan mixed instance policies, permitiendo especificar varios tipos de instancia y una mezcla de On-Demand y Spot con estrategias de asignacion configurables. Las launch configurations son la opcion heredada y no soportan multiples tipos de instancia ni mezcla de opciones de compra dentro del mismo Auto Scaling group.',
  },
  {
    n: 42,
    d: 'PERF',
    m: 1,
    q: 'A live, on-demand fitness leaderboard needs high availability, low latency, and real-time processing for customizable user data. Which two in-memory data stores fit? (Choose two.)',
    o: [
      'DynamoDB with DynamoDB Accelerator (DAX)',
      'RDS Aurora',
      'AWS Neptune',
      'ElastiCache Redis',
      'DynamoDB without DAX',
    ],
    a: [0, 3],
    x: 'DynamoDB con DAX da caching en memoria con latencia de microsegundos y alta disponibilidad, y ElastiCache Redis es un almacen en memoria optimizado para baja latencia y ranking en tiempo real; ambos encajan con un leaderboard en vivo. Aurora es relacional (no en memoria) y no llega a la latencia de submilisegundos requerida, Neptune es una base de grafos sin relacion con este caso, y DynamoDB sin DAX no alcanza el rendimiento en memoria necesario.',
  },
  {
    n: 43,
    d: 'COST',
    m: 0,
    q: 'A data processing workflow takes about 60 minutes, can withstand disruptions, and can be started and stopped multiple times. What is the most cost-effective solution?',
    o: ['EC2 Spot Instances', 'EC2 Reserved Instances', 'EC2 On-Demand Instances', 'An AWS Lambda function'],
    a: [0],
    x: 'Las Spot Instances ofrecen hasta un 90% de ahorro frente a On-Demand y encajan con cargas tolerantes a interrupciones y reiniciables, como este workflow de 60 minutos. Reserved Instances son para cargas estables y continuas (no para procesos periodicos e interrumpibles), On-Demand es la opcion mas cara, y Lambda tiene un limite de 15 minutos de ejecucion, insuficiente para un proceso de 60 minutos.',
  },
  {
    n: 44,
    d: 'PERF',
    m: 0,
    q: 'An on-premises Windows Distributed File System Replication (DFSR) service keeps files synchronized across servers, and needs an AWS replacement. What do you recommend?',
    o: ['EFS', 'FSx for Windows File Server', 'Amazon S3', 'FSx for Lustre'],
    a: [1],
    x: 'FSx for Windows File Server soporta el uso de Microsoft DFS para organizar shares en una unica estructura, siendo el reemplazo natural de DFSR con acceso SMB e integracion con Active Directory. EFS y FSx for Lustre estan pensados para Linux, y S3 no se puede montar como sistema de archivos SMB de Windows.',
  },
  {
    n: 45,
    d: 'RES',
    m: 0,
    q: 'What correctly summarizes the replication behavior of RDS Multi-AZ versus RDS read replicas?',
    o: [
      'Multi-AZ is asynchronous and spans at least two AZs in one region; read replicas are synchronous and can be within an AZ, cross-AZ, or cross-region',
      'Multi-AZ is asynchronous and spans one AZ in one region; read replicas are synchronous and can be within an AZ, cross-AZ, or cross-region',
      'Multi-AZ is synchronous and spans at least two AZs in one region; read replicas are asynchronous and can be within an AZ, cross-AZ, or cross-region',
      'Multi-AZ is asynchronous; read replicas are also asynchronous',
    ],
    a: [2],
    x: 'Multi-AZ replica de forma sincrona hacia una instancia standby en otra AZ dentro de la misma region para dar alta disponibilidad, mientras que los read replicas replican de forma asincrona y pueden estar en la misma AZ, en otra AZ, o en otra region para escalar lecturas.',
  },
  {
    n: 46,
    d: 'PERF',
    m: 0,
    q: 'A computer vision application on EC2 takes about 3 minutes to start each time the instance stops and starts, due to auxiliary software that must run. How can the bootstrap time be minimized?',
    o: [
      'Use EC2 metadata',
      'Use EC2 user data',
      'Create an AMI and launch instances from it',
      'Use EC2 Instance Hibernate',
    ],
    a: [3],
    x: 'La hibernacion de EC2 guarda el contenido de la memoria en el volumen raiz de EBS al detener la instancia y lo restaura al arrancarla de nuevo, reanudando los procesos exactamente donde estaban sin repetir el arranque de 3 minutos. User data y una AMI ayudan a automatizar la instalacion inicial, pero no evitan que el software auxiliar se vuelva a ejecutar en cada arranque, y la metadata solo describe la instancia, no acelera nada.',
  },
  {
    n: 47,
    d: 'SEC',
    m: 1,
    q: 'A streaming company has distribution rights only for the US and must deny access to live-streamed matches from other countries. Which two options enforce this? (Choose two.)',
    o: [
      'Route 53 failover routing policy to restrict distribution to the licensed locations',
      'Route 53 geolocation routing policy',
      'Route 53 latency routing policy to restrict distribution to the licensed locations',
      'Route 53 weighted routing policy to restrict distribution to the licensed locations',
      'CloudFront georestriction',
    ],
    a: [1, 4],
    x: 'El enrutamiento por geolocalizacion de Route 53 resuelve las consultas DNS segun la ubicacion del usuario, y la georestriction de CloudFront bloquea o permite la entrega de contenido por pais en el edge; juntas aplican la restriccion de distribucion requerida. El failover, la latencia y el weighted routing estan pensados para disponibilidad, rendimiento o pruebas graduales, no para restringir el acceso por pais.',
  },
  {
    n: 48,
    d: 'COST',
    m: 0,
    q: '100 years of seismological data arrives at 1GB/minute, and only the most relevant attributes are needed to build a predictive model. What is the most cost-effective solution with the least infrastructure maintenance?',
    o: [
      'Ingest with Kinesis Data Firehose, using an intermediary Lambda to filter/transform before writing to S3',
      'Ingest with Kinesis Data Streams, using an intermediary Lambda to filter/transform before writing to S3',
      'Ingest with Kinesis Data Analytics and use SQL to filter/transform before writing to S3',
      'Ingest into a Spark Streaming cluster on EMR, using Spark transformations before writing to S3',
    ],
    a: [0],
    x: 'Firehose con una funcion Lambda intermedia filtra y transforma el stream antes de guardarlo en S3, de forma totalmente serverless y sin mantenimiento de infraestructura, guardando solo los atributos relevantes. Kinesis Data Analytics no ingiere datos directamente de la fuente, Kinesis Data Streams no escribe a S3 de forma nativa sin desarrollo adicional, y un cluster EMR implica gestionar infraestructura, justo lo que se quiere evitar.',
  },
  {
    n: 49,
    d: 'PERF',
    m: 1,
    q: 'CloudFront has regional edge caches that improve latency, but certain content types bypass them and go directly to the origin. Which two content types skip the regional edge cache? (Choose two.)',
    o: [
      'E-commerce assets such as product photos',
      'Proxy methods PUT/POST/PATCH/OPTIONS/DELETE, which go directly to the origin',
      'Dynamic content, as determined at request time when the cache behavior forwards all headers',
      'Static content such as stylesheets and JavaScript files',
      'User-generated videos',
    ],
    a: [1, 2],
    x: 'Los metodos proxy (PUT/POST/PATCH/OPTIONS/DELETE) van directos al origen desde los POPs, y el contenido dinamico determinado en el momento de la peticion (cuando el cache behavior reenvia todas las cabeceras) tampoco pasa por las regional edge caches. Los assets de e-commerce, el contenido generado por usuarios (videos, fotos) y el contenido estatico si suelen pasar por las regional edge caches para mejorar el rendimiento.',
  },
  {
    n: 50,
    d: 'COST',
    m: 0,
    q: 'A 1GB test file is copied into S3 Standard, an EBS gp2 volume (100GB provisioned), and an EFS Standard filesystem. What is the correct cost order for storing that file across these three services?',
    o: [
      'S3 Standard < EBS < EFS',
      'EFS < S3 Standard < EBS',
      'EBS < S3 Standard < EFS',
      'S3 Standard < EFS < EBS',
    ],
    a: [3],
    x: 'S3 Standard cobra solo por el GB realmente usado (1GB), siendo la opcion mas barata; EFS Standard cobra por GB almacenado a un precio mayor que S3; y EBS gp2 cobra por la capacidad aprovisionada (100GB), no por el archivo real, haciendolo el mas caro de los tres en este escenario.',
  },
  {
    n: 51,
    d: 'PERF',
    m: 0,
    q: 'A notification system publishes via SNS, handled by a Lambda function. During peak season, throughput reaches about 5,000 requests/second and a significant number of notifications fail to deliver. What is the best explanation and fix?',
    o: [
      'The team needs to provision more servers running the Lambda service',
      'SNS deliveries to Lambda crossed the account\u2019s Lambda concurrency quota; contact AWS support to raise the limit',
      'SNS has hit a scalability limit; contact AWS support to raise the account limit',
      'The team needs to provision more servers running the SNS service',
    ],
    a: [1],
    x: 'Si las entregas de SNS a Lambda superan la cuota de ejecuciones concurrentes de la cuenta, las entregas se limitan por throttling, y la solucion es pedir a soporte de AWS que aumente ese limite de concurrencia. Tanto SNS como Lambda son servicios totalmente gestionados y serverless, asi que no existe la opcion de "provisionar mas servidores", y SNS en si no tiene un limite de escalabilidad que cause este sintoma.',
  },
  {
    n: 52,
    d: 'PERF',
    m: 0,
    q: 'An on-premises analytical application needs to integrate data files with AWS via an NFS interface. What is the most efficient AWS service for this?',
    o: [
      'Storage Gateway File Gateway',
      'Storage Gateway Tape Gateway',
      'AWS Site-to-Site VPN',
      'Storage Gateway Volume Gateway',
    ],
    a: [0],
    x: 'File Gateway ofrece acceso SMB o NFS con cache local para guardar archivos de aplicacion como objetos duraderos en S3, encajando exactamente con el requisito de integracion via NFS. Volume Gateway presenta volumenes iSCSI (no NFS), Tape Gateway simula cintas de backup (tampoco NFS), y una VPN de Site-to-Site solo da conectividad de red, no una interfaz de archivos.',
  },
  {
    n: 53,
    d: 'COST',
    m: 0,
    q: 'AWS Shield Advanced was enabled across multiple AWS accounts owned by the same company, and costs are much higher than expected. What likely explains this?',
    o: [
      'Shield Advanced is being used for custom servers outside AWS Cloud, raising costs',
      'Consolidated billing is not enabled; all accounts should fall under one consolidated bill for the monthly fee to be charged only once',
      'Savings Plans has not been enabled for Shield Advanced across all accounts',
      'Shield Advanced also covers the Shield Standard plan, increasing costs',
    ],
    a: [1],
    x: 'Si las cuentas no estan bajo la misma facturacion consolidada, la cuota mensual de Shield Advanced se cobra una vez por cada cuenta en lugar de una sola vez para todo el conjunto de cuentas del propietario. Shield Advanced si protege recursos fuera de AWS sin coste adicional inesperado por ello, Shield Standard es gratuito y no genera cargo, y Savings Plans no aplica a Shield Advanced.',
  },
];
