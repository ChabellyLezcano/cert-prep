import type { RawQuestion } from '@/quiz/quiz.types';

/**
 * AWS SAA-C03 practice set 6 (57 questions), authored by Chabe.
 */
export const exam107: RawQuestion[] = [
  {
    n: 1,
    d: 'SEC',
    m: 1,
    q: 'A company is moving from many independent AWS accounts to a structured multi-account setup and wants to authenticate access using its centralized corporate directory. Which two steps should be taken? (Choose two.)',
    o: [
      'Create an AWS Organizations entity with all features enabled and create the new accounts within it',
      'Set up a Cognito identity pool and configure IAM Identity Center to accept Cognito authentication',
      'Create a Transit Gateway for centralized network management linking the accounts',
      'Deploy AWS Directory Service integrated with the corporate directory, and set up IAM Identity Center for cross-account authentication',
      'Install AWS Control Tower and use IAM Identity Center only to manage identity',
    ],
    a: [0, 3],
    x: 'AWS Organizations es la base para una estructura multi-cuenta con gobernanza centralizada, y AWS Directory Service integrado con IAM Identity Center permite autenticar contra el directorio corporativo existente en todas las cuentas. Cognito esta pensado para autenticacion de usuarios de aplicaciones (no acceso empresarial a cuentas AWS), y Transit Gateway resuelve conectividad de red, no autenticacion.',
  },
  {
    n: 2,
    d: 'COST',
    m: 0,
    q: 'Thousands of edge devices generate 1 TB of ~2KB status alerts daily. The solution must be highly available, minimize cost, avoid managing infrastructure, keep 14 days available for analysis, and archive older data. What is the most operationally efficient solution?',
    o: [
      'EC2 instances across two AZs behind an ELB, scripted to store alerts in S3, with a lifecycle rule to Glacier after 14 days',
      'A Kinesis Data Firehose delivery stream to S3, with a lifecycle rule to Glacier after 14 days',
      'A Kinesis Data Firehose delivery stream to OpenSearch, with manual daily snapshots and manual deletion after 14 days',
      'An SQS standard queue with a 14-day retention, with consumers copying old messages to S3 before deleting them',
    ],
    a: [1],
    x: 'Kinesis Data Firehose es serverless y totalmente gestionado, ingiere el TB diario sin aprovisionar infraestructura y entrega directo a S3, donde una lifecycle policy archiva a Glacier tras 14 dias automaticamente. Usar EC2 exige gestionar servidores, OpenSearch con snapshots manuales anade complejidad operativa, y SQS no esta pensado para almacenar 1TB diario ni para archivado a largo plazo.',
  },
  {
    n: 3,
    d: 'SEC',
    m: 0,
    q: 'A 3-tier app has a web EC2 instance in a public subnet, app middleware EC2 in a private subnet, and an RDS MySQL database in a private subnet. The database must only accept connections from the application layer. What ensures this?',
    o: [
      'A route table excluding the public subnets\u2019 CIDR, associated with the database subnets',
      'A security group denying inbound traffic from the public subnets\u2019 security group, attached to the DB instances',
      'A peering connection between public and private subnets, and another between private and database subnets',
      'A security group allowing inbound traffic from the application layer\u2019s security group, attached to the DB instances',
    ],
    a: [3],
    x: 'Un security group que permite trafico entrante solo desde el security group de la capa de aplicacion es la forma correcta de restringir el acceso a la base de datos a un origen especifico. Los security groups no admiten reglas deny, una route table modificada solo rompe el enrutamiento dentro de la VPC, y el peering se usa para conectar VPCs entre si, no subnets dentro de la misma VPC.',
  },
  {
    n: 4,
    d: 'SEC',
    m: 0,
    q: 'An application in a private subnet needs outbound internet access for updates without being exposed to inbound connections. What should be done?',
    o: [
      'Attach an internet gateway to the private subnet and create a NAT gateway',
      'Attach an internet gateway to the VPC but do not create a NAT gateway',
      'Create a NAT gateway but do not attach an internet gateway to the VPC',
      'Create a NAT gateway and attach an internet gateway to the VPC',
    ],
    a: [3],
    x: 'El NAT gateway se crea en una subnet publica y necesita un internet gateway adjunto a la VPC para dar salida a internet a las instancias privadas, sin permitir conexiones entrantes iniciadas desde fuera. Un internet gateway se adjunta a la VPC (no a una subnet), y sin el, el NAT gateway no puede funcionar.',
  },
  {
    n: 5,
    d: 'PERF',
    m: 0,
    q: 'A database migration needs an in-memory caching layer that supports multiple cores and threads for repeated queries. Which service fits?',
    o: [
      'Amazon RDS MySQL',
      'Amazon DynamoDB DAX',
      'Amazon ElastiCache Redis',
      'Amazon ElastiCache Memcached',
    ],
    a: [3],
    x: 'Memcached soporta multiples nucleos e hilos y nodos grandes, encajando con una cache multi-hilo para consultas repetidas. Redis no soporta multiples nucleos de la misma forma, DAX es especifico de DynamoDB, y RDS MySQL no es una base de datos en memoria.',
  },
  {
    n: 6,
    d: 'SEC',
    m: 0,
    q: 'An app migrated to EC2 has an unknown dependency requiring private-IP communication with an on-premises server. Which action provisions connectivity fastest?',
    o: [
      'Configure a Virtual Private Gateway',
      'Create an AWS Transit Gateway',
      'Set up an AWS Direct Connect connection',
      'Create a CloudFront distribution',
    ],
    a: [0],
    x: 'Un virtual private gateway es el endpoint necesario en el lado de AWS para establecer una VPN Site-to-Site gestionada rapidamente hacia el servidor on-premises. Direct Connect tarda demasiado en aprovisionarse, Transit Gateway es para conectar multiples VPCs/redes (no un requisito aqui), y CloudFront es un CDN, no una solucion de conectividad privada.',
  },
  {
    n: 7,
    d: 'RES',
    m: 0,
    q: 'A DR plan requires running a scaled-down but fully functional version of the environment continuously, minimizing recovery time. Which DR strategy fits?',
    o: ['Pilot light', 'Warm standby', 'Backup and restore', 'Multi-site'],
    a: [1],
    x: 'Warm standby mantiene una version reducida pero completamente funcional del entorno siempre activa en la nube, minimizando el tiempo de recuperacion. Pilot light solo mantiene un nucleo minimo de servicios, backup and restore es el enfoque de menor coste pero mayor tiempo de recuperacion, y multi-site implica un entorno activo-activo a plena capacidad.',
  },
  {
    n: 8,
    d: 'SEC',
    m: 0,
    q: 'A product manager without admin access needs to view specific CloudWatch logs/dashboards tied to a past incident, following least privilege. What is the best solution?',
    o: [
      'Share the dashboard via email from the CloudWatch console with a shareable link',
      'Create a dedicated IAM user with CloudWatchReadOnlyAccess attached, and share the dashboard URL',
      'Deploy a bastion host with cached AWS credentials configured to open the dashboard',
      'Create an IAM user with a "ViewOnlyAccess" managed policy and share the dashboard name',
    ],
    a: [1],
    x: 'Crear un usuario IAM dedicado con la politica CloudWatchReadOnlyAccess da exactamente el acceso minimo necesario para ver los dashboards, y compartir la URL directa completa el requisito de forma simple. No existe una funcion de compartir dashboards por email/enlace publico, un bastion host con credenciales en cache es innecesariamente complejo e inseguro, y no existe una politica gestionada llamada "ViewOnlyAccess" que otorgue exactamente ese alcance.',
  },
  {
    n: 9,
    d: 'RES',
    m: 0,
    q: 'A company has multiple VPCs across several AWS accounts and needs routing between all private subnets, with a simple architecture that allows transitive routing. What should be configured?',
    o: [
      'A hub-and-spoke topology with AWS App Mesh, sharing route tables via AWS RAM',
      'An AWS Transit Gateway shared with each account using AWS Resource Access Manager',
      'An AWS Managed VPN between each pair of VPCs with route tables configured',
      'A transitive VPC peering connection between each VPC with route tables configured',
    ],
    a: [1],
    x: 'Transit Gateway soporta de forma nativa el enrutamiento transitivo entre muchas VPCs con una topologia en estrella simple, y AWS RAM permite compartirlo entre cuentas. VPC peering no admite enrutamiento transitivo, una VPN gestionada entre cada par de VPCs es mucho mas compleja de mantener, y App Mesh es para networking a nivel de aplicacion en microservicios, no para VPCs.',
  },
  {
    n: 10,
    d: 'SEC',
    m: 1,
    q: 'A travel agency\u2019s API Gateway REST API needs individual, secure URLs per travel agent with the least operational complexity. Which three steps achieve this? (Choose three.)',
    o: [
      'Request a wildcard ACM certificate matching the custom domain in the same Region as API Gateway',
      'Request a wildcard ACM certificate for the custom domain in a different Region than API Gateway',
      'Set up a custom domain name in API Gateway for the REST API, importing the ACM certificate',
      'Create separate API Gateway endpoints for each travel agent',
      'Register the domain, set up a wildcard custom domain in a Route 53 hosted zone, and create a record pointing to the API Gateway endpoint',
    ],
    a: [0, 2, 4],
    x: 'Un certificado ACM comodin en la misma region que API Gateway, un dominio personalizado en API Gateway usando ese certificado, y un registro comodin en Route 53 apuntando al endpoint cubren subdominios individuales por agente con una sola configuracion. El certificado debe estar en la misma region que API Gateway, y crear un endpoint de API separado por cada agente anade complejidad operativa innecesaria.',
  },
  {
    n: 11,
    d: 'SEC',
    m: 0,
    q: 'A manufacturing company migrating apps into multiple AWS accounts under Organizations needs SSO across all accounts integrated with its existing Active Directory. What is the solution?',
    o: [
      'Deploy an IdP on-premises and enable IAM Identity Center from the console',
      'Use AWS Directory Service to create a two-way trust with the self-managed Active Directory',
      'Enable IAM Identity Center and create a two-way forest trust connecting the self-managed AD via AWS Directory Service for Microsoft AD',
      'Enable IAM Identity Center and create a one-way domain trust connecting the self-managed AD via AWS Directory Service for Microsoft AD',
    ],
    a: [2],
    x: 'IAM Identity Center es el servicio de SSO recomendado para multiples cuentas, y un two-way forest trust con AWS Directory Service for Microsoft AD conecta con el AD existente para autenticar con las credenciales actuales. Un trust de una sola direccion no permite la autenticacion/autorizacion completa necesaria, y desplegar solo un IdP on-premises sin el trust no logra la integracion requerida.',
  },
  {
    n: 12,
    d: 'PERF',
    m: 0,
    q: 'A call center wants multi-speaker recognition and transcript generation, plus SQL querying of the transcripts for business pattern analysis. What is the solution?',
    o: [
      'Amazon Translate for speaker recognition, storing transcripts in Redshift and querying with SQL',
      'Amazon Rekognition for speaker recognition, storing transcripts in S3 and analyzing with ML models',
      'Amazon Transcribe for speaker recognition, querying transcripts with Amazon Athena',
      'Amazon Rekognition for speaker recognition, storing transcripts in S3 and analyzing with Amazon Textract',
    ],
    a: [2],
    x: 'Amazon Transcribe convierte audio a texto e identifica multiples hablantes, y Athena permite consultar esas transcripciones directamente en S3 con SQL estandar sin gestionar infraestructura. Rekognition es para imagenes/video (no audio), Translate es para traduccion, y Textract extrae texto de documentos escaneados, no transcribe audio.',
  },
  {
    n: 13,
    d: 'COST',
    m: 0,
    q: 'Log files up to 1GB each, generated at unpredictable intervals, take ~40 seconds to process. What is the most cost-effective solution?',
    o: [
      'Write logs to an EC2 instance with an attached EBS volume, then save the processed files to S3',
      'Write logs to an SQS queue, using Lambda to process the files from the queue and save to S3',
      'Write logs to an S3 bucket, with an event notification invoking a Lambda function to process the files',
      'Write logs to an S3 bucket, with an event notification invoking an ECS task to process the files',
    ],
    a: [2],
    x: 'Una notificacion de evento de S3 que invoca directamente una funcion Lambda es la solucion serverless mas economica: solo se paga por el tiempo de ejecucion real, sin gestionar servidores. Usar EC2 no es serverless ni coste-efectivo, SQS no es necesario para archivos de hasta 1GB (supera el limite de mensaje de SQS de 256KB), y S3 no puede invocar tareas de ECS directamente via notificaciones de evento.',
  },
  {
    n: 14,
    d: 'RES',
    m: 0,
    q: 'A multi-tier medical records app across three AZs (ALB + EC2 session tier + a PostgreSQL database on EC2) must scale for a surge in traffic and stay highly available across all three AZs. What do you recommend?',
    o: [
      'Migrate to RDS PostgreSQL Multi-AZ, use ElastiCache for Redis with a replication group for session data, and put the app servers in an ASG across three AZs',
      'Keep PostgreSQL on EC2, use ElastiCache for Redis for session data, and put the app servers in an ASG across three AZs',
      'Migrate to Aurora PostgreSQL single-AZ, use ElastiCache for Memcached for session data, and put the app servers in an ASG across three AZs',
      'Migrate to DynamoDB, use DAX to cache reads, store sessions in DynamoDB, and put the app servers in an ASG across three AZs',
    ],
    a: [0],
    x: 'RDS Multi-AZ da failover automatico para la base de datos, ElastiCache Redis con grupo de replicacion soporta sesiones con durabilidad y baja latencia, y el ASG en tres AZs completa la alta disponibilidad de toda la pila. Mantener PostgreSQL en EC2 pierde los beneficios gestionados de RDS, single-AZ no cumple el requisito de alta disponibilidad, y migrar a DynamoDB exigiria reescribir la logica de la aplicacion sin necesidad real.',
  },
  {
    n: 15,
    d: 'SEC',
    m: 0,
    q: 'A microservices app on EKS must encrypt sensitive data in Kubernetes ConfigMaps and Secrets at rest. What do you recommend?',
    o: [
      'Create the EKS cluster with default options and add the EFS CSI driver',
      'Implement AWS Secrets Manager to manage and rotate all sensitive data, integrated with EKS',
      'Create a new KMS key and enable EKS KMS secrets encryption on the cluster',
      'Store all sensitive data in S3 with server-side encryption using a new KMS key',
    ],
    a: [2],
    x: 'EKS soporta cifrado envelope de los Secrets de Kubernetes usando una clave de KMS, activandolo directamente en la configuracion del cluster para cifrar lo que se guarda en etcd. Secrets Manager no cifra directamente lo almacenado en etcd, el driver CSI de EFS no aporta cifrado de Secrets, y guardar los datos en S3 no resuelve el cifrado de los ConfigMaps/Secrets de Kubernetes en si.',
  },
  {
    n: 16,
    d: 'SEC',
    m: 0,
    q: 'A financial company needs to track all configuration changes to AWS resources and have detailed records of who accessed the environment and what API calls they made. What should be used?',
    o: [
      'CloudWatch to track configuration changes and Config to record API calls',
      'Macie to track configuration changes and CloudTrail to record API calls',
      'Config to track configuration changes and CloudTrail to record API calls',
      'Config to track configuration changes and EventBridge to record API calls',
    ],
    a: [2],
    x: 'AWS Config rastrea y audita cambios de configuracion de los recursos, mientras que CloudTrail registra en detalle la actividad de la API, incluidos los inicios de sesion y las llamadas realizadas por cada usuario. CloudWatch no rastrea configuraciones, Macie detecta datos sensibles en S3 (no cambios de configuracion), y EventBridge es un bus de eventos, no un servicio de auditoria de llamadas a la API.',
  },
  {
    n: 17,
    d: 'PERF',
    m: 0,
    q: 'A gaming company wants to gradually roll out a significant API Gateway update while monitoring for issues and minimizing impact on existing users. What should they do?',
    o: [
      'Update the existing API directly and immediately direct all traffic to it',
      'Create a new version of the API and use Route 53 to gradually shift DNS queries to it',
      'Create a completely new API and redirect half of user traffic to it',
      'Use an API Gateway canary release deployment, directing a small percentage of traffic to the new version before promoting it',
    ],
    a: [3],
    x: 'Un despliegue canary de API Gateway dirige inicialmente solo un pequeno porcentaje de trafico a la nueva version, permitiendo verificar su comportamiento antes de promoverla por completo a produccion. Actualizar directamente expone a todos los usuarios al riesgo de inmediato, crear una API completamente nueva con la mitad del trafico no es un rollout gradual, y usar Route 53 para cambiar DNS es mas lento e imprevisible que el canary nativo de API Gateway.',
  },
  {
    n: 18,
    d: 'SEC',
    m: 0,
    q: 'A team manages RDS MySQL databases and wants to enable secure user access with short-lived credentials. How can this be met?',
    o: [
      'Configure the MySQL databases to use AWS STS directly',
      'Configure the application to use the AUTH command to send a unique password',
      'Create the MySQL user accounts to use the AWSAuthenticationPlugin with IAM',
      'Configure the MySQL databases to use AWS KMS data encryption keys',
    ],
    a: [2],
    x: 'RDS for MySQL soporta autenticacion de base de datos con IAM usando el AWSAuthenticationPlugin, permitiendo a los usuarios conectarse con tokens IAM de corta duracion en lugar de contrasenas de larga duracion. STS emite credenciales para llamadas a la API de AWS (no para login directo en MySQL), el comando AUTH de MySQL sigue usando contrasenas estaticas, y KMS gestiona claves de cifrado, no autenticacion de usuarios.',
  },
  {
    n: 19,
    d: 'PERF',
    m: 0,
    q: 'A developer wrote a Lambda function to process a webhook from a third-party song feed and must expose it for the third party to invoke, with the least operational complexity. What do you recommend?',
    o: [
      'Generate an API Gateway endpoint for the Lambda function and share it with the third party',
      'Create an SNS topic linked to the Lambda function and share the topic ARN with the third party',
      'Deploy a Network Load Balancer to distribute requests to the Lambda function and share the NLB URL',
      'Create an SQS queue connected to the Lambda function and share the queue ARN with the third party',
    ],
    a: [0],
    x: 'API Gateway crea un endpoint HTTP publico que puede invocar la funcion Lambda directamente, siendo la forma mas simple y directa de exponerla para un webhook externo. SNS y SQS no exponen endpoints publicos invocables desde internet por terceros, y un NLB delante de Lambda anade complejidad innecesaria frente a la integracion nativa de API Gateway.',
  },
  {
    n: 20,
    d: 'COST',
    m: 0,
    q: 'A DynamoDB table keeps growing, but the application only needs data from the most recent 30 days. What is the lowest-cost, lowest-effort solution?',
    o: [
      'Run a monitoring app on EC2 using DynamoDB Streams to track timestamps and script deletion of old items',
      'Redeploy the entire solution via CloudFormation every 30 days, deleting the original stack',
      'Add a TTL attribute set to the current timestamp plus 30 days, configuring it as the table\u2019s TTL attribute',
      'Have DynamoDB Streams invoke a Lambda function on every new item to delete items older than 30 days',
    ],
    a: [2],
    x: 'DynamoDB Time to Live (TTL) elimina automaticamente los items una vez pasada la marca de tiempo definida, sin coste adicional ni consumo de capacidad de escritura, resolviendo el requisito de forma nativa. Las otras opciones requieren infraestructura o codigo adicional (EC2, CloudFormation, Lambda disparado por Streams) para lograr lo mismo con mucho mas esfuerzo operativo.',
  },
  {
    n: 21,
    d: 'COST',
    m: 0,
    q: 'EC2 costs increased due to unwanted vertical scaling of instance types, and the team needs a graph comparing the last 2 months of costs with in-depth analysis by instance type. What has the least operational overhead?',
    o: [
      'Use AWS Budgets to create a budget report comparing EC2 costs by instance type',
      'Use Cost Explorer\u2019s granular filtering to analyze EC2 costs by instance type',
      'Use Cost and Usage Reports sent to S3, with QuickSight using S3 as a source for interactive graphs',
      'Use graphs from the AWS Billing and Cost Management dashboard to compare EC2 costs by instance type',
    ],
    a: [1],
    x: 'Cost Explorer permite filtrar y graficar los costes con el nivel de detalle necesario (por tipo de instancia, por periodo) directamente desde la consola, sin configuracion adicional. AWS Budgets esta pensado para alertas de umbral (no para analisis grafico detallado), el dashboard de Billing no ofrece esa granularidad sin el Cost and Usage Report, y montar QuickSight sobre el CUR en S3 implica mucho mas esfuerzo operativo del necesario.',
  },
  {
    n: 22,
    d: 'RES',
    m: 1,
    q: 'A social app converts uploaded images to thumbnails: store the original in S3, process/compress with Lambda, store the result in another S3 bucket. The solution must be stateless, durable, and process images automatically on upload. Which two actions achieve this? (Choose two.)',
    o: [
      'Launch an EC2 instance connected to an SQS queue that logs the file name to a local text file and invokes the Lambda function',
      'Configure the Lambda function to use the SQS queue as its event source, resizing the image and storing it in a separate bucket',
      'Configure an EventBridge rule to monitor the S3 bucket and send an email alert via SNS for manual processing',
      'Configure the S3 bucket as a Lambda event source, writing the processed file name to a text file in memory to track progress',
      'Create an SQS queue and configure an S3 event notification to add a message to it when an image is uploaded',
    ],
    a: [1, 4],
    x: 'Una notificacion de evento de S3 que anade un mensaje a una cola SQS al subir una imagen, combinada con una funcion Lambda que usa esa cola como event source para redimensionar y guardar el resultado, forma una solucion serverless, duradera y sin estado. Guardar el progreso en memoria no es duradero, un unico EC2 no es resiliente, y enviar solo un email para procesamiento manual no automatiza el flujo.',
  },
  {
    n: 23,
    d: 'PERF',
    m: 1,
    q: 'A containerized multi-tier app runs on Linux EC2 instances connecting to a PostgreSQL database on separate dedicated EC2 instances. The company wants to optimize operational efficiency and performance. Which two actions should be taken? (Choose two.)',
    o: [
      'Set up ElastiCache between the web application and the PostgreSQL database',
      'Migrate the PostgreSQL database to Amazon Aurora',
      'Migrate the web application to run on the same EC2 instances as the database',
      'Set up a CloudFront distribution for the web application content',
      'Migrate the web application to be hosted on AWS Fargate with Amazon ECS',
    ],
    a: [1, 4],
    x: 'Migrar a Aurora mejora el rendimiento y reduce la gestion operativa frente a PostgreSQL autogestionado en EC2, y mover la aplicacion a Fargate con ECS elimina la necesidad de gestionar el clustering de contenedores, siendo serverless. Consolidar en las mismas instancias que la base de datos no aporta beneficios reales, CloudFront ayuda con contenido cacheable pero no reduce la carga operativa de la arquitectura, y ElastiCache solo ayuda si hay datos "calientes" repetidos.',
  },
  {
    n: 24,
    d: 'RES',
    m: 0,
    q: 'A digital marketing agency wants all AWS resources, including untagged ones, backed up properly, with the least operational overhead. What do you recommend?',
    o: [
      'Use Lambda to periodically scan for untagged resources, add tags, then set up AWS Backup',
      'Rely on each account owner to identify their untagged resources, then use AWS Backup',
      'Manually search for untagged resources per service, tag them, and set up AWS Backup separately per service',
      'Use AWS Config to identify all untagged resources and tag them programmatically, then use AWS Backup to automate backups based on tags',
    ],
    a: [3],
    x: 'AWS Config detecta recursos sin las etiquetas requeridas y puede corregirlos programaticamente, y AWS Backup automatiza la copia de seguridad basada en esas etiquetas, cubriendo el ciclo completo con minimo esfuerzo manual. Depender de cada propietario de cuenta es inconsistente, buscar manualmente por servicio es muy laborioso, y un Lambda personalizado para escanear y etiquetar anade complejidad de desarrollo evitable frente a Config.',
  },
  {
    n: 25,
    d: 'COST',
    m: 0,
    q: 'A web app places order messages on an SQS queue picked up by EC2 instances, and there is a large number of empty responses to ReceiveMessage requests. How can this be eliminated to reduce operational overhead?',
    o: [
      'Use a FIFO queue to preserve the exact order of messages',
      'Use a standard queue to provide at-least-once delivery',
      'Configure long polling so SQS waits until a message is available before responding',
      'Configure short polling to reduce the length of time a connection stays open',
    ],
    a: [2],
    x: 'El long polling hace que SQS espere hasta que haya un mensaje disponible antes de responder, eliminando las respuestas vacias y reduciendo el numero de llamadas facturables. El orden y la entrega (FIFO vs standard) no tienen relacion con este problema, y el short polling es precisamente el comportamiento por defecto que causa las respuestas vacias.',
  },
  {
    n: 26,
    d: 'PERF',
    m: 0,
    q: 'On-premises semi-structured key-value data is being migrated with the application code to Lambda, needing a highly scalable data store. Which fits best?',
    o: ['Amazon EBS', 'Amazon EFS', 'Amazon RDS MySQL', 'Amazon DynamoDB'],
    a: [3],
    x: 'DynamoDB es una base NoSQL clave-valor pensada para grandes volumenes de datos semi-estructurados, con escalabilidad practicamente ilimitada y buena integracion con Lambda. EFS no es apto para pares clave-valor, RDS MySQL es relacional (esquema fijo tipo SQL), y EBS es almacenamiento de bloque para instancias, no un datastore para Lambda.',
  },
  {
    n: 27,
    d: 'RES',
    m: 0,
    q: 'An on-premises MySQL server must be migrated to AWS with a managed solution supporting high availability and automatic failover during an AZ outage. What is the best fit?',
    o: [
      'Snapshot the on-premises server, use DataSync to move it to S3, and launch a new RDS MySQL Multi-AZ deployment from the snapshot',
      'Use AWS DMS to directly migrate the database to RDS MySQL, using SCT to convert the schema',
      'Use AWS DMS to directly migrate the database to an EC2 MySQL Multi-AZ deployment',
      'Use AWS DMS to directly migrate the database to an RDS MySQL Multi-AZ deployment',
    ],
    a: [3],
    x: 'AWS DMS puede migrar directamente y en linea la base de datos hacia un despliegue RDS MySQL Multi-AZ, que ofrece alta disponibilidad y failover automatico gestionado por AWS. No existe "Multi-AZ" para MySQL corriendo directamente en EC2, no se puede crear un snapshot de un servidor MySQL on-premises de esa forma, y no hace falta conversion de esquema (SCT) al migrar de MySQL a MySQL.',
  },
  {
    n: 28,
    d: 'SEC',
    m: 0,
    q: 'A third-party virtual firewall appliance in an inspection VPC must inspect all traffic to a web application before it reaches the web server, with the least operational overhead. What do you recommend?',
    o: [
      'A Network Load Balancer in the public subnet of the app\u2019s VPC routing traffic to the appliance',
      'A Transit Gateway in the inspection VPC, with route tables routing incoming packets through it',
      'A Gateway Load Balancer in the inspection VPC, with a Gateway Load Balancer endpoint receiving and forwarding packets to the appliance',
      'An Application Load Balancer in the public subnet of the app\u2019s VPC routing traffic to the appliance',
    ],
    a: [2],
    x: 'El Gateway Load Balancer esta disenado especificamente para desplegar y escalar appliances virtuales de inspeccion, combinando un gateway transparente de red con distribucion de trafico hacia el firewall. NLB y ALB no estan pensados para inspeccion de paquetes, y un Transit Gateway enruta trafico entre redes pero no realiza ni distribuye la inspeccion en si.',
  },
  {
    n: 29,
    d: 'RES',
    m: 0,
    q: 'A business-critical app on EC2 needs a DR solution across regions with an RTO of 4 hours or less, using minimal resources during normal operations, in the most cost-effective way. What do you recommend?',
    o: [
      'Create AMIs of the instances, copy them to a secondary region, and automate deployment there with CloudFormation',
      'Launch EC2 instances in a secondary region and keep them active at all times',
      'Launch EC2 instances in a secondary Availability Zone and keep them active at all times',
      'Create AMIs of the instances, copy them to a secondary region, and automate deployment there with Lambda and custom scripts',
    ],
    a: [0],
    x: 'Copiar AMIs a una region secundaria y usar CloudFormation para desplegar rapidamente cuando ocurra un desastre da un RTO de pocas horas sin mantener recursos activos innecesariamente, siendo la opcion mas coste-efectiva. Mantener instancias activas permanentemente (en otra region o AZ) es mucho mas caro, y usar Lambda con scripts personalizados es menos adecuado que CloudFormation para desplegar infraestructura completa.',
  },
  {
    n: 30,
    d: 'COST',
    m: 0,
    q: 'An EC2 instance needs high throughput for a MapReduce process with large I/O sizes over a large dataset. Which EBS volume is the most cost-effective for this?',
    o: [
      'EBS Provisioned IOPS SSD',
      'EBS Throughput Optimized HDD',
      'EBS General Purpose SSD',
      'EBS General Purpose SSD in a RAID 1 configuration',
    ],
    a: [1],
    x: 'Throughput Optimized HDD esta disenado justo para cargas de alto throughput con datasets grandes y E/S grande, como MapReduce, siendo la opcion mas economica entre las que cumplen ese perfil. Las opciones SSD (General Purpose o Provisioned IOPS) son mas caras y estan orientadas a IOPS altos con tamanos de E/S pequenos, no al throughput secuencial masivo.',
  },
  {
    n: 31,
    d: 'RES',
    m: 0,
    q: 'How many of the most recent snapshots of an EBS volume must be retained to guarantee that the full volume can be recreated?',
    o: [
      'Two snapshots, the oldest and the most recent',
      'All snapshots must be retained since the process is incremental',
      'Only the most recent snapshot; the deletion process ensures no data is lost',
      'The oldest snapshot, since it references data in all other snapshots',
    ],
    a: [2],
    x: 'Aunque los snapshots de EBS son incrementales, el proceso de eliminacion de snapshots antiguos esta disenado para que solo haga falta conservar el snapshot mas reciente para poder restaurar el volumen completo. No es necesario guardar todos los snapshots ni el mas antiguo especificamente.',
  },
  {
    n: 32,
    d: 'COST',
    m: 0,
    q: 'A critical Python job runs every 15 minutes for about 2 minutes, using 1.5GB memory and heavy CPU. The company wants to minimize costs. What do you recommend?',
    o: [
      'Implement the app as a Lambda function with 1.5GB memory, scheduled every 15 minutes via EventBridge',
      'Containerize the app with App2Container and run it on ECS Fargate with 1 vCPU and 1.5GB memory',
      'Containerize the app with App2Container and run it on EC2, using a CloudWatch alarm to stop the instance when idle',
      'Deploy the app on an EC2 instance and manually start/stop it according to the schedule',
    ],
    a: [0],
    x: 'Lambda cobra solo por el tiempo de ejecucion real (unos 2 minutos cada 15 minutos), y EventBridge programa la invocacion de forma nativa, siendo la opcion mas economica para un job corto y periodico. Usar Fargate o EC2 implica pagar por capacidad reservada o gestionar arranques/paradas, resultando mas caro y con mas esfuerzo operativo para una carga tan breve.',
  },
  {
    n: 33,
    d: 'PERF',
    m: 0,
    q: 'A large quantity of production data on EBS needs to be cloned into a test environment in the same Region, with modifications not affecting production and consistently high I/O performance. What minimizes the time required to clone the data?',
    o: [
      'Snapshot the volumes, create and initialize new EBS volumes, attach them, then restore from the snapshots',
      'Snapshot the volumes, enable EBS fast snapshot restore, restore into new volumes, and attach them to the test instances',
      'Snapshot the volumes and restore the snapshots onto EC2 instance store volumes in the test environment',
      'Configure EBS Multi-Attach on the production volumes, snapshot them, and attach the production volumes to the test instances',
    ],
    a: [1],
    x: 'EBS fast snapshot restore inicializa completamente el volumen al crearlo desde el snapshot, eliminando la latencia de la primera lectura de cada bloque y entregando el rendimiento aprovisionado desde el primer momento. No se puede restaurar un snapshot a un volumen instance store (efimero), y Multi-Attach solo permite compartir el mismo volumen entre instancias de la misma AZ, sin aislar produccion de pruebas.',
  },
  {
    n: 34,
    d: 'RES',
    m: 0,
    q: 'A company needs seamless failover between AWS Regions with minimal downtime and data loss, running the application in an active-active configuration. Which DR strategy fits?',
    o: ['Multi-site', 'Warm standby', 'Pilot light', 'Backup and restore'],
    a: [0],
    x: 'Multi-site es la estrategia activa-activa donde el entorno corre a plena capacidad en varias ubicaciones simultaneamente, minimizando el tiempo de inactividad y la perdida de datos en un failover. Warm standby y pilot light mantienen capacidad reducida hasta el momento del desastre, y backup and restore es el enfoque de mayor tiempo de recuperacion.',
  },
  {
    n: 35,
    d: 'SEC',
    m: 0,
    q: 'A company using AWS Organizations wants to limit S3 access to only accounts enrolled in the organization, avoiding listing dozens of frequently-changing account IDs in the bucket policy. What is the least-effort strategy?',
    o: [
      'Add all non-organizational accounts to an OU and attach an SCP denying access to the bucket',
      'Use Config and Lambda to remediate the bucket policy whenever accounts are created or removed from the organization',
      'Use Attribute Based Access Control referencing tags on accounts that are enrolled or not',
      'Use the aws:PrincipalOrgID global key in a bucket policy to allow access only to accounts in the Organization',
    ],
    a: [3],
    x: 'La clave global aws:PrincipalOrgID permite referenciar toda la organizacion en una sola condicion de la bucket policy, sin necesitar listar ni actualizar manualmente los IDs de cuenta segun cambian. Mantener tags de forma manual es propenso a errores, construir un flujo con Config+Lambda es innecesariamente complejo, y las SCP solo aplican a cuentas que ya son miembros de la organizacion.',
  },
  {
    n: 36,
    d: 'SEC',
    m: 1,
    q: 'Within the default VPC\u2019s default security group, which two settings are configured by default? (Choose two.)',
    o: [
      'An outbound rule allowing traffic to the VPC router',
      'An inbound rule allowing all traffic from the security group itself',
      'An outbound rule allowing all traffic to the security group itself',
      'An outbound rule allowing all traffic to all addresses',
      'An inbound rule allowing all traffic from any address',
    ],
    a: [1, 3],
    x: 'El security group por defecto incluye una regla inbound que permite todo el trafico proveniente de instancias asociadas al mismo security group, y una regla outbound que permite todo el trafico saliente hacia cualquier destino. No hay una regla inbound que permita trafico desde cualquier direccion externa, ni reglas outbound limitadas especificamente al router de la VPC o solo al propio security group.',
  },
  {
    n: 37,
    d: 'PERF',
    m: 0,
    q: 'An EC2 instance is generating very high packets-per-second and the application stack performance is impacted. What should be done to resolve this?',
    o: [
      'Configure a RAID 1 array from multiple EBS volumes',
      'Create a placement group and put the EC2 instance in it',
      'Use enhanced networking',
      'Add multiple Elastic IP addresses to the instance',
    ],
    a: [2],
    x: 'El enhanced networking (SR-IOV) da acceso directo al adaptador de red, mayor throughput y mejor rendimiento de paquetes por segundo con menor latencia, resolviendo justo el cuello de botella descrito. RAID 1 aporta redundancia (no rendimiento de red), un placement group con una sola instancia no ayuda, y anadir mas Elastic IPs no aumenta el ancho de banda disponible en la ENI.',
  },
  {
    n: 38,
    d: 'PERF',
    m: 0,
    q: 'A PowerShell script needs to run on a fleet of Windows EC2 instances already launched in a VPC, executed from the AWS Management Console. What tool should be used?',
    o: ['AWS CodeDeploy', 'AWS Config', 'Run Command', 'AWS OpsWorks'],
    a: [2],
    x: 'Systems Manager Run Command permite ejecutar scripts o comandos de PowerShell ad-hoc en muchas instancias a la vez desde la consola, sin necesitar SSH/RDP individual. CodeDeploy es un servicio de despliegue de aplicaciones, Config es para evaluar configuraciones, y OpsWorks gestiona instancias de Puppet/Chef, ninguno pensado para ejecucion ad-hoc de scripts.',
  },
  {
    n: 39,
    d: 'RES',
    m: 0,
    q: 'An app runs on EC2 in a private subnet behind an internal ALB, distributed across AZs, and needs highly available outbound internet connectivity for API calls. How should this be enabled?',
    o: [
      'Create a NAT instance in the private subnet of each AZ, updating each route table to point to it',
      'Create a NAT gateway in the public subnet of each AZ, updating each private subnet\u2019s route table to point to its own NAT gateway',
      'Configure an internet gateway and add a route to it from each private subnet route table',
      'Create a NAT gateway and attach it directly to the VPC, adding a route from each private subnet route table',
    ],
    a: [1],
    x: 'Un NAT gateway por AZ, colocado en la subnet publica correspondiente y referenciado desde la route table de la subnet privada de esa misma AZ, evita un punto unico de fallo por AZ. Los NAT gateways no se crean en subnets privadas, no se adjuntan directamente a la VPC (van en una subnet publica), y un internet gateway no se puede enrutar desde una subnet privada sin exponer las instancias.',
  },
  {
    n: 40,
    d: 'PERF',
    m: 0,
    q: 'An application requires extremely high throughput and low latency over TCP, with load balancing to multiple ports on an instance. Which load balancer should be used?',
    o: ['Application Load Balancer', 'Amazon Route 53', 'Classic Load Balancer', 'Network Load Balancer'],
    a: [3],
    x: 'El Network Load Balancer opera a nivel de conexion (capa 4), soporta millones de peticiones por segundo con latencia muy baja y permite balancear hacia multiples puertos en una misma instancia. El Classic Load Balancer no ofrece ese nivel de throughput/latencia ni ese balanceo multipuerto, el ALB opera solo a nivel HTTP/HTTPS, y Route 53 es DNS, no un load balancer.',
  },
  {
    n: 41,
    d: 'SEC',
    m: 0,
    q: 'A software firm wants to centrally manage and distribute a standard set of solutions and tools for its clients\u2019 self-service needs. What best satisfies this?',
    o: [
      'Create CloudFormation stacks for the clients',
      'Create Config rules for the clients',
      'Create Systems Manager documents for the clients',
      'Create AWS Service Catalog portfolios for the clients',
    ],
    a: [3],
    x: 'AWS Service Catalog esta pensado exactamente para catalogar y distribuir de forma centralizada un conjunto estandar de servicios/herramientas aprobados que los clientes pueden usar de forma self-service. CloudFormation por si solo no ofrece descubrimiento self-service ni control de acceso centralizado, Config evalua cumplimiento (no distribuye herramientas), y los documentos de Systems Manager definen acciones de automatizacion, no un catalogo de autoservicio.',
  },
  {
    n: 42,
    d: 'PERF',
    m: 0,
    q: 'A serverless web app must interact with numerous DynamoDB tables reliably without impacting performance, in the most operationally efficient way. What do you recommend?',
    o: [
      'Amazon S3 with Lambda triggers',
      'AWS AppSync with multiple data sources and resolvers',
      'AWS Lambda with Step Functions',
      'AWS Glue with a DynamoDB connector',
    ],
    a: [1],
    x: 'AppSync ofrece una capa GraphQL con resolvers que mapean directamente hacia operaciones de DynamoDB, escalando automaticamente y evitando logica de orquestacion compleja para consultar varias tablas. S3 con Lambda triggers es para procesamiento de eventos de subida de objetos, Step Functions anade orquestacion innecesaria para consultas simples, y Glue es para ETL/analitica batch, no para consultas de baja latencia en tiempo real.',
  },
  {
    n: 43,
    d: 'SEC',
    m: 0,
    q: 'Logged into an EC2 Linux instance via SSH, what is the best source to determine the assigned IAM role, instance ID, and attached security group names?',
    o: ['User data', 'Metadata', 'Tags', 'Parameters'],
    a: [1],
    x: 'La metadata de instancia (disponible en http://169.254.169.254/latest/meta-data) contiene informacion propia de la instancia como el rol IAM, el instance ID y los security groups asociados. Los tags categorizan recursos, el user data configura el sistema al arrancar (no informa sobre el estado actual), y los parametros no son un concepto relevante para EC2 en este contexto.',
  },
  {
    n: 44,
    d: 'RES',
    m: 1,
    q: 'To be able to launch EC2 instances in another Region in the event of a disaster, which two steps should be taken? (Choose two.)',
    o: [
      'Launch instances in the second Region using the S3 API',
      'Copy the snapshots using Amazon S3 cross-region replication',
      'Create AMIs of the instances and copy them to another Region',
      'Enable cross-region snapshots for the EC2 instances',
      'Launch instances in the second Region from the AMIs',
    ],
    a: [2, 4],
    x: 'Crear AMIs de las instancias y copiarlas a otra region da una copia puntual del estado de las instancias en la region remota, y desde esas AMIs se pueden lanzar nuevas instancias cuando haga falta. No existe una funcion de "snapshots cross-region" automatica como tal, los snapshots no se acceden ni replican via la API de S3, sino via la API de EC2.',
  },
  {
    n: 45,
    d: 'PERF',
    m: 1,
    q: 'A microservices app on EKS needs the cluster to scale up and down with fluctuating traffic, with minimal operational overhead. Which two steps satisfy this? (Choose two.)',
    o: [
      'Integrate SQS and connect it to EKS for workload management',
      'Use X-Ray to track and analyze the application\u2019s network activity',
      'Use the Kubernetes Metrics Server to enable horizontal pod autoscaling based on resource utilization',
      'Employ the Kubernetes Cluster Autoscaler to dynamically manage the number of nodes in the cluster',
      'Implement the Kubernetes Vertical Pod Autoscaler to adjust CPU/memory allocation for pods',
    ],
    a: [2, 3],
    x: 'El Metrics Server alimenta al Horizontal Pod Autoscaler para escalar el numero de pods segun uso de recursos, y el Cluster Autoscaler ajusta el numero de nodos del cluster segun la demanda de esos pods, cubriendo el escalado completo de la aplicacion. El Vertical Pod Autoscaler ajusta recursos por pod (no el numero de pods/nodos), y SQS o X-Ray no gestionan directamente el autoescalado del cluster.',
  },
  {
    n: 46,
    d: 'RES',
    m: 0,
    q: 'An EC2 instance behind an ELB is being deregistered. Which ELB feature allows existing connections to close cleanly?',
    o: ['Proxy Protocol', 'Sticky Sessions', 'Deletion Protection', 'Connection Draining'],
    a: [3],
    x: 'El connection draining (deregistration delay) da un periodo de tiempo para que las conexiones existentes hacia una instancia que se esta des-registrando terminen de forma ordenada antes de cortarlas. Sticky sessions vincula un cliente a una instancia via cookie, el Proxy Protocol transmite la IP original del cliente, y deletion protection solo evita que se elimine el propio load balancer.',
  },
  {
    n: 47,
    d: 'PERF',
    m: 0,
    q: 'Most API calls behind an ALB complete quickly, but a single endpoint takes much longer, introducing overall latency into the system. What minimizes the effects of these long-running calls?',
    o: [
      'Create an SQS queue and decouple the long-running API calls',
      'Change the EC2 instance to one with enhanced networking',
      'Change the ALB to a Network Load Balancer with SSL/TLS termination',
      'Increase the ALB idle timeout to allow the long-running requests to complete',
    ],
    a: [0],
    x: 'Desacoplar las llamadas de larga duracion mediante una cola SQS permite procesarlas de forma asincrona en instancias separadas, evitando que su latencia afecte al resto del sistema. El enhanced networking mejora la red (no la duracion propia de la llamada), cambiar a NLB con TLS no ataca el problema de fondo, y aumentar el idle timeout solo evita que se corte la conexion, sin reducir la latencia real.',
  },
  {
    n: 48,
    d: 'RES',
    m: 0,
    q: 'A multi-region app with Aurora spanning multiple AZs in one region is launching in a new geographic area, needing maximum availability with minimal service interruption. What strategy should be adopted?',
    o: [
      'Set up the app layer in the new region, use Aurora Global Database across primary and new regions, and use Route 53 failover health checks with manual promotion as needed',
      'Expand the existing Auto Scaling group into the new region, extend the database with Aurora Global Database, and use Route 53 failover health checks',
      'Set up a similar app layer and a new Aurora MySQL database in the new region, using DMS for ongoing replication and Route 53 failover health checks',
      'Replicate the app layer in the new region, implement an Aurora MySQL Read Replica there with Route 53 failover, promoting it to primary on failure',
    ],
    a: [0],
    x: 'Aurora Global Database replica entre regiones con latencia muy baja y permite una promocion manual rapida ante un fallo, y Route 53 con health checks de failover dirige el trafico a la region correcta, dando la maxima disponibilidad con minima interrupcion. Un Auto Scaling group no se puede extender entre regiones, DMS es mas apropiado para migracion/replicacion puntual (no failover continuo de alta disponibilidad), y un read replica tarda mas en promocionarse que una region secundaria de Aurora Global Database.',
  },
  {
    n: 49,
    d: 'COST',
    m: 0,
    q: 'An RDS MySQL instance is nearing its storage capacity due to constant new data, and the company wants to ensure sufficient storage without operational interruption or manual intervention. What is the least-overhead solution?',
    o: [
      'Use ElastiCache to offload some read traffic and reduce database load',
      'Enable automatic storage scaling for the MySQL instance',
      'Implement a lifecycle policy to delete older data from the MySQL instance',
      'Migrate the database to a larger RDS MySQL instance',
    ],
    a: [1],
    x: 'El auto-escalado de almacenamiento de RDS incrementa automaticamente la capacidad cuando se acerca al limite asignado, sin intervencion manual ni interrupcion del servicio. Migrar a una instancia mayor no evita el problema a futuro, borrar datos antiguos puede no ser aceptable si los datos son necesarios, y ElastiCache mejora lecturas pero no resuelve la falta de espacio en disco.',
  },
  {
    n: 50,
    d: 'COST',
    m: 0,
    q: 'A development team needs to run a few lab servers for a weekend project, running uninterrupted for a few hours. Which EC2 pricing option is most suitable?',
    o: ['Reserved', 'Dedicated instances', 'Spot', 'On-Demand'],
    a: [3],
    x: 'On-Demand garantiza que las instancias no se terminen inesperadamente y es economico para necesidades ad-hoc y de corta duracion como esta. Spot puede ser interrumpido por AWS en cualquier momento (incompatible con el requisito de "sin interrupcion"), Reserved exige compromiso de 1-3 anos (no apto para uso puntual), y Dedicated instances son mas caras sin aportar beneficio aqui.',
  },
  {
    n: 51,
    d: 'PERF',
    m: 1,
    q: 'Which two block devices can be specified in a block device mapping to be used with an EC2 instance? (Choose two.)',
    o: ['EBS volume', 'Instance store volume', 'Snapshot', 'S3 bucket', 'EFS volume'],
    a: [0, 1],
    x: 'Un block device mapping puede especificar volumenes EBS adicionales o volumenes de instance store para adjuntar a la instancia en el momento del lanzamiento. No se pueden especificar directamente un snapshot, un bucket de S3 ni un volumen EFS como dispositivo de bloque en el mapping (aunque un volumen EBS si puede crearse a partir de un snapshot).',
  },
  {
    n: 52,
    d: 'PERF',
    m: 0,
    q: 'An API experiences significant traffic spikes at specific times and needs consistently low latency with minimal operational overhead. What do you recommend?',
    o: [
      'Use API Gateway with Fargate tasks to handle the API requests',
      'Implement the API on an EC2 instance behind an ALB with manual scaling',
      'Use API Gateway along with Lambda functions with provisioned concurrency',
      'Implement the API using Elastic Beanstalk with auto-scaling groups',
    ],
    a: [2],
    x: 'La concurrencia aprovisionada en Lambda mantiene un pool de funciones ya "calientes" listas para responder de inmediato, garantizando baja latencia constante incluso durante picos, todo dentro de una arquitectura totalmente gestionada junto con API Gateway. Fargate anade gestion de ciclo de vida de contenedores, EC2 con escalado manual no reacciona con la rapidez necesaria, y Elastic Beanstalk no garantiza la misma baja latencia sostenida durante picos repentinos.',
  },
  {
    n: 53,
    d: 'RES',
    m: 0,
    q: 'A web app on RDS MySQL experienced 3 minutes of downtime during a scheduled failover as part of a scaling operation. The company wants to mitigate this downtime in future scaling exercises with minimal operational overhead. What is the most effective solution?',
    o: [
      'Add more RDS MySQL read replicas to manage the load during the failover',
      'Configure an RDS Proxy for the database and have the application connect to the proxy endpoint',
      'Implement ElastiCache for Redis to manage the load during the failover',
      'Establish a secondary RDS MySQL cluster in the same Region and manually reconnect the app during failover',
    ],
    a: [1],
    x: 'RDS Proxy mantiene abierto un pool de conexiones y, durante un failover, se conecta automaticamente a la instancia standby preservando las conexiones de la aplicacion, reduciendo drasticamente el tiempo de interrupcion percibido. Los read replicas no aceleran el proceso de failover en si, ElastiCache no gestiona failovers de base de datos relacional, y mantener un segundo cluster con reconexion manual anade complejidad operativa sin garantizar menos downtime.',
  },
  {
    n: 54,
    d: 'COST',
    m: 0,
    q: 'A web app writes transaction log files to an S3 bucket, growing quite large. It is acceptable to retain the most recent 60 days and permanently delete the rest. How can this happen automatically?',
    o: [
      'Use a lifecycle policy to move log files older than 60 days to the GLACIER storage class',
      'Use a lifecycle policy with object expiration configured to remove objects older than 60 days',
      'Write a script that checks object age and deletes objects older than 60 days',
      'Use a bucket policy that deletes objects older than 60 days',
    ],
    a: [1],
    x: 'Una lifecycle policy con una accion de expiracion configurada elimina automaticamente los objetos una vez superan los 60 dias, cumpliendo el requisito de eliminacion permanente sin scripts adicionales. Mover a Glacier reduce coste pero no borra los archivos (contradice el requisito de eliminar), un script propio es innecesario cuando existe la funcion nativa, y las bucket policies no tienen capacidad de eliminar objetos por antiguedad.',
  },
  {
    n: 55,
    d: 'SEC',
    m: 0,
    q: 'Security policy requires that use of specific API actions be limited across all accounts in an AWS Organization, centrally controlled. What is the simplest method?',
    o: [
      'Create a network ACL that limits access to the services/actions and attach it to all relevant subnets',
      'Create an IAM policy in the root account and attach it to users and groups in each account',
      'Create a service control policy in the root organizational unit to deny access to the services or actions',
      'Create cross-account roles in each account to limit access to the allowed services and actions',
    ],
    a: [2],
    x: 'Una service control policy (SCP) aplicada en la OU raiz de la organizacion restringe de forma centralizada los permisos maximos disponibles en todas las cuentas de una sola vez. Las network ACL controlan trafico de red (no acciones de la API), replicar politicas IAM en cada cuenta no esta centralizado, y crear roles cross-account en cada cuenta es mucho mas complejo e ineficiente que una unica SCP.',
  },
  {
    n: 56,
    d: 'PERF',
    m: 0,
    q: 'A MySQL database on EC2 generates high I/O with constant writes to a single table. Which EBS volume type provides the most consistent performance and lowest latency?',
    o: [
      'General Purpose SSD (gp2)',
      'Provisioned IOPS SSD (io1)',
      'Cold HDD (sc1)',
      'Throughput Optimized HDD (st1)',
    ],
    a: [1],
    x: 'Provisioned IOPS SSD (io1) se puede configurar con la cantidad exacta de IOPS necesaria y ofrece el rendimiento mas consistente y de menor latencia entre los tipos de EBS, ideal para escrituras constantes de alto I/O. Las opciones HDD no son adecuadas para cargas de baja latencia, y gp2 no iguala la consistencia de rendimiento de Provisioned IOPS bajo carga sostenida.',
  },
  {
    n: 57,
    d: 'RES',
    m: 1,
    q: 'A multi-tier app has its web and database tiers on EC2 in private subnets within the same Availability Zone. Which two steps add high availability to this architecture? (Choose two.)',
    o: [
      'Create new private subnets in a different AZ and create a database using EC2 in one AZ',
      'Create new private subnets in a different AZ and migrate the database to an RDS Multi-AZ deployment',
      'Create an Auto Scaling group and Application Load Balancer spanning multiple AZs',
      'Add the existing web application instances to an Auto Scaling group behind an ALB',
      'Create new public subnets in the same AZ and move the web tier there',
    ],
    a: [1, 2],
    x: 'Migrar la base de datos a un despliegue RDS Multi-AZ da replicacion sincrona y failover automatico entre AZs, y un Auto Scaling group con ALB repartido en varias AZs da alta disponibilidad a la capa web. Crear otra instancia de base de datos en EC2 en una sola AZ no aporta failover automatico, simplemente anadir las instancias existentes a un ASG no las redistribuye entre AZs, y cambiar a subnets publicas en la misma AZ no mejora la disponibilidad.',
  },
];
