import type { RawQuestion } from '@/quiz/quiz.types';

/**
 * AWS SAA-C03 practice set 2 (51 questions), authored by Chabe.
 */
export const exam103: RawQuestion[] = [
  {
    n: 1,
    d: 'PERF',
    m: 0,
    q: 'An on-premises accounting application uses MySQL and slows down whenever users run reports during working hours. You want to move to AWS and improve performance. Which solution is most cost-effective to build and operate?',
    o: [
      'Create a database on an EC2 instance with CPU specs equal to or higher than the existing database',
      'Build a DynamoDB table and import the data, refactoring the app to use DynamoDB for reporting',
      'Deploy Aurora MySQL in Multi-AZ and point reporting traffic at the cluster\u2019s standby instance',
      'Deploy Aurora MySQL in Multi-AZ with read replicas, and point reporting traffic at the replica endpoint',
    ],
    a: [3],
    x: 'Aurora MySQL con read replicas descarga las consultas de reporting de la instancia primaria, mejorando el rendimiento en horas punta sin rediseñar la aplicacion, y al ser gestionado reduce el coste operativo frente a EC2 manual. Migrar a DynamoDB exigiria reescribir la app, y el standby de Multi-AZ es solo para failover, no acepta lecturas.',
  },
  {
    n: 2,
    d: 'RES',
    m: 0,
    q: 'A company needs a multi-region, disaster-recoverable relational database with strong write consistency, an RPO of 10 seconds, and an RTO of 1 minute. Which solution fulfills this?',
    o: [
      'DynamoDB global tables',
      'RDS for MySQL with Multi-AZ',
      'RDS for MySQL with cross-region snapshot copy',
      'Aurora Global Database',
    ],
    a: [3],
    x: 'Aurora Global Database replica entre regiones con retraso tipico por debajo de un segundo y permite promover la region secundaria en menos de un minuto ante un fallo, cumpliendo el RPO/RTO requerido para una base relacional. DynamoDB no es relacional, Multi-AZ solo cubre fallos dentro de una region, y las copias de snapshot cross-region no dan ese nivel de RPO/RTO.',
  },
  {
    n: 3,
    d: 'SEC',
    m: 0,
    q: 'A payment service processes transactions through a third-party payment provider that only allows up to 5 IP addresses to connect. The system runs on EC2 behind an ELB. What is the most secure solution?',
    o: [
      'Place the ELB in a public subnet and EC2 in a private subnet, with a NAT gateway in the public subnet routing EC2\u2019s outbound traffic',
      'Place the ELB and EC2 in a public subnet, restricting the payment provider IP in the EC2 security group',
      'Place the ELB in a public subnet and EC2 in a private subnet, routing outbound traffic through a customer gateway',
      'Place the ELB and EC2 in a public subnet with a network ACL allowing only the payment provider IP',
    ],
    a: [0],
    x: 'Un NAT gateway da una IP publica fija para todo el trafico saliente de las instancias en subnet privada, cumpliendo el limite de IPs del proveedor sin importar cuantas instancias EC2 haya, y mantener EC2 en subnet privada es mas seguro. Un customer gateway es para VPN hacia on-premises (no aplica aqui), y dejar EC2 en subnet publica va contra buenas practicas de seguridad.',
  },
  {
    n: 4,
    d: 'PERF',
    m: 0,
    q: 'A company hosts its website on EC2 across multiple AZs and expects a traffic spike at a specific date and time for a press release. How should scaling be configured so performance doesn\u2019t degrade at that moment?',
    o: ['Use step scaling', 'Use simple scaling', 'Use scheduled scaling', 'Use lifecycle hooks'],
    a: [2],
    x: 'El scheduled scaling permite definir con antelacion una accion de escalado para una fecha y hora concretas, encajando con un evento conocido como un press release. Step y simple scaling reaccionan a metricas en tiempo real (no anticipan el pico), y los lifecycle hooks solo pausan instancias durante transiciones de estado, no gestionan capacidad.',
  },
  {
    n: 5,
    d: 'SEC',
    m: 0,
    q: 'A Lambda function needs to call an API that lists S3 buckets. What is the most secure way to grant it the permissions it needs?',
    o: [
      'Create an IAM user with the required permissions and store its access key and secret key in the Lambda function',
      'Create the Lambda function using the permissions of the IAM user who created it',
      'Create an IAM user with the required permissions, store the keys encrypted in RDS, and have Lambda fetch them from there',
      'Create an IAM role with permissions to list S3 buckets and attach it to the Lambda function',
    ],
    a: [3],
    x: 'Los roles de IAM son la forma recomendada de dar permisos a Lambda: se adjunta un rol con la politica necesaria y Lambda asume credenciales temporales automaticamente, sin gestionar claves. Guardar access keys en el codigo o en RDS es una mala practica de seguridad, y los permisos del creador del Lambda no se heredan por la funcion.',
  },
  {
    n: 6,
    d: 'PERF',
    m: 0,
    q: 'A financial company writes transactions frequently and reads must always reflect the latest state. Reports occasionally join across multiple tables, and the database must scale automatically as data grows. Which service fits?',
    o: ['Amazon S3', 'Amazon Aurora', 'Amazon DynamoDB', 'Amazon Redshift'],
    a: [1],
    x: 'Aurora es una base relacional con lecturas fuertemente consistentes, soporta joins entre tablas para los reportes y escala su almacenamiento automaticamente conforme crecen los datos. S3 no es una base de datos, DynamoDB no garantiza por defecto lectura inmediata tras escritura ni joins nativos, y Redshift es un data warehouse sin autoescalado de este tipo.',
  },
  {
    n: 7,
    d: 'SEC',
    m: 1,
    q: 'Everyone in the IT department needs access to the AWS account, and the company already authenticates internally with an on-premises LDAP directory. You want single sign-on to the AWS Management Console using the same credentials. Which two actions achieve this? (Choose two.)',
    o: [
      'Use IAM roles to set permissions for users federated from your identity provider (IdP)',
      'Enable single sign-on with identity federation using SAML 2.0',
      'Launch an EC2 instance to federate identities and integrate it with the on-premises LDAP',
      'In the organization\u2019s IdP, map users or groups to IAM users',
      'Integrate Cognito with the on-premises LDAP directory',
    ],
    a: [0, 1],
    x: 'La federacion SAML 2.0 permite que el IdP on-premises autentique al usuario y devuelva una assertion que AWS STS usa para generar credenciales temporales, y esas credenciales se rigen por permisos definidos en roles de IAM asociados a la assertion. No hace falta un EC2 dedicado para esto, ni mapear usuarios del IdP a usuarios IAM individuales (rompe el proposito de la federacion), ni pasar por Cognito para este caso.',
  },
  {
    n: 8,
    d: 'PERF',
    m: 0,
    q: 'A chat application uses DynamoDB for its data and needs to load messages faster, while keeping application changes to a minimum. What is the best way to achieve this?',
    o: [
      'Add DynamoDB read replicas and point the app at the replica\u2019s read endpoint',
      'Configure DynamoDB Accelerator (DAX) for the message table and update the app to use the DAX endpoint',
      'Add ElastiCache for Redis and point the app at the Redis endpoint',
      'Double the DynamoDB read capacity units and keep using the existing endpoint',
    ],
    a: [1],
    x: 'DAX es una cache en memoria compatible con la API de DynamoDB, por lo que solo hace falta cambiar el endpoint para acelerar las lecturas, minimizando cambios en la app. DynamoDB no tiene read replicas como RDS, anadir Redis exige mas cambios de integracion, y subir las read capacity units mejora throughput pero no la velocidad de lectura en si.',
  },
  {
    n: 9,
    d: 'PERF',
    m: 0,
    q: 'A company collects website access logs near real time on EC2, stores them in RDS, and another EC2 instance queries them with SQL for recommendations. You want to decouple the architecture and analyze the real-time data with SQL. What is the right solution?',
    o: [
      'Receive access logs via SNS and run SQL queries from a Lambda function',
      'Receive access logs via SQS and let the current EC2 instance consume them',
      'Use Kinesis Data Streams to receive logs, store them in S3 via Kinesis Firehose, and analyze with Athena',
      'Use Kinesis Data Streams to receive logs, analyze them with Kinesis Data Analytics using SQL, and store them in S3 via Kinesis Firehose',
    ],
    a: [3],
    x: 'Kinesis Data Streams recibe los logs en tiempo real, Kinesis Data Analytics permite consultarlos con SQL sobre el propio stream, y Firehose persiste los datos en S3, desacoplando por completo el pipeline original. SNS no es apto para procesar streams con SQL, SQS solo desacopla sin dar capacidad de consulta SQL, y Athena sobre datos ya volcados en S3 no es analisis en tiempo real.',
  },
  {
    n: 10,
    d: 'COST',
    m: 0,
    q: 'A mobile application needs to upload business data to S3, but routing all that traffic through the web application servers would generate too much load. What is the most cost-effective and appropriate way to upload directly from the mobile app?',
    o: [
      'Use S3 cross-region replication',
      'Upload to a temporary bucket and have a Lambda function copy files to the final bucket',
      'Increase the web server instance size to handle the extra traffic',
      'Generate presigned URLs for S3 and upload data directly from the mobile app',
    ],
    a: [3],
    x: 'Las presigned URLs permiten que la app movil suba directamente a S3 sin pasar por los servidores web, evitando ese trafico y coste extra. La replicacion cross-region solo copia datos ya existentes, un bucket intermedio con Lambda anade pasos y complejidad innecesarios, y escalar los servidores web es mas caro y no ataca la causa del problema.',
  },
  {
    n: 11,
    d: 'PERF',
    m: 0,
    q: 'A global application used in the US, Europe, and Japan needs a database distributed across multiple regions for fast local access, with writes in one region reflected in other regions within about 1 second, and last-writer-wins conflict resolution. Which solution meets these requirements?',
    o: [
      'ElastiCache for Redis as the data store',
      'Redshift with a cluster of 3+ compute nodes',
      'Aurora with read replicas',
      'DynamoDB with global tables',
    ],
    a: [3],
    x: 'Las global tables de DynamoDB replican de forma activa-activa entre regiones con una latencia tipica muy por debajo de 1 segundo, y resuelven conflictos con la politica de "ultimo en escribir gana", cumpliendo todos los requisitos. ElastiCache no es una base de datos distribuida multi-region con estas garantias, Redshift es analitica (no transaccional multi-region), y Aurora solo acepta escrituras en una region primaria.',
  },
  {
    n: 12,
    d: 'SEC',
    m: 1,
    q: 'You started operating an AWS account as the root user, and the security team flagged this as a risk. Which two actions eliminate the risk? (Choose two.)',
    o: [
      'Create an IAM user with permissions scoped to the intended purpose',
      'Remove the root user and disable its login',
      'Set up multi-factor authentication (MFA) for the root user',
      'Operate from the AWS CLI using the root user\u2019s access key and secret key',
      'Operate from the management console using an EC2 key pair',
    ],
    a: [0, 2],
    x: 'La practica recomendada es habilitar MFA en el root user y guardar sus credenciales de forma segura, y usar usuarios IAM con permisos acotados para las operaciones del dia a dia en vez del root. El root user no se puede eliminar, usar sus access keys por CLI va contra las buenas practicas de seguridad, y un key pair de EC2 no sirve para operar en la consola de gestion.',
  },
  {
    n: 13,
    d: 'RES',
    m: 0,
    q: 'Two VPCs (VPC1 and VPC2) each run EC2 instances that exchange large amounts of data. Communication between the VPCs must have no single point of failure, sufficient bandwidth, and be secure. Which solution fits?',
    o: [
      'Configure Direct Connect on VPC1 and VPC2',
      'Configure Transit Gateways in VPC1 and VPC2',
      'Place a NAT gateway in VPC1 and route EC2 communication through it',
      'Configure a gateway VPC endpoint for EC2 in VPC2 and route from VPC1',
    ],
    a: [1],
    x: 'AWS Transit Gateway conecta multiples VPCs de forma escalable, sin punto unico de fallo y con el ancho de banda necesario para grandes volumenes de datos, todo dentro de la infraestructura de AWS. Direct Connect conecta on-premises con AWS (no VPC a VPC), un NAT gateway es para salida a internet, y los gateway endpoints solo dan acceso privado a servicios como S3/DynamoDB, no a otra VPC.',
  },
  {
    n: 14,
    d: 'SEC',
    m: 0,
    q: 'A web server is being accessed maliciously from a specific IP address, and you want to deny connections from it. Which AWS service or feature should you use?',
    o: ['Internet gateway', 'Network ACLs', 'AWS Direct Connect', 'Amazon API Gateway'],
    a: [1],
    x: 'Las network ACLs operan a nivel de subnet y permiten definir reglas explicitas de deny para una IP concreta, siendo la herramienta adecuada para bloquear trafico malicioso a ese nivel. Un internet gateway no filtra trafico, Direct Connect no tiene relacion con bloquear IPs, y API Gateway gestiona APIs, no el trafico general de un servidor web.',
  },
  {
    n: 15,
    d: 'SEC',
    m: 0,
    q: 'A 3-tier web app behind CloudFront (with an ALB as origin) and EC2 has recently shown malicious access from a specific IP in the logs. What is the right solution to block that IP going forward?',
    o: [
      'Block the IP with an EC2 security group under the ALB',
      'Attach AWS WAF to CloudFront and block the IP using a WAF IP match condition',
      'Block the IP with a network ACL on the EC2 subnet under the ALB',
      'Block the IP directly in the CloudFront distribution settings',
    ],
    a: [1],
    x: 'AWS WAF se integra con CloudFront y permite crear una condicion de coincidencia de IP que bloquea las peticiones antes de que lleguen a la aplicacion, siendo la solucion centralizada y escalable para este caso. Los security groups actuan a nivel de instancia (no cubren el trafico que llega via CloudFront), las network ACLs son demasiado generales para reglas finas de IP, y CloudFront no tiene bloqueo de IP nativo sin WAF.',
  },
  {
    n: 16,
    d: 'PERF',
    m: 0,
    q: 'Every day, a headquarters team runs many queries against data stored in Redshift, and there is normally enough time for each region to load its data before analysis. Occasionally, though, data loading is slow and the number of concurrent queries spikes, but reports still need to be delivered reliably. What should you do?',
    o: [
      'Store pending queries in SQS',
      'Take advantage of cross-region snapshots',
      'Increase the number of nodes',
      'Enable Redshift concurrency scaling',
    ],
    a: [3],
    x: 'El concurrency scaling de Redshift anade capacidad transitoria de forma automatica cuando aumenta la concurrencia de consultas, asegurando que los reportes se sigan entregando sin intervencion manual. SQS no gestiona consultas SQL, los snapshots cross-region son para DR (no para picos de carga), y anadir nodos de forma permanente es una solucion manual y menos eficiente que el escalado automatico.',
  },
  {
    n: 17,
    d: 'COST',
    m: 0,
    q: 'An e-commerce site runs on a public ALB with EC2 in a private subnet, static content on EC2 and dynamic content from RDS for Oracle, all in Japan. The company now sells to users in the Americas and Europe and wants no browsing delay for them. What is the most cost-effective solution?',
    o: [
      'Host the entire website on S3',
      'Use CloudFront and S3 to host the static content',
      'Deploy the application in the Americas and Europe regions',
      'Increase the number of EC2 instances',
    ],
    a: [1],
    x: 'CloudFront cachea el contenido estatico servido desde S3 en edge locations cercanas a cada usuario, reduciendo la latencia internacional a bajo coste, mientras el contenido dinamico sigue viniendo de RDS en Japon. Alojar todo el sitio en S3 no soporta contenido dinamico de Oracle, desplegar en varias regiones exige replicar RDS Oracle entre regiones (caro y complejo), y anadir mas EC2 no reduce la latencia geografica.',
  },
  {
    n: 18,
    d: 'COST',
    m: 0,
    q: 'A small company with limited network bandwidth needs to move 300 TB of application data to S3 without impacting normal business operations. What is the most cost-effective solution?',
    o: [
      'Transfer the data to S3 using Storage Gateway File Gateway',
      'Load the data onto a Snowmobile, ship it to AWS, and upload it to S3',
      'Load the data onto a Snowball Edge, ship it to AWS, and upload it to S3',
      'Transfer the data to S3 over the internet',
    ],
    a: [2],
    x: 'Snowball Edge esta pensado justo para migraciones masivas de datos cuando el ancho de banda es limitado: se envia el dispositivo, se carga localmente y AWS lo importa a S3, sin saturar la red corporativa. Transferir 300 TB por internet o via File Gateway seria demasiado lento con ese ancho de banda, y Snowmobile esta pensado para migraciones a escala de exabytes, no de 300 TB.',
  },
  {
    n: 19,
    d: 'COST',
    m: 1,
    q: 'You are designing a static single-page application used globally, with low operating costs and low latency as requirements. Which two AWS services should you combine? (Choose two.)',
    o: ['Amazon RDS', 'Auto Scaling', 'Amazon S3', 'Network Load Balancer', 'Amazon CloudFront'],
    a: [2, 4],
    x: 'S3 aloja el contenido estatico de forma barata y muy disponible, y CloudFront distribuye ese contenido desde edge locations cercanas a cada usuario, dando baja latencia global a bajo coste. Una SPA estatica no necesita RDS ni Auto Scaling (no hay servidores de computo que escalar) ni un Network Load Balancer (no hay backend que balancear).',
  },
  {
    n: 20,
    d: 'COST',
    m: 0,
    q: 'A 3-tier campaign site on EC2 uses Aurora as its database, and a traffic spike is expected over the weekend. What design avoids overloading the database while minimizing cost?',
    o: [
      'Configure an Auto Scaling group on EC2',
      'Configure DynamoDB Accelerator (DAX) to cache database reads',
      'Migrate to DynamoDB to handle the traffic spike',
      'Configure Auto Scaling on the Aurora database instances',
    ],
    a: [3],
    x: 'Aurora Auto Scaling anade o quita replicas de lectura automaticamente segun la demanda, absorbiendo el pico del fin de semana sin sobreaprovisionar de forma permanente, lo cual minimiza el coste. Escalar solo EC2 no alivia la carga sobre la base de datos, DAX no aplica a Aurora (es de DynamoDB), y migrar a DynamoDB exigiria un rediseno que no es necesario para este caso.',
  },
  {
    n: 21,
    d: 'SEC',
    m: 0,
    q: 'Operations logs from a project AWS account must be aggregated in a separate operations-team AWS account, and the ops team should be able to view the project account when an error is detected. How should access be granted following least privilege?',
    o: [
      'Create new IAM users in the project account for operations team members',
      'Launch an EC2 instance with an IAM role, connect via VPC peering, and let ops SSH in and use the AWS CLI',
      'Add the operations team account to the trust policy of an IAM role in the project account',
      'Have the operations account submit an access request to the project account for manual approval',
    ],
    a: [2],
    x: 'Un rol de IAM cross-account con la cuenta de operaciones en su trust policy permite acceso controlado y temporal, sin crear identidades adicionales ni exponer recursos de forma amplia, cumpliendo el principio de minimo privilegio. Crear usuarios IAM dedicados anade gestion innecesaria de credenciales, montar EC2 con VPC peering es una solucion sobredimensionada, y AWS no tiene un flujo de "solicitud de permiso" manual entre cuentas.',
  },
  {
    n: 22,
    d: 'PERF',
    m: 1,
    q: 'Which two statements correctly describe Amazon DynamoDB Accelerator (DAX)? (Choose two.)',
    o: [
      'It is a lower-performance but cheaper NoSQL database service compared to DynamoDB',
      'It periodically reads semi-structured data from S3 into DynamoDB, so DynamoDB can serve it without accessing S3',
      'It is an in-memory cache service for DynamoDB',
      'It can deliver millisecond-scale latency even while handling millions of requests per second',
      'It is a feature that lets you set scaling rules for the fully managed DynamoDB service',
    ],
    a: [2, 3],
    x: 'DAX es una cache en memoria totalmente gestionada para DynamoDB que reduce los tiempos de respuesta y soporta millones de peticiones por segundo con latencia de milisegundos, sin que el desarrollador gestione la logica de cache. No es una base de datos alternativa (mas lenta) a DynamoDB, no importa datos desde S3, y no gestiona reglas de auto-escalado de capacidad (eso lo hace DynamoDB Auto Scaling).',
  },
  {
    n: 23,
    d: 'COST',
    m: 0,
    q: 'A company runs an application server and a database server on-premises, and needs quick recovery for the core business database. You are choosing backup storage that minimizes build and operating cost while giving instant access to backed-up data. What is the best solution?',
    o: [
      'Deploy Storage Gateway File Gateway on-premises and transfer files to an S3 bucket',
      'Mount EFS on-premises to store the database backup files',
      'Get a database VM image and periodically transfer it to an S3 bucket via the AWS CLI',
      'Back up the database to a Snowball device and use a lifecycle rule to move it to S3 Glacier Deep Archive',
    ],
    a: [0],
    x: 'El File Gateway de Storage Gateway sirve carpetas SMB/NFS on-premises que se respaldan en S3, minimizando el trabajo de configuracion y operacion gracias a ser un servicio gestionado, con acceso inmediato a los datos en S3. EFS no se puede montar directamente on-premises, transferir manualmente con el CLI exige mas scripting y mantenimiento, y Snowball con Glacier Deep Archive introduce tiempos de transporte y de recuperacion incompatibles con "acceso instantaneo".',
  },
  {
    n: 24,
    d: 'PERF',
    m: 0,
    q: 'A global application will always have heavy reads and writes to its database, and you don\u2019t want the database to become a bottleneck. Which EBS volume type maximizes performance without introducing a bottleneck on the database side?',
    o: ['Provisioned IOPS SSD', 'Throughput Optimized HDD', 'Cold HDD', 'General Purpose SSD'],
    a: [0],
    x: 'Provisioned IOPS SSD (io1/io2) esta disenado para cargas con IOPS altos y consistentes, tipico de bases de datos criticas con lecturas/escrituras intensas. Throughput Optimized HDD y Cold HDD estan pensados para throughput secuencial y datos poco accedidos (no para bases de datos transaccionales), y General Purpose SSD ofrece un equilibrio menor que Provisioned IOPS para este nivel de exigencia.',
  },
  {
    n: 25,
    d: 'SEC',
    m: 1,
    q: 'A web application server in a public subnet accepts HTTPS from the internet and connects to a database server in a private subnet, which must accept connections only from the web application server. Which two security settings achieve this with the least impact on the live service? (Choose two.)',
    o: [
      'A network ACL that allows all inbound and denies all outbound HTTPS traffic for the web application server subnet',
      'In the security group, open the database port and only allow connections from the web application server\u2019s security group',
      'Relocate the database server to the same subnet as the web application server',
      'A network ACL that allows all inbound and denies all outbound traffic on the database port for the private subnet',
      'Open the HTTPS port of the web application server and allow connections from the internet (0.0.0.0/0) in the security group',
    ],
    a: [1, 4],
    x: 'Restringir el puerto de la base de datos en su security group para aceptar solo el security group del servidor web, y abrir el puerto HTTPS del servidor web a internet en su propio security group, cumple ambos requisitos sin romper el trafico de retorno. Las network ACLs son sin estado, asi que denegar todo el trafico saliente rompe las respuestas legitimas, y mover la base de datos a la subnet publica va contra el requisito de aislamiento.',
  },
  {
    n: 26,
    d: 'SEC',
    m: 0,
    q: 'An EC2 application needs an IAM role that only allows deleting records from the "users" table in DynamoDB. Which IAM policy follows the principle of least privilege?',
    o: [
      '{"Effect":"Allow","Action":"dynamodb:DeleteItem","Resource":"arn:aws:dynamodb:ap-northeast-1:xxxxxxxxxxxx:table/*"}',
      '{"Effect":"Allow","Action":"dynamodb:DeleteItem","Resource":"arn:aws:dynamodb:ap-northeast-1:xxxxxxxxxxxx:table/users"}',
      '{"Effect":"Allow","Action":"dynamodb:*","Resource":"arn:aws:dynamodb:ap-northeast-1:xxxxxxxxxxxx:table/*"}',
      '{"Effect":"Allow","Action":"dynamodb:*","Resource":"arn:aws:dynamodb:ap-northeast-1:xxxxxxxxxxxx:table/users"}',
    ],
    a: [1],
    x: 'La opcion que limita tanto la accion (solo DeleteItem) como el recurso (solo la tabla users) es la unica que cumple el minimo privilegio real. Las que usan "table/*" abarcan todas las tablas, y las que usan "dynamodb:*" conceden todas las acciones del servicio, ambas demasiado amplias para el requisito.',
  },
  {
    n: 27,
    d: 'PERF',
    m: 0,
    q: 'A new application requires low network latency and high network throughput between EC2 instances. Which is the correct solution?',
    o: ['AWS Auto Scaling', 'Cluster placement group', 'Partition placement group', 'Spread placement group'],
    a: [1],
    x: 'Un cluster placement group agrupa las instancias fisicamente cerca dentro del mismo rack/AZ, logrando baja latencia y alto throughput de red entre ellas. Auto Scaling gestiona capacidad, no latencia de red; el partition placement group esta pensado para aislar fallos en cargas distribuidas (como Hadoop); y el spread placement group prioriza tolerancia a fallos repartiendo en hardware distinto, no rendimiento de red.',
  },
  {
    n: 28,
    d: 'COST',
    m: 0,
    q: 'An internal file server stores 100 TB of business data that needs to be migrated to S3 Glacier as a backup destination, reducing strain on the internal network line, in a cost-effective way. Which migration method fits?',
    o: [
      'Upload files from a browser to S3 over the internet, then set a lifecycle policy to archive to Glacier',
      'Set up a site-to-site VPN and upload files from a browser directly to Glacier over the VPN',
      'Set up a site-to-site VPN, upload files to S3 over the VPN, then set a lifecycle policy to archive to Glacier',
      'Use AWS Snowball with an S3 bucket as the migration destination, then set a lifecycle policy to archive to Glacier',
    ],
    a: [3],
    x: 'Snowball transporta fisicamente los 100 TB sin cargar la linea corporativa, y una vez los datos llegan a S3, una lifecycle policy los archiva automaticamente a Glacier. Subir 100 TB por internet o por VPN saturaria la red y tardaria demasiado, y ademas no se puede subir directamente a Glacier desde el navegador; los datos deben pasar primero por S3.',
  },
  {
    n: 29,
    d: 'SEC',
    m: 0,
    q: 'A photo-sharing web application is designed for many members, and you want to store images in S3 without adding traffic to the web servers when images are uploaded. What is the best way to handle uploads?',
    o: [
      'Fix the web server\u2019s IP address using an Elastic IP',
      'Use multiple temporary S3 buckets and a Lambda function to move files to the final bucket',
      'Upload via an Auto Scaling group behind a Classic Load Balancer configured to write to S3',
      'Use Spot Instances to extend the web server and provide resources for image processing',
      'Upload directly to S3 using a presigned URL',
    ],
    a: [4],
    x: 'Una presigned URL permite subir la imagen directamente a S3 desde el cliente, sin que el trafico de subida pase por los servidores web. Fijar la IP del servidor, usar buckets temporales con Lambda, o anadir Spot Instances no elimina la carga sobre el servidor web, que es justamente el problema a resolver.',
  },
  {
    n: 30,
    d: 'PERF',
    m: 0,
    q: 'A video distribution service gets complaints from members in some countries about long delays between accessing and playing a video. What reduces the time from access to playback regardless of location?',
    o: [
      'Store video files in S3 and access the URL directly',
      'Store video files in S3 and use signed URLs',
      'Run a web server on EC2 and store video files on an EBS volume',
      'Use Amazon CloudFront signed URLs',
    ],
    a: [3],
    x: 'CloudFront distribuye el video desde edge locations repartidas globalmente, y las signed URLs anaden control de acceso temporal sin sacrificar esa distribucion, reduciendo la latencia de reproduccion en cualquier region. Servir directamente desde S3 (con o sin signed URL) no aprovecha la red de distribucion global, y un servidor EC2 con EBS es un unico punto geografico, igual de lento para usuarios lejanos.',
  },
  {
    n: 31,
    d: 'SEC',
    m: 0,
    q: 'You want to serve images only to users visiting from the United States. Which is the correct method?',
    o: [
      'Restrict IP addresses using a NAT instance',
      'Use security groups',
      'Store the images in S3 and set a bucket policy',
      'Use CloudFront\u2019s geo-restriction feature',
    ],
    a: [3],
    x: 'CloudFront ofrece geo-restriction (geo-blocking), que permite distribuir o bloquear contenido segun el pais del usuario de forma nativa. Los security groups filtran por IP, no por pais, una bucket policy de S3 no tiene granularidad geografica, y una NAT instance no controla el acceso de usuarios externos por region.',
  },
  {
    n: 32,
    d: 'PERF',
    m: 0,
    q: 'A business application needs a database that can withstand frequent inserts, updates, and deletes of large volumes of data, support joins across multiple tables for reports, and automatically expand storage as data grows. Which service fits best?',
    o: ['Amazon DynamoDB', 'Amazon Glacier', 'Amazon Aurora', 'Amazon Redshift', 'Amazon S3'],
    a: [2],
    x: 'Aurora es un motor relacional gestionado que soporta joins entre tablas via SQL, encaja bien con escrituras/eliminaciones frecuentes de grandes volumenes, y expande su almacenamiento automaticamente segun crece la base. DynamoDB no soporta SQL/joins nativos entre tablas, Glacier y S3 no son bases de datos, y Redshift esta orientado a analitica columnar, no a operaciones OLTP frecuentes.',
  },
  {
    n: 33,
    d: 'SEC',
    m: 0,
    q: 'The security team wants to restrict access to specific services or actions across every AWS account owned by the company, all part of an AWS Organization. Permissions must be set from a single place and scale easily. What is the correct approach?',
    o: [
      'Create ACLs to restrict access to services or actions',
      'Create a security group that allows the account and attach it to a user group',
      'Deny access to services or actions by creating cross-account roles in each account',
      'Create a service control policy at the root organizational unit denying the service or action',
    ],
    a: [3],
    x: 'Una service control policy (SCP) aplicada a nivel de organizational unit raiz permite restringir servicios o acciones de forma centralizada para todas las cuentas de la organizacion. No existe una funcion de ACL o security group para restringir cuentas completas de AWS, y crear roles cross-account en cada cuenta obligaria a repetir la configuracion cuenta por cuenta.',
  },
  {
    n: 34,
    d: 'PERF',
    m: 0,
    q: 'A large on-premises Microsoft SharePoint deployment requires Windows shared file storage. You want it highly available on AWS and integrated with Active Directory for access control. What fits?',
    o: [
      'Use Amazon EFS and set up an Active Directory domain for authentication',
      'Create a Storage Gateway File Gateway with SMB sharing across two Availability Zones',
      'Create an S3 bucket and mount it as a Windows Server volume',
      'Create Amazon FSx for Windows File Server and configure Active Directory authentication',
    ],
    a: [3],
    x: 'FSx for Windows File Server es un sistema de archivos SMB totalmente gestionado, disenado para cargas tipo SharePoint, con alta disponibilidad e integracion nativa con Active Directory o AWS Managed Microsoft AD. EFS se accede por NFS (no sirve para cargas Windows/SMB), Storage Gateway por si solo no reparte instancias entre AZs de forma nativa, y S3 no se monta directamente como volumen de Windows.',
  },
  {
    n: 35,
    d: 'COST',
    m: 0,
    q: 'A company uses Windows servers as an internal file server, with SMB folder sharing and DFS replication between multiple Windows servers. You want to migrate this to AWS to reduce build and operating costs. What is a good migration target?',
    o: [
      'Amazon EFS',
      'Amazon S3',
      'Amazon FSx for Windows File Server',
      'Amazon EBS',
      'Amazon FSx for Lustre',
    ],
    a: [2],
    x: 'FSx for Windows File Server es un sistema de archivos gestionado y accesible por SMB, construido sobre Windows Server, con funciones estandar como cuotas e integracion con Active Directory, reduciendo el coste de operar servidores Windows propios. EFS solo ofrece NFS, S3 es almacenamiento de objetos, EBS es un volumen de bloque para una sola instancia, y FSx for Lustre esta pensado para cargas de alto rendimiento, no para comparticion de archivos tipo oficina.',
  },
  {
    n: 36,
    d: 'PERF',
    m: 1,
    q: 'A company expects its application\u2019s user base to grow fivefold in a year. It runs in one region with RDS MySQL, an ALB, and EC2 serving static and dynamic content plus microservices. Which two design changes support that growth? (Choose two.)',
    o: [
      'Move static files from EC2 to S3',
      'Use the Amazon Route 53 geolocation routing policy',
      'Scale the environment based on real-time CloudTrail logs',
      'Create a dedicated Elastic Load Balancer for each microservice',
      'Create RDS read replicas and modify the application to use them',
    ],
    a: [0, 4],
    x: 'Mover los archivos estaticos a S3 los saca de EC2 (que tiene capacidad fija que hay que dimensionar), y anadir read replicas de RDS aumenta el throughput de lectura de la base de datos, ambas medidas clave para absorber un crecimiento 5x. La app corre en una sola region, asi que el enrutamiento geografico de Route 53 no aporta nada aqui; CloudTrail registra llamadas a la API, no carga de trafico de aplicacion; y el ALB ya escala y es redundante por si mismo, sin necesitar uno dedicado por microservicio.',
  },
  {
    n: 37,
    d: 'COST',
    m: 0,
    q: 'A company runs many EC2 instances for stateless batch processing with highly variable data volumes; jobs can be started and stopped anytime without affecting the system, and typically take 60 minutes. You need a cost-effective, scalable solution. What do you propose?',
    o: ['Spot Instances', 'Reserved Instances', 'On-Demand Instances', 'AWS Lambda'],
    a: [0],
    x: 'Las Spot Instances son la opcion mas economica para cargas stateless que se pueden interrumpir sin problema, y encajan con un volumen de trabajo dificil de predecir. Los 60 minutos de duracion superan el limite de ejecucion de Lambda (15 minutos), las Reserved Instances exigen comprometerse a uso constante durante 1-3 anos (mal ajuste para carga variable), y On-Demand es mas caro que Spot para este patron.',
  },
  {
    n: 38,
    d: 'RES',
    m: 0,
    q: 'You want to automate the creation, retention, and deletion of backups for EBS volumes in a VPC. What is the easiest way to automate all of these tasks with AWS tools?',
    o: [
      'Use EBS Data Lifecycle Manager (DLM) to manage snapshots of the volumes',
      'Create a scheduled job that runs the AWS CLI command "create-backup"',
      'Configure EBS volume replication to create a backup on S3',
      'Create a scheduled job that runs the AWS CLI command "create-snapshot"',
    ],
    a: [0],
    x: 'DLM automatiza de forma centralizada la creacion, retencion y eliminacion de snapshots de EBS segun politicas definidas, cubriendo las tres tareas a la vez. Programar el comando create-snapshot solo automatiza la creacion (no la retencion/eliminacion), "create-backup" ni siquiera es el comando correcto, y EBS no soporta replicacion directa hacia S3.',
  },
  {
    n: 39,
    d: 'PERF',
    m: 0,
    q: 'An EC2 instance will run an application requiring heavy read/write IOPS. A single 500 GiB volume must support 20,000 IOPS. What EBS volume type should be selected?',
    o: [
      'EBS General Purpose SSD',
      'EBS Provisioned IOPS SSD',
      'EBS General Purpose SSD in a RAID 1 configuration',
      'EBS Throughput Optimized HDD',
    ],
    a: [1],
    x: 'Provisioned IOPS SSD (io1/io2) es el unico tipo que soporta mas de 16.000 IOPS por volumen, cubriendo el requisito de 20.000 IOPS. General Purpose SSD tope en 16.000 IOPS por volumen (y montarlo en RAID 1 no aumenta las IOPS, solo replica los datos), y Throughput Optimized HDD no esta pensado para cargas de IOPS altos.',
  },
  {
    n: 40,
    d: 'PERF',
    m: 0,
    q: 'A financial modelling application needs a high-performance file system, with the dataset stored on S3 and seamless integration so objects can be accessed as files. Which storage solution fits?',
    o: [
      'Amazon FSx for Lustre',
      'Amazon Elastic File System (EFS)',
      'Amazon Elastic Block Store (EBS)',
      'Amazon FSx for Windows File Server',
    ],
    a: [0],
    x: 'FSx for Lustre ofrece un sistema de archivos de alto rendimiento e integra de forma nativa con S3, dejando acceder a los objetos como si fueran ficheros locales para cargas como modelado financiero o HPC. EFS y EBS no exponen objetos de S3 como archivos de forma transparente, y FSx for Windows esta pensado para comparticion SMB, no para esta integracion con S3.',
  },
  {
    n: 41,
    d: 'PERF',
    m: 0,
    q: 'An event registration web page needs a managed service to send a text message to users every time they sign up for an event. Which AWS service should be used?',
    o: ['Amazon SQS', 'Amazon SNS', 'Amazon STS', 'AWS Lambda'],
    a: [1],
    x: 'Amazon SNS soporta notificaciones a multiples destinos, incluido SMS, y es el servicio pensado para enviar este tipo de mensajes de forma gestionada. SQS es una cola de mensajes para desacoplar componentes (no envia SMS), STS emite credenciales temporales, y Lambda es computo, no un servicio de notificaciones en si mismo.',
  },
  {
    n: 42,
    d: 'RES',
    m: 0,
    q: 'You need to scale read operations for an Aurora DB within a region and be able to fail over if the primary instance fails. What should you implement?',
    o: ['Aurora Global Database', 'An Aurora cluster volume', 'Aurora Replicas', 'A DB cluster'],
    a: [2],
    x: 'Los Aurora Replicas son endpoints independientes dentro del cluster que reparten la carga de lectura y ademas sirven como objetivo de failover: si la instancia primaria falla, uno de ellos se promociona. Un cluster volume es solo el almacenamiento subyacente (no escala lecturas por si mismo), un DB cluster no implica necesariamente tener replicas desplegadas, y Aurora Global Database esta pensado para escenarios multi-region, no para escalar lecturas dentro de una sola region.',
  },
  {
    n: 43,
    d: 'SEC',
    m: 0,
    q: 'You are deploying EC2 instances across several subnets and multiple AZs within a VPC. What AWS feature can act as an instance-level firewall to control traffic between the EC2 instances?',
    o: ['Security group', 'Route table', 'AWS WAF', 'Network ACL'],
    a: [0],
    x: 'Un security group actua como firewall a nivel de instancia, controlando el trafico entrante y saliente de cada EC2 individualmente, sin importar la subnet en la que este. AWS WAF opera a nivel de aplicacion web (no de instancia), las network ACLs actuan a nivel de subnet, y las route tables solo definen el enrutamiento, no filtran trafico.',
  },
  {
    n: 44,
    d: 'RES',
    m: 0,
    q: 'You need to automatically and repeatably create many member accounts within an AWS Organization, move them into an OU, and provision VPCs and subnets in each. What is the best way to achieve this?',
    o: [
      'Use the AWS CLI',
      'Use the AWS Organizations API',
      'Use CloudFormation with automation scripts',
      'Use the AWS Management Console',
    ],
    a: [2],
    x: 'CloudFormation como Infrastructure as Code, combinado con scripts de automatizacion, permite crear cuentas, moverlas a la OU correcta y aprovisionar VPCs/subnets de forma repetible y consistente en todas ellas. La CLI y la API de Organizations por si solas son manuales y no orquestan de forma completa la creacion de infraestructura, y la consola de gestion no es repetible ni escalable para este volumen de tareas.',
  },
  {
    n: 45,
    d: 'SEC',
    m: 0,
    q: 'Developers can attach existing IAM policies to existing IAM roles to move faster, but the security team worries a developer could attach the AdministratorAccess policy and bypass other security controls. How should this be addressed?',
    o: [
      'Prevent developers from attaching any policies and assign all IAM duties to the security team',
      'Use service control policies to disable IAM activity across every account in the organizational unit',
      'Set an IAM permissions boundary on the developer role that explicitly denies attaching the administrator policy',
      'Create an SNS topic that alerts every time a developer creates a new policy',
    ],
    a: [2],
    x: 'Un permissions boundary en el rol de los desarrolladores limita el maximo de permisos que ese rol puede llegar a tener, permitiendo denegar explicitamente que se adjunte la politica de administrador, sin dejar de permitir el resto de la agilidad deseada. Bloquear todo IAM a los desarrolladores anula la agilidad que se busca, deshabilitar IAM por SCP en toda la OU es demasiado drastico, y un aviso por SNS solo informa despues del hecho, no lo previene.',
  },
  {
    n: 46,
    d: 'PERF',
    m: 0,
    q: 'A High Performance Computing (HPC) application will be migrated to AWS, requiring low network latency and high throughput between nodes, deployed in a single AZ. How should the instances be placed?',
    o: [
      'In a spread placement group',
      'In a cluster placement group',
      'In a partition placement group',
      'Behind a Network Load Balancer',
    ],
    a: [1],
    x: 'Los cluster placement groups colocan las instancias fisicamente cerca dentro de una misma AZ, dando la baja latencia y el alto throughput que necesitan las cargas HPC fuertemente acopladas. El spread placement group prioriza tolerancia a fallos (hardware distinto), el partition placement group aisla fallos para cargas distribuidas tipo Hadoop, y un NLB distribuye trafico entrante, no mejora la comunicacion entre nodos.',
  },
  {
    n: 47,
    d: 'PERF',
    m: 0,
    q: 'An application writes data to a DynamoDB table, and you need to run code in response to item-level changes in that table. What would you suggest?',
    o: [
      'Enable server access logging and map a Lambda event source to the S3 bucket where logs are written',
      'Use Kinesis Data Streams and configure DynamoDB as a producer',
      'Enable DynamoDB Streams and create an event source mapping between Lambda and the stream',
      'Create a local secondary index that records item-level changes and write custom code that reacts to updates on the index',
    ],
    a: [2],
    x: 'DynamoDB Streams captura los cambios a nivel de item, y al mapear ese stream como event source de Lambda, la funcion se dispara automaticamente en cada insercion, actualizacion o eliminacion. DynamoDB no genera logs de acceso en S3 de forma nativa, no actua como productor de Kinesis de forma directa, y los indices secundarios locales sirven para consultas, no para disparar eventos.',
  },
  {
    n: 48,
    d: 'SEC',
    m: 1,
    q: 'A school\u2019s document submission application uses an S3 bucket for storage. The solution must prevent accidental deletion of documents. Which two options should be implemented? (Choose two.)',
    o: [
      'Encrypt the bucket using AWS SSE-S3',
      'Attach an IAM policy to the bucket',
      'Enable versioning on the bucket',
      'Set read-only permissions on the bucket',
      'Enable MFA Delete on the bucket',
    ],
    a: [2, 4],
    x: 'El versionado conserva versiones previas de cada objeto, de forma que una eliminacion solo anade un delete marker y el documento sigue siendo recuperable, y MFA Delete anade una capa extra que exige autenticacion multifactor para borrar objetos o versiones de forma permanente. Cifrar el bucket protege la confidencialidad (no la borrado accidental), una politica IAM controla permisos pero no evita que un usuario autorizado borre por error, y dejar el bucket solo lectura rompe la funcionalidad de subida de documentos.',
  },
  {
    n: 49,
    d: 'PERF',
    m: 0,
    q: 'An insurance company serves users in the UK and Australia. The database tier uses MySQL hosted in eu-west-2, and the web tier runs from eu-west-2 and ap-southeast-2 with Route 53 geoproximity routing. Australian users report slow query response times. What change should be made to the database tier?',
    o: [
      'Migrate to an Aurora Global Database in MySQL compatibility mode, with a read replica in ap-southeast-2',
      'Migrate to Amazon RDS for MySQL and configure Multi-AZ in the Australian region',
      'Migrate to DynamoDB and use global tables to replicate to additional regions',
      'Deploy MySQL instances in each region behind an Application Load Balancer',
    ],
    a: [0],
    x: 'Un Aurora Global Database con una region secundaria en ap-southeast-2 replica los datos con muy baja latencia, permitiendo que las lecturas en Australia se sirvan localmente en vez de cruzar hasta el Reino Unido. Migrar Multi-AZ a Australia solo desplazaria el problema (las escrituras seguirian teniendo que ir alli desde UK), DynamoDB probablemente no sea compatible sin reescribir la app, y un ALB no se puede poner delante de instancias MySQL como capa de base de datos.',
  },
  {
    n: 50,
    d: 'SEC',
    m: 0,
    q: 'An RDS Read Replica is being deployed in a separate region from an unencrypted master database, but all data in the new region must be encrypted. How can this be achieved?',
    o: [
      'Enable encryption with KMS when creating the cross-region read replica',
      'Enable encryption on the master DB instance, then create an encrypted cross-region read replica',
      'Encrypt a snapshot of the master DB instance, then create an encrypted cross-region read replica from the snapshot',
      'Encrypt a snapshot of the master DB instance, restore it into a new encrypted master DB instance, then create an encrypted cross-region read replica',
    ],
    a: [3],
    x: 'No se puede activar cifrado directamente sobre una instancia RDS ya existente sin cifrar ni sobre sus replicas: hay que tomar un snapshot, copiarlo cifrandolo con KMS, restaurar un nuevo master cifrado a partir de ese snapshot, y solo entonces crear el read replica cross-region cifrado a partir de ese nuevo master. Cifrar directamente el master existente o crear un replica cifrado desde un snapshot sin antes tener un master cifrado no son operaciones que RDS permita.',
  },
  {
    n: 51,
    d: 'SEC',
    m: 0,
    q: 'An AWS Organization has an OU with multiple member accounts. The company needs to restrict the ability to launch only specific EC2 instance types. How can this policy be applied across the accounts with the least effort?',
    o: [
      'Create an IAM policy to deny launching all but the specific instance types',
      'Use AWS Resource Access Manager to control which launch types can be used',
      'Create an SCP with a deny rule that denies all but the specific instance types',
      'Create an SCP with an allow rule that allows launching only the specific instance types',
    ],
    a: [2],
    x: 'Una SCP con regla deny es el mecanismo de AWS Organizations para aplicar restricciones de forma centralizada a todas las cuentas de una OU de una sola vez. Una politica IAM solo aplicaria dentro de una cuenta (no a todas las de la OU), Resource Access Manager sirve para compartir recursos entre cuentas (no para restringir tipos de instancia), y las SCP no funcionan como "allow" (son siempre limites de tipo deny sobre los permisos que ya conceden las politicas IAM).',
  },
];
