import type { RawQuestion } from '@/quiz/quiz.types';

/**
 * AWS SAA-C03 practice set 7 (52 questions), authored by Chabe.
 */
export const exam108: RawQuestion[] = [
  {
    n: 1,
    d: 'SEC',
    m: 0,
    q: 'An elastic 3-tier application runs in an Auto Scaling group and writes to an RDS MySQL database. Instances in the app tier are constantly launched and terminated, but only they should reach the database. How should secure access to the database tier be configured?',
    o: [
      'Configure the database security group to allow traffic only from the application security group',
      'Configure a network ACL on the database subnet to allow all traffic from the application subnet',
      'Configure the database security group to allow traffic only from port 3306',
      'Configure a network ACL on the database subnet to deny all traffic to ports other than 3306',
    ],
    a: [0],
    x: 'Referenciar el security group de la capa de aplicacion como origen permite que cualquier instancia lanzada y asociada a ese security group se conecte automaticamente, sin importar cuantas veces cambien las instancias. Especificar solo el puerto 3306 como criterio de origen no tiene sentido (3306 debe ser el puerto de destino), y las network ACL no distinguen especificamente el origen a nivel de instancia de aplicacion.',
  },
  {
    n: 2,
    d: 'PERF',
    m: 0,
    q: 'A dynamic website on EC2 behind an ALB has global users reporting poor performance. Which set of actions improves performance while minimizing cost?',
    o: [
      'Launch new EC2 instances and ALBs in different Regions, using Global Accelerator to direct to the closest Region',
      'Create a latency-based Route 53 record for the ALB, and launch larger EC2 instances registered with the ALB',
      'Create a CloudFront distribution with the ALB as an origin, then update the Route 53 record to point to the distribution',
      'Host the website in S3, delete the ALB and EC2 instances, enable Transfer Acceleration, and update Route 53 to point to the S3 bucket',
    ],
    a: [2],
    x: 'CloudFront sirve tanto contenido estatico como dinamico, cacheando en edge locations y aprovechando la red global de AWS, siendo la opcion mas coste-efectiva para mejorar el rendimiento global. Multiplicar EC2/ALBs por region es mucho mas caro, instancias mas grandes no reducen la latencia geografica, y S3 solo puede servir contenido estatico (no dinamico).',
  },
  {
    n: 3,
    d: 'PERF',
    m: 0,
    q: 'An app on EC2 must asynchronously invoke a Lambda function to analyze thousands of CSV files, with the services decoupled. Which service can be used?',
    o: ['Amazon SWF', 'Amazon Kinesis', 'Amazon SNS', 'Amazon OpsWorks'],
    a: [2],
    x: 'SNS es un servicio de mensajeria pub/sub totalmente gestionado; EC2 puede publicar mensajes de forma asincrona y Lambda se suscribe para consumirlos, desacoplando ambos componentes. SWF esta pensado para orquestacion de workflows complejos, Kinesis es para streaming de datos en tiempo real (mas de lo necesario aqui), y OpsWorks es gestion de configuracion, no mensajeria.',
  },
  {
    n: 4,
    d: 'RES',
    m: 0,
    q: 'A company replicates only a limited set of core services to its DR site, ready to take over seamlessly, while all other services are switched off. Which DR strategy is this?',
    o: ['Multi-site', 'Backup and restore', 'Pilot light', 'Warm standby'],
    a: [2],
    x: 'Pilot light replica solo un nucleo minimo de servicios criticos en el sitio de DR, listos para escalar rapidamente en caso de desastre, mientras el resto permanece apagado. Multi-site implica entornos activos completos en ambas ubicaciones, backup and restore es el enfoque mas basico y lento, y warm standby mantiene mas servicios corriendo continuamente que pilot light.',
  },
  {
    n: 5,
    d: 'PERF',
    m: 0,
    q: 'A team collects environmental sensor and camera data with limited local storage and intermittent, unreliable connectivity to EC2/S3, needing immediate local processing. What should be recommended?',
    o: [
      'Configure Kinesis Data Firehose to load data directly to a Snowball device and process locally with Lambda@Edge',
      'Use AWS DataSync on the laptops to synchronize data to S3, then process with EC2 instances',
      'Use AWS Snowball Edge devices to process the data locally',
      'Upload the data to SQS in batches and process the messages using EC2 instances',
    ],
    a: [2],
    x: 'Snowball Edge tiene almacenamiento y capacidad de computo integrados para procesar datos localmente sin depender de conectividad fiable a internet, encajando con el escenario de campo con conectividad intermitente. DataSync y la carga a SQS dependen de tener conexion confiable, y Firehose no puede entregar datos directamente a un dispositivo Snowball.',
  },
  {
    n: 6,
    d: 'PERF',
    m: 0,
    q: 'An SQL database is migrating to Amazon RDS and must deliver sub-millisecond responses to common read requests. What should be recommended?',
    o: [
      'Deploy RDS read replicas',
      'Use EBS Provisioned IOPS volumes',
      'Deploy a database cache using DynamoDB Accelerator',
      'Deploy a database cache using ElastiCache',
    ],
    a: [3],
    x: 'ElastiCache es una capa de cache en memoria totalmente gestionada que se puede configurar delante de RDS a nivel de aplicacion, devolviendo respuestas con latencia de submilisegundos para las consultas cacheadas. DAX solo es compatible con DynamoDB (no con RDS/SQL), los read replicas mejoran el throughput de lectura pero no llegan a submilisegundos, y Provisioned IOPS mejora el I/O de disco pero no iguala una cache en memoria.',
  },
  {
    n: 7,
    d: 'COST',
    m: 0,
    q: 'An RDS database runs in the Prod1 AWS account; EC2 instances that must access it run in the Prod2 account, both under the same Organization. How should this be achieved most cost-effectively?',
    o: [
      'Set up VPC sharing with Prod1 as the owner and Prod2 as the participant',
      'Take a snapshot of the RDS database and share it with Prod2, restoring a new database there',
      'Create a cross-Region replica of the RDS database in the Prod2 account',
      'Create a Lambda function in Prod1 to transfer data to the EC2 instances in Prod2',
    ],
    a: [0],
    x: 'El VPC sharing (via AWS RAM) permite que la cuenta Prod1 comparta la subnet donde vive la base de datos con la cuenta Prod2, dando acceso directo sin duplicar infraestructura ni gestionar transferencias de datos. No se puede crear un replica cross-region en otra cuenta de esta forma, restaurar desde snapshot en Prod2 implica una segunda base de datos duplicada (mas cara), y transferir datos via Lambda no es lo que se pide (acceso, no copia de datos).',
  },
  {
    n: 8,
    d: 'SEC',
    m: 0,
    q: 'A financial company wants each department to have its own AWS account with preconfigured, least-privilege access to authorized services only. What actions ensure compliance with these security requirements?',
    o: [
      'Configure Organizations with SCPs and create new member accounts, using CloudFormation for networking',
      'Deploy a Landing Zone within Organizations, letting department admins create accounts and granting them power user permissions',
      'Use CloudFormation to create member accounts and networking, using IAM roles to allow access to approved services',
      'Deploy a Landing Zone within AWS Control Tower, letting department admins create accounts with automated guardrails',
    ],
    a: [3],
    x: 'AWS Control Tower automatiza la creacion de cuentas, aplica SCPs y guardrails de cumplimiento desde su Landing Zone, dando a cada departamento una cuenta preconfigurada con permisos minimos sin esfuerzo manual. AWS Organizations por si sola no ofrece una Landing Zone con esa automatizacion, y usar solo CloudFormation para crear cuentas y redes exige mucha mas configuracion manual sin la gobernanza centralizada de Control Tower.',
  },
  {
    n: 9,
    d: 'RES',
    m: 0,
    q: 'A legacy app runs on a single Linux EC2 instance, cannot be modified, and cannot run on more than one instance. What should be recommended to improve the recovery time for the system?',
    o: [
      'Deploy the EC2 instance in a cluster placement group in an Availability Zone',
      'Launch the EC2 instance with two EBS volumes and configure RAID 1',
      'Create a CloudWatch alarm to recover the EC2 instance in case of failure',
      'Launch the EC2 instance with two EBS volumes and configure RAID 0',
    ],
    a: [2],
    x: 'Una alarma de CloudWatch configurada para la accion de recuperacion automatica de EC2 reinicia la instancia en hardware sano ante un fallo de status check, mejorando el tiempo de recuperacion sin necesitar mas de una instancia. RAID 1/0 son configuraciones de almacenamiento (no ayudan si falla la instancia en si), y un cluster placement group reduce latencia de red, no mejora la recuperacion ante fallos.',
  },
  {
    n: 10,
    d: 'COST',
    m: 0,
    q: 'HR records in S3 must be retained 7 years, accessed infrequently for the first 3 months, then available within 10 minutes thereafter. Which lifecycle action minimizes cost while meeting these requirements?',
    o: [
      'Store in S3 Standard for 3 months, then transition to S3 Glacier',
      'Store in S3 Standard-IA for 3 months, then transition to S3 Glacier',
      'Store in S3 Standard for 3 months, then transition to S3 Standard-IA',
      'Store in S3 Intelligent-Tiering for 3 months, then transition to S3 Standard-IA',
    ],
    a: [1],
    x: 'S3 Standard-IA cuesta menos que S3 Standard y encaja con el acceso infrecuente de los primeros 3 meses; despues, Glacier (con recuperacion Expedited de minutos) da el menor coste de largo plazo cumpliendo el requisito de disponibilidad en 10 minutos. S3 Standard es mas caro de lo necesario para ese periodo inicial, y quedarse en Standard-IA a largo plazo es mas caro que archivar en Glacier para datos retenidos 7 anos.',
  },
  {
    n: 11,
    d: 'PERF',
    m: 0,
    q: 'A shared file system for company users must be fault tolerant and integrate with the company\u2019s Microsoft Active Directory for access control. Which storage solution meets these requirements?',
    o: [
      'Use S3 for storing the data and configure Cognito to connect S3 to Active Directory for access control',
      'Create an EFS file system and configure AWS Single Sign-On with Active Directory',
      'Create a file system with FSx for Windows File Server and enable Multi-AZ, joined to Active Directory',
      'Use an EC2 Windows instance to create a file share, attaching EBS volumes in different Availability Zones',
    ],
    a: [2],
    x: 'FSx for Windows File Server con despliegue Multi-AZ da tolerancia a fallos gestionada, y se integra de forma nativa con Active Directory para el control de acceso, cumpliendo ambos requisitos directamente. EFS no se integra con Active Directory de esa forma, S3 no funciona como sistema de archivos compartido con Cognito+AD, y no se pueden adjuntar volumenes EBS de distintas AZs a una misma instancia.',
  },
  {
    n: 12,
    d: 'SEC',
    m: 0,
    q: 'Data must be transferred from EC2 to S3 preventing API calls and data from routing over the public internet, using a private connection, allowing only that single EC2 instance to upload. What meets these requirements?',
    o: [
      'Create an S3 interface VPC endpoint in the instance\u2019s subnet, with a resource policy on the bucket allowing only the instance\u2019s IAM role',
      'Obtain the S3 bucket\u2019s API private IP via the console and add a VPC route to it, with a bucket resource policy for the instance\u2019s role',
      'Attach security groups to the endpoint and use a bucket policy allowing only the instance\u2019s IAM role',
      'Use nslookup on the instance to find S3\u2019s private IP and add a VPC route, with a bucket resource policy for the instance\u2019s role',
    ],
    a: [0],
    x: 'Un S3 endpoint de tipo interfaz mantiene el trafico entre EC2 y S3 dentro de la red privada de AWS, y una politica de recursos en el bucket que solo permite el rol IAM de esa instancia concreta cumple ambos requisitos de seguridad y exclusividad de acceso. S3 no expone una IP privada fija consultable ni enrutable manualmente, y aunque se pueden asociar security groups al endpoint, primero hay que crearlo (no basta con "asociar" sin crearlo).',
  },
  {
    n: 13,
    d: 'PERF',
    m: 0,
    q: 'An e-commerce app must share order details with several internal apps in near-real-time at high scale, remove sensitive data first, and store the result in a document database for low-latency retrieval. What should be recommended?',
    o: [
      'Store transactions in DynamoDB, set up a DynamoDB rule to remove sensitive data, and share via DynamoDB Streams',
      'Stream transactions into Kinesis Data Streams, use Lambda to remove sensitive data and store the result in DynamoDB, letting other apps consume the stream',
      'Store batched transactions as files in S3, use Lambda to strip sensitive data and store results in DynamoDB, letting other apps consume the S3 files',
      'Stream transactions into Kinesis Data Firehose to store in DynamoDB and S3, using Lambda to remove sensitive data',
    ],
    a: [1],
    x: 'Kinesis Data Streams ingiere en tiempo real a gran escala, la integracion con Lambda limpia los datos sensibles antes de guardarlos en DynamoDB, y otras aplicaciones pueden consumir el mismo stream de forma independiente. No existe una funcion de "reglas" en DynamoDB para eliminar datos sensibles al escribir, Firehose no puede entregar directamente a DynamoDB, y basar el flujo en archivos S3 introduce mas latencia y complejidad de la necesaria.',
  },
  {
    n: 14,
    d: 'COST',
    m: 0,
    q: 'Users need to download PDF statement reports from a growing global user base, with a cost-effective solution that minimizes operational overhead. Which combination of services should be recommended?',
    o: [
      'Route 53 with Network Load Balancers',
      'CloudFront and S3',
      'Lambda and DynamoDB',
      'Application Load Balancer with Auto Scaling',
    ],
    a: [1],
    x: 'S3 almacena los PDFs de forma economica y CloudFront los cachea en edge locations globales, dando escalado automatico sin gestionar servidores, la combinacion mas coste-efectiva para contenido estatico descargable. Lambda+DynamoDB anadiria coste y complejidad innecesarios para archivos estaticos, y las opciones con ALB/NLB implican instancias EC2, mas caras para este caso.',
  },
  {
    n: 15,
    d: 'COST',
    m: 0,
    q: 'A Linux app must process over 200TB of log files in S3 and store results in another S3 bucket, minimizing data transfer charges. How should this be achieved?',
    o: [
      'Launch an EC2 instance in the same Region as the source bucket, uploading the output to a bucket in a different Region',
      'Use an on-premises VM to retrieve the logs and upload the output to a bucket in the same Region',
      'Launch an EC2 instance in the same Region as the source bucket, uploading the output to a bucket in the same Region',
      'Connect a Lambda function to the S3 bucket via a VPC endpoint',
    ],
    a: [2],
    x: 'Manteniendo tanto la instancia de procesamiento como el bucket de salida en la misma region que el bucket de origen se evita cualquier cargo de transferencia de datos entre regiones. Usar una VM on-premises implicaria transferencia saliente de la region (con coste), enviar el resultado a otra region tambien genera cargos, y Lambda no puede ejecutar una aplicacion Linux personalizada de este tipo.',
  },
  {
    n: 16,
    d: 'COST',
    m: 0,
    q: 'A retailer sends coupons twice a week, causing predictable traffic surges on an EC2 fleet behind an ELB, and wants to lower costs while still meeting demand. What do you recommend?',
    o: [
      'Use capacity reservations with savings plans',
      'Purchase EC2 dedicated hosts',
      'Use a mixture of spot and on-demand instances',
      'Increase the instance size of the existing EC2 instances',
    ],
    a: [0],
    x: 'Combinar Savings Plans (que dan descuento significativo frente a On-Demand) con capacity reservations garantiza que la capacidad necesaria para los picos predecibles este disponible a menor coste. Los dedicated hosts se usan por cumplimiento/licencias (no ahorro), mezclar Spot es arriesgado para picos predecibles que deben estar garantizados, y simplemente aumentar el tamano de instancia eleva el coste sin optimizarlo.',
  },
  {
    n: 17,
    d: 'PERF',
    m: 0,
    q: 'A decoupled application uses an MQTT-based message broker that will migrate to EC2 instances, without requiring application code changes. Which AWS service fits?',
    o: ['Amazon MQ', 'Amazon SNS', 'Amazon SQS', 'AWS Step Functions'],
    a: [0],
    x: 'Amazon MQ es un broker de mensajes gestionado compatible con protocolos estandar de la industria incluido MQTT, permitiendo migrar sin reescribir el codigo de mensajeria existente. SQS es un servicio propietario de AWS sin soporte MQTT, SNS es un servicio de notificaciones (no un broker de colas), y Step Functions es orquestacion de workflows, no mensajeria.',
  },
  {
    n: 18,
    d: 'SEC',
    m: 0,
    q: 'IAM users must rotate access keys every 60 days, and keys found older must be removed automatically. Which solution meets these requirements?',
    o: [
      'Create an AWS Config rule to check for key age, configured to trigger an SNS notification',
      'Create an EventBridge rule to check for key age, triggering a Lambda function that removes the key',
      'Create an EventBridge rule to check for key age, triggering an AWS Config remediation that removes the key',
      'Create an AWS Config rule to check for key age, with an EventBridge rule that executes a Lambda function to remove the key',
    ],
    a: [3],
    x: 'La regla gestionada de Config (access-keys-rotated) evalua el estado NON_COMPLIANT cuando la clave supera el maximo de antiguedad, y EventBridge puede reaccionar a ese cambio de estado disparando un Lambda que elimina la clave, automatizando el ciclo completo. EventBridge no evalua por si solo la antiguedad de la clave (eso es tarea de Config), y solo notificar via SNS no elimina automaticamente la clave.',
  },
  {
    n: 19,
    d: 'SEC',
    m: 0,
    q: 'A company wants to reduce complexity in managing software licenses across a hybrid setup with some licenses in the cloud and some on-premises. What actions should be taken?',
    o: [
      'Use Secrets Manager to store the licenses as secrets',
      'Use AWS License Manager to manage the software licenses',
      'Use AWS Key Management Service to store the license key securely',
      'Use S3 with governance lock to manage the storage of the licenses',
    ],
    a: [1],
    x: 'AWS License Manager esta disenado especificamente para gestionar licencias de software de proveedores como Microsoft, SAP y Oracle a traves de entornos hibridos, con reglas personalizadas que reflejan los terminos de licencia. Secrets Manager y KMS estan pensados para secretos/claves de cifrado (no licencias de software), y S3 no es una herramienta de gestion de licencias.',
  },
  {
    n: 20,
    d: 'SEC',
    m: 0,
    q: '20 department accounts under AWS Organizations need different access levels to items in one shared S3 bucket, adhering to least privilege with the simplest approach. What is best?',
    o: [
      'Use S3 access points to administer different access policies per team, controlling them with SCPs in AWS Organizations',
      'Copy items into separate buckets per team and administer bucket policies for each account',
      'Create the S3 bucket in one account, and configure a cross-account IAM role per user with scoped permissions',
      'Create a new AWS Organization, assigning each team to a different OU with appropriate permissions',
    ],
    a: [0],
    x: 'Los S3 access points permiten crear politicas de acceso distintas por equipo apuntando al mismo bucket subyacente, y se pueden controlar adicionalmente con SCPs de la organizacion existente, sin duplicar datos. Duplicar el bucket por equipo aumenta el riesgo de error y el coste de almacenamiento, crear roles cross-account por usuario es mas complejo de administrar, y crear una nueva Organizacion es innecesario cuando ya existe una.',
  },
  {
    n: 21,
    d: 'PERF',
    m: 0,
    q: 'A tightly-coupled HPC application using MPI runs across many nodes, requiring a service-managed deployment with minimal operational overhead. Which option is most suitable?',
    o: [
      'Use Elastic Beanstalk to provision and manage the EC2 instances',
      'Use CloudFormation to deploy a Cluster Placement Group on EC2',
      'Use AWS Batch to deploy a multi-node parallel job',
      'Use EC2 Auto Scaling to deploy instances in multiple subnets',
    ],
    a: [2],
    x: 'Los multi-node parallel jobs de AWS Batch estan disenados justo para cargas HPC fuertemente acopladas con soporte MPI, gestionando el aprovisionamiento y coordinacion de instancias sin que el usuario tenga que administrar EC2 directamente. CloudFormation despliega un cluster placement group pero no lo gestiona en ejecucion, Elastic Beanstalk no esta orientado a este tipo de carga HPC especializada, y Auto Scaling en varias subnets no coordina la comunicacion MPI entre nodos.',
  },
  {
    n: 22,
    d: 'PERF',
    m: 0,
    q: 'A serverless, microservices-based web application needs a GraphQL API as a single entry point. Which AWS managed service fits?',
    o: ['Amazon Athena', 'API Gateway', 'AWS AppSync', 'AWS Lambda'],
    a: [2],
    x: 'AppSync es el servicio de AWS pensado especificamente para APIs GraphQL, ofreciendo un unico endpoint que consulta o actualiza datos de multiples bases de datos, microservicios y APIs de forma segura. API Gateway no soporta GraphQL de forma nativa, Athena es para consultas SQL sobre S3, y Lambda es solo computo, no un servicio de API en si mismo.',
  },
  {
    n: 23,
    d: 'SEC',
    m: 0,
    q: 'New objects uploaded to S3 must remain unchangeable for an unspecified period until the company decides otherwise, with only specific users able to delete them. What should be done?',
    o: [
      'Use CloudTrail to track actions and restore modified objects from backup versions if a delete occurs',
      'Create a new bucket with Object Lock enabled, versioning enabled, and a 50-year retention in governance mode',
      'Create a bucket with Object Lock enabled, versioning enabled, apply a legal hold, and grant s3:PutObjectLegalHold only to users who need to delete objects',
      'Create an S3 Glacier vault and apply a vault lock policy to prevent object modification',
    ],
    a: [2],
    x: 'S3 Object Lock con un legal hold protege indefinidamente los objetos hasta que se retire explicitamente el hold, y conceder el permiso s3:PutObjectLegalHold solo a los usuarios autorizados controla quien puede levantarlo para eliminar objetos. CloudTrail solo registra actividad (no evita cambios), un periodo de retencion fijo de 50 anos en modo gobernanza no es "indefinido hasta que la empresa decida" y ademas el modo gobernanza puede ser anulado por usuarios privilegiados, y Glacier Vault Lock no aplica a un bucket S3 activo para acceso normal.',
  },
  {
    n: 24,
    d: 'RES',
    m: 0,
    q: 'Three VPCs in the same Region need to share two Direct Connect connections to two company offices, using a Direct Connect gateway already created. How should the connectivity be configured?',
    o: [
      'Create a transit virtual interface between the Direct Connect gateway and each VPC',
      'Associate the Direct Connect gateway to a virtual private gateway in each VPC',
      'Create a VPC peering connection between the VPCs and route entries for the Direct Connect gateway',
      'Associate the Direct Connect gateway to a transit gateway',
    ],
    a: [3],
    x: 'Asociar el Direct Connect gateway a un Transit Gateway permite gestionar una sola conexion compartida entre multiples VPCs de la misma region, simplificando la arquitectura. VPC peering no admite entradas de ruta hacia un Direct Connect gateway de esa forma, una virtual private gateway por VPC es innecesaria cuando ya existe un Transit Gateway, y la transit virtual interface se conecta del lado de la conexion DX, no directamente a cada VPC.',
  },
  {
    n: 25,
    d: 'PERF',
    m: 0,
    q: 'Decoupled batches of up to 1000 messages per second must be received in the correct order by consumers. What should be done?',
    o: [
      'Create an AWS Step Functions state machine',
      'Create an SQS FIFO queue',
      'Create an SNS topic',
      'Create an SQS Standard queue',
    ],
    a: [1],
    x: 'Solo las colas FIFO garantizan el orden de los mensajes, y con batching soportan hasta 3,000 mensajes por segundo, cubriendo el requisito de 1000 mensajes/segundo en orden. Una cola standard no garantiza orden, SNS es notificacion (no cola con orden garantizado), y Step Functions es orquestacion de workflows, no un mecanismo de mensajeria ordenada.',
  },
  {
    n: 26,
    d: 'COST',
    m: 0,
    q: 'Files on a shared EFS file system are only accessed for the first few days after creation but must be retained. How can storage costs be optimized?',
    o: [
      'Implement Storage Gateway and transition files to S3 after 7 days',
      'Configure a lifecycle policy to move the files to the S3 Standard-IA storage class after 7 days',
      'Configure a lifecycle policy to move the files to the EFS Infrequent Access (IA) storage class after 7 days',
      'Move the files to an instance store on each EC2 instance after 7 days',
    ],
    a: [2],
    x: 'EFS soporta una lifecycle policy nativa que transiciona los archivos no accedidos durante el periodo definido (por ejemplo, 7 dias) a la clase EFS-IA de menor coste, manteniendolos en el mismo sistema de archivos compartido. No se puede transicionar directamente de EFS a S3 mediante una lifecycle policy de EFS, Storage Gateway no aplica aqui, y un instance store es efimero y pondria los datos en riesgo de perdida.',
  },
  {
    n: 27,
    d: 'PERF',
    m: 0,
    q: 'A company needs to aggregate customer support communications from multiple channels, run sentiment analysis, and store results in a data warehouse. Which series of AWS services fits?',
    o: [
      'DynamoDB for the original data, Amazon Comprehend for sentiment analysis, and Redshift for output storage',
      'DynamoDB for the original data, Amazon Kendra for sentiment analysis, and Redshift for output storage',
      'An S3 data lake as the original data store, Amazon Textract for sentiment analysis, and Redshift for output storage',
      'An S3 data lake as the original data store, Amazon Comprehend for sentiment analysis, and Redshift for output storage',
    ],
    a: [3],
    x: 'Un data lake en S3 es un repositorio ideal para datos de multiples canales usados en machine learning, Comprehend detecta sentimiento y extrae insights del texto de forma nativa, y Redshift almacena los resultados agregados para analitica. DynamoDB es menos adecuado como repositorio de datos para ML que un data lake, Textract extrae texto de documentos escaneados (no analiza sentimiento), y Kendra es un motor de busqueda inteligente, no de analisis de sentimiento.',
  },
  {
    n: 28,
    d: 'RES',
    m: 1,
    q: 'A web app with static and dynamic content runs on EC2 in an ASG behind an ALB, with an Aurora database, and needs to be more resilient to periodic increases in request rates. Which two architecture changes should be made? (Choose two.)',
    o: [
      'Add Aurora Replicas',
      'Add a Global Accelerator',
      'Add a Direct Connect link',
      'Add a Transit Gateway',
      'Add a CloudFront distribution',
    ],
    a: [0, 4],
    x: 'CloudFront cachea contenido en edge locations reduciendo la carga que llega hasta el ALB durante los picos, y los Aurora Replicas descargan la carga de lectura sobre la instancia primaria, ambas mejoras directas de resiliencia ante picos de peticiones. Transit Gateway y Direct Connect no aportan valor a este escenario, y Global Accelerator mejora enrutamiento global mas que absorber picos de contenido cacheable, para lo cual CloudFront es mas apropiado.',
  },
  {
    n: 29,
    d: 'RES',
    m: 1,
    q: 'A company runs the same app in multiple Regions behind two NLBs, advertised via two public IPs from two /24 ranges they want to keep unchanged, and wants users routed to the closest endpoint. Which two actions should be taken? (Choose two.)',
    o: [
      'Assign new static anycast IP addresses and modify any existing pointers',
      'Create PTR records to map the existing public IPs to an Alias',
      'Create a Route 53 geolocation-based routing policy',
      'Migrate both public IP addresses to AWS Global Accelerator',
      'Create an AWS Global Accelerator and attach endpoints in each Region',
    ],
    a: [3, 4],
    x: 'Global Accelerator permite traer las propias direcciones IP existentes (hasta dos rangos /24) y usarlas como puntos de entrada fijos, con endpoints anadidos en cada region para dirigir a los usuarios al mas cercano por la red global de AWS. Asignar IPs anycast nuevas es innecesario cuando se pueden traer las propias, el enrutamiento geografico de Route 53 exigiria nuevas IPs por region, y no existe una forma de mapear PTR records a un Alias para este proposito.',
  },
  {
    n: 30,
    d: 'SEC',
    m: 0,
    q: 'A podcast company wants to gradually release content by region via CloudFront and ensure listeners outside those regions cannot access it. What should be done?',
    o: [
      'Create a new URL for the content with an expiration date-based access policy for signed URLs',
      'Create a new URL, control access with signed URLs and cookies, and set up a custom error message',
      'Implement CloudFront geo restriction using a deny list and create a custom error message',
      'Encrypt the content data and establish a custom error message',
    ],
    a: [2],
    x: 'La geo restriction de CloudFront con una lista de bloqueo impide directamente el acceso desde regiones no autorizadas, y un mensaje de error personalizado informa a esos usuarios sin exponer el contenido. Las signed URLs/cookies no evaluan la ubicacion geografica del usuario, y cifrar los datos no restringe el acceso por region, solo protege la confidencialidad del contenido en si.',
  },
  {
    n: 31,
    d: 'SEC',
    m: 0,
    q: 'An online education platform must ensure only enrolled students access course materials in S3 via CloudFront, and occasionally grant exclusive resources to certain students. What should be done?',
    o: [
      'Create and provide S3 presigned URLs to authenticated students',
      'Implement CloudFront Field-Level Encryption to block non-enrolled students',
      'Utilize S3 object-level encryption for course materials',
      'Implement CloudFront signed cookies for authenticated students',
    ],
    a: [3],
    x: 'Las signed cookies de CloudFront aplican el control de acceso a multiples archivos/directorios a la vez tras autenticar al estudiante, encajando mejor que gestionar URLs firmadas individuales por archivo cuando hay muchos recursos. El cifrado field-level de CloudFront esta pensado para proteger datos sensibles en peticiones POST (no para controlar acceso a contenido), y el cifrado a nivel de objeto en S3 protege la confidencialidad en reposo, no controla quien puede acceder.',
  },
  {
    n: 32,
    d: 'COST',
    m: 0,
    q: 'PDF statements are downloadable from a website for up to 30 days after creation, and a ZIP of all statements is emailed when accounts close. What is the most cost-effective storage solution?',
    o: [
      'Store using S3 Glacier, then transition to S3 Glacier Deep Archive after 30 days',
      'Store using S3 Standard-IA, then transition to S3 Intelligent-Tiering after 30 days',
      'Store using S3 Standard, then transition to S3 Glacier storage after 30 days',
      'Store using S3 Standard, then transition to S3 One Zone-IA storage after 30 days',
    ],
    a: [2],
    x: 'S3 Standard encaja con las descargas frecuentes del sitio web durante los primeros 30 dias, y transicionar a Glacier despues reduce drasticamente el coste de almacenamiento para el largo plazo, ya que a partir de ahi el acceso es infrecuente (solo al cierre de cuenta). Glacier desde el inicio dificultaria las descargas rapidas en los primeros 30 dias, y S3 Standard-IA/One Zone-IA son mas caros que Glacier para el almacenamiento a largo plazo tras el periodo activo.',
  },
  {
    n: 33,
    d: 'SEC',
    m: 0,
    q: 'A golden AMI is shared across accounts, and the company needs to detect and alert whenever the EC2 CreateImage API is called within the account, with the least operational overhead. What should be recommended?',
    o: [
      'Configure an SQS FIFO queue as a target for CloudTrail logs, with Lambda sending an alert to SNS when CreateImage is detected',
      'Configure CloudTrail with an SNS notification when logs are sent to S3, and use Athena to query for CreateImage calls',
      'Create a Lambda function to query CloudTrail logs and send an alert when CreateImage is detected',
      'Create an EventBridge rule for the CreateImage API call, with an SNS topic as the target to send the alert',
    ],
    a: [3],
    x: 'EventBridge puede definir una regla basada directamente en llamadas de la API registradas por CloudTrail, disparando una accion (como una notificacion SNS) en cuanto se detecta CreateImage, con el minimo esfuerzo operativo. CloudTrail no puede enviar directamente a una cola SQS, consultar con Athena requiere pasos adicionales de configuracion, y crear un Lambda personalizado para consultar logs anade complejidad innecesaria frente a una regla nativa de EventBridge.',
  },
  {
    n: 34,
    d: 'SEC',
    m: 0,
    q: 'A password should be required to access an ElastiCache for Redis cluster for enhanced security. What should be used?',
    o: ['AWS IAM Policy', 'Redis AUTH command', 'VPC Security Group', 'AWS Directory Service'],
    a: [1],
    x: 'El comando/parametro AUTH de Redis exige un token (contrasena) antes de permitir que los clientes ejecuten comandos, mejorando la seguridad de acceso a los datos en el propio motor. Una politica IAM no aplica autenticacion a nivel de Redis, un security group protege a nivel de red (no de aplicacion), y Directory Service es un servicio de directorio Active Directory sin relacion con la autenticacion de Redis.',
  },
  {
    n: 35,
    d: 'SEC',
    m: 0,
    q: 'All resources for various web applications must be tagged with the appropriate application name, minimizing effort and automating the task. What should be done with the least operational overhead?',
    o: [
      'Write API calls to check all resources for proper tagging, scheduled via a Lambda function through CloudWatch',
      'Use Cost Explorer to display untagged application components, then tag those resources with a Python script',
      'Configure CloudTrail to send events to CloudWatch Logs, using insights queries to detect missing TagResources actions',
      'Use AWS Config to detect resources that are not properly tagged, creating a Systems Manager automation document for remediation',
    ],
    a: [3],
    x: 'AWS Config detecta de forma nativa recursos que incumplen las reglas de etiquetado, y un documento de automatizacion de Systems Manager puede corregirlo automaticamente, minimizando el esfuerzo operativo. Cost Explorer no esta pensado para cumplimiento de configuracion, escribir llamadas a la API propias anade desarrollo evitable, y usar CloudTrail+Logs Insights para detectar acciones TagResources faltantes es ineficiente e indirecto frente a Config.',
  },
  {
    n: 36,
    d: 'PERF',
    m: 0,
    q: 'Customer transaction data structured hierarchically in S3 needs complex queries run as quickly as possible with minimal operational overhead. What should be recommended?',
    o: [
      'Use Athena on S3 to perform the queries',
      'Use Elasticsearch on S3 to perform the queries',
      'Use Data Pipeline to move data into EMR, then query it',
      'Use Glue to transform the data into Redshift tables, then query it',
    ],
    a: [0],
    x: 'Athena es un servicio serverless de consultas SQL interactivas directamente sobre S3, sin infraestructura que gestionar y con soporte para consultas complejas (joins, funciones de ventana), siendo la opcion mas rapida de poner en marcha y con menor coste operativo. Elasticsearch no ejecuta consultas SQL de forma nativa sobre S3, y montar EMR o Redshift para este caso anade infraestructura y coste operativo innecesarios.',
  },
  {
    n: 37,
    d: 'PERF',
    m: 0,
    q: 'Proprietary application log files in JSON format sit in S3, needing on-demand, simple query execution with minimal changes to the existing architecture. What should be done with the lowest operational overhead?',
    o: [
      'Use Glue to catalog the logs, with a transient Spark cluster on EMR running the SQL queries on demand',
      'Use Athena to query and analyze the data in S3 with standard SQL on demand',
      'Use Redshift to place all content in one place and run SQL queries as needed',
      'Use CloudWatch Logs for log storage, running SQL queries on demand from the CloudWatch console',
    ],
    a: [1],
    x: 'Athena consulta directamente los archivos JSON en S3 con SQL estandar sin necesitar mover ni transformar los datos, cumpliendo el requisito de minimos cambios y bajo overhead. Redshift y EMR requieren cargar/migrar los datos primero (mucho refactorizado), y CloudWatch Logs Insights no usa SQL estandar y exigiria mover los logs alli primero.',
  },
  {
    n: 38,
    d: 'COST',
    m: 1,
    q: 'A company purchased an EC2 Reserved Instance for a project that has now ended, leaving the RI unused, and wants to apply its benefit across other AWS accounts. Which two steps achieve this? (Choose two.)',
    o: [
      'Use Organizations to establish a new payer account and invite the other accounts to join',
      'Establish an Organization in the account that purchased the RI, inviting the other accounts to join from the management account',
      'Enable Reserved Instance sharing in billing preferences for the management account',
      'Enable Reserved Instance sharing in billing preferences for the account that purchased the RI',
      'Use AWS Resource Access Manager from the management account to share the Reserved Instance with other accounts',
    ],
    a: [1, 3],
    x: 'Crear una Organizacion desde la cuenta que compro la RI e invitar a las demas habilita la facturacion consolidada, y habilitar el compartir Reserved Instances en las preferencias de facturacion de esa misma cuenta compradora permite que el beneficio se aplique a instancias de otras cuentas del grupo. No hace falta crear una cuenta pagadora nueva, la opcion de compartir debe activarse en la cuenta propietaria de la RI (no solo en la de gestion), y AWS RAM no se usa para compartir Reserved Instances.',
  },
  {
    n: 39,
    d: 'RES',
    m: 0,
    q: 'An e-commerce app uses EC2 instance stores for session data and wants that data to be highly available and stored durably. What should be recommended?',
    o: [
      'Store the session data in a DynamoDB table',
      'Move the session data to ElastiCache for Memcached',
      'Move the session data to S3 Glacier Deep Archive',
      'Deploy a larger EC2 instance with a larger instance store',
    ],
    a: [0],
    x: 'DynamoDB es un servicio de base de datos totalmente gestionado, duradero y de alta disponibilidad, con replicacion automatica entre AZs, ideal para persistir datos de sesion mas alla del ciclo de vida de la instancia EC2. Memcached es una cache en memoria sin durabilidad garantizada, Glacier Deep Archive tiene tiempos de recuperacion de horas (inadecuado para sesiones), y ampliar el instance store no soluciona su naturaleza efimera.',
  },
  {
    n: 40,
    d: 'PERF',
    m: 0,
    q: 'Shipment tracking data updates every few seconds and requires real-time analysis before landing in an S3 data lake. Which solution offers the most operational efficiency?',
    o: [
      'Use SQS for data ingestion and EMR for real-time analysis',
      'Use Kinesis Data Streams for data ingestion and Lambda for real-time data analysis',
      'Use Direct Connect for data ingestion and Athena for real-time analysis',
      'Use Kinesis Data Firehose for data ingestion and Kinesis Data Analytics for real-time analysis',
    ],
    a: [3],
    x: 'Kinesis Data Firehose ingiere datos de alta velocidad y los entrega directamente al data lake en S3, mientras Kinesis Data Analytics permite analizar el stream en tiempo real, ambos totalmente gestionados y con la maxima eficiencia operativa para este caso. SQS y EMR exigen mas gestion de infraestructura, Direct Connect es solo conectividad de red (no ingesta de datos), y Athena esta pensada para consultas ad-hoc sobre datos ya almacenados, no analisis en tiempo real del stream.',
  },
  {
    n: 41,
    d: 'RES',
    m: 0,
    q: 'A microservices app on ECS needs a resilient, shared filesystem restorable to a different Region with an RPO of 2 hours, providing a mount target per AZ, using AWS Backup for cross-Region replication. Which option meets this?',
    o: [
      'Amazon FSx for OpenZFS',
      'Amazon FSx for NetApp ONTAP with a Multi-AZ deployment',
      'Amazon EFS with the Standard storage class',
      'Amazon FSx for Windows File Server with a Multi-AZ deployment',
    ],
    a: [2],
    x: 'EFS Standard ofrece mount targets en cada AZ de la region de forma nativa y se integra con AWS Backup para replicacion y restauracion cross-region, cumpliendo el RPO de 2 horas con una arquitectura simple y escalable para ECS. FSx for OpenZFS y NetApp ONTAP son mas complejos y no ofrecen esa misma integracion nativa multi-AZ sencilla, y FSx for Windows esta pensado para cargas Windows/SMB, no para microservicios Linux tipicos en ECS.',
  },
  {
    n: 42,
    d: 'PERF',
    m: 0,
    q: 'Multiple Windows .NET/SQL Server application servers on EC2 need a shared file system that is highly available, durable, and provides high throughput and IOPS. What is the best way to meet this?',
    o: [
      'Extend to EFS with a Multi-AZ configuration and migrate the data there',
      'Migrate all data to S3 and set up IAM authentication for user access',
      'Extend to FSx for Windows File Server with a Multi-AZ configuration and migrate the data there',
      'Set up an S3 File Gateway and mount it on the existing EC2 instances',
    ],
    a: [2],
    x: 'FSx for Windows File Server con Multi-AZ da un sistema de archivos SMB totalmente gestionado, altamente disponible y con alto throughput/IOPS, integrandose ademas con IAM, Active Directory y KMS, ideal para cargas Windows/.NET/SQL Server. EFS solo soporta NFS (no aplica a Windows), S3 no es un sistema de archivos SMB, y S3 File Gateway esta pensado para entornos on-premises, no para instancias EC2 directamente.',
  },
  {
    n: 43,
    d: 'PERF',
    m: 0,
    q: 'A shared Microsoft filesystem uses Distributed File System Namespaces (DFSN) and needs a migration strategy to AWS. What is the most suitable approach?',
    o: [
      'Use AWS Server Migration Service to migrate to FSx for Lustre',
      'Use AWS DataSync to migrate to an EFS filesystem',
      'Use AWS DataSync to migrate to FSx for Windows File Server',
      'Use AWS Server Migration Service to migrate to an S3 bucket',
    ],
    a: [2],
    x: 'FSx for Windows File Server soporta DFSN de forma nativa, y DataSync automatiza la migracion de datos directamente hacia ese destino, siendo la combinacion mas adecuada para un sistema de archivos Microsoft. FSx for Lustre no soporta DFSN, EFS es exclusivamente Linux/NFS, y Server Migration Service esta pensado para migrar maquinas virtuales, no datos de archivos.',
  },
  {
    n: 44,
    d: 'PERF',
    m: 1,
    q: 'A public-facing three-tier app (EC2 web tier behind an ALB, connecting directly to DynamoDB) is slow, and the solution cannot be re-architected. Which two actions should be taken? (Choose two.)',
    o: [
      'Create a CloudFront distribution in front of the Application Load Balancer',
      'Migrate the entire stack to Elastic Beanstalk with web server and worker environments',
      'Migrate the DynamoDB database to Aurora with a multi-AZ deployment',
      'Set up a DynamoDB Accelerator (DAX) cluster in front of the DynamoDB table',
      'Migrate the web application to a containerized solution using AWS Fargate',
    ],
    a: [0, 3],
    x: 'Una distribucion de CloudFront delante del ALB cachea contenido y reduce la latencia percibida sin cambiar la arquitectura, y un cluster DAX delante de DynamoDB reduce drasticamente la latencia de lectura, ambas mejoras que no exigen rediseno. Las demas opciones (Elastic Beanstalk, Aurora, Fargate) implican migrar de plataforma o de motor de base de datos, lo cual va en contra del requisito explicito de no rearquitecturar.',
  },
  {
    n: 45,
    d: 'PERF',
    m: 0,
    q: 'An e-commerce app tests a spike in requests and encounters performance issues, with an Aurora PostgreSQL database, a Lambda compute layer, and API Gateway. A solution is needed to improve scalability with minimal configuration effort. What should be recommended?',
    o: [
      'Set up two Lambda functions integrated via an SQS queue: one to receive the info, one to load it into the database',
      'Set up two Lambda functions integrated via SNS: one to receive the info, one to load it into the database',
      'Refactor the Lambda code to run on Apache Tomcat on EC2 instances, connecting via native JDBC drivers',
      'Change the platform from Aurora to DynamoDB, provisioning a DAX cluster and using the DAX client SDK',
    ],
    a: [0],
    x: 'Desacoplar con una cola SQS entre una funcion Lambda que recibe la informacion y otra que la carga en la base de datos absorbe picos de peticiones sin exigir cambios de plataforma, mejorando la escalabilidad con poco esfuerzo de configuracion. SNS esta pensado para difusion (fan-out), no para colas con procesamiento asincrono controlado, migrar a EC2/Tomcat implica reescribir la aplicacion, y cambiar a DynamoDB+DAX es un cambio de plataforma mayor no justificado por el problema descrito.',
  },
  {
    n: 46,
    d: 'PERF',
    m: 0,
    q: '500 Amazon EC2 instances used for batch processing need a third-party tool installed quickly and efficiently, with future scalability for new instances. What should a solutions architect do with the LEAST effort?',
    o: [
      'Create a Lambda function to make configuration changes to all instances, validating installation with another Lambda',
      'Use Systems Manager Patch Manager to install the tool on all instances within a single patch',
      'Use Systems Manager Run Command to run a custom command that installs the tool on all instances',
      'Use Systems Manager Maintenance Windows to install the tool on all instances within a set period',
    ],
    a: [2],
    x: 'Systems Manager Run Command ejecuta el mismo comando en un gran grupo de instancias sin necesitar conexion individual ni gestionar claves de acceso, escalando facilmente a nuevas instancias que se anadan. Patch Manager esta pensado para gestionar parches (no instalar herramientas de terceros arbitrarias), Maintenance Windows solo define ventanas de tiempo para tareas (no ejecuta comandos por si mismo), y una solucion custom con Lambda anade complejidad de desarrollo innecesaria.',
  },
  {
    n: 47,
    d: 'RES',
    m: 0,
    q: 'A persistent block storage volume needs data encrypted at rest with automated volume-level backups. Which solution options should be used?',
    o: [
      'Use an encrypted EBS volume and Data Lifecycle Manager to automate snapshots',
      'Use an encrypted EFS filesystem with a CloudWatch Events rule to start backups via Lambda',
      'Use server-side encryption on an S3 bucket and Cross-Region Replication for backup on a schedule',
      'Use an encrypted EC2 instance store, copying data to another EC2 instance via a cron job',
    ],
    a: [0],
    x: 'Un volumen EBS cifrado combinado con Data Lifecycle Manager automatiza los snapshots a nivel de volumen, cumpliendo tanto el requisito de cifrado en reposo como el de backups automatizados, siendo la solucion nativa para almacenamiento de bloque persistente. EFS es almacenamiento de archivos (no de bloque), S3 es almacenamiento de objetos, y un instance store es efimero, por lo que no cumple el requisito de persistencia.',
  },
  {
    n: 48,
    d: 'PERF',
    m: 0,
    q: 'Large data on an on-premises NAS device is accessed via the SMB protocol, needing a managed filesystem service and an automated migration tool. What actions should be taken?',
    o: [
      'Migrate the data to FSx for Lustre using DataSync',
      'Migrate the data to S3 using a Snowball Edge device',
      'Migrate the data to EFS using the AWS Server Migration Service',
      'Migrate the data to FSx for Windows File Server using DataSync',
    ],
    a: [3],
    x: 'FSx for Windows File Server ofrece almacenamiento de archivos gestionado accesible via SMB, y DataSync automatiza la migracion desde servidores de archivos SMB hacia ese destino de forma nativa. FSx for Lustre no soporta SMB, EFS solo soporta NFS (no SMB), y Snowball Edge se usa para transferencia masiva de datos, no para migracion continua hacia un sistema de archivos SMB gestionado.',
  },
  {
    n: 49,
    d: 'PERF',
    m: 0,
    q: 'An on-premises MySQL database needs migration to an AWS database service for higher availability and better performance, while offloading reporting workloads from the primary database. What is the most operationally efficient solution?',
    o: [
      'Self-managed MySQL on EC2 with a replication setup for reporting, spread across AZs',
      'Amazon Aurora with MySQL compatibility, directing reporting to an Aurora Replica',
      'Amazon RDS MySQL Single-AZ with a read replica in the same AZ, used for reporting',
      'AWS DMS to create an Aurora DB cluster in multiple Regions, pointing reporting to a separate instance',
    ],
    a: [1],
    x: 'Aurora es una base gestionada de alto rendimiento y disponibilidad, y sus Aurora Replicas descargan la carga de reporting sin afectar a la instancia primaria, con backups, parcheo y replicacion gestionados automaticamente. MySQL autogestionado en EC2 exige mucha mas administracion manual, Single-AZ no da alta disponibilidad, y usar DMS para crear un cluster multi-region es innecesario y mas complejo que un simple replica local.',
  },
  {
    n: 50,
    d: 'SEC',
    m: 0,
    q: 'A finance-based organization needs additional protection against SQL injection and cross-site scripting for a serverless API Gateway app deployed in two AWS Regions. What is the LEAST administratively demanding solution?',
    o: [
      'Set up Firewall Manager in both Regions to centrally configure WAF rules',
      'Set up WAF in both Regions and associate Regional web ACLs with an API stage',
      'Set up Shield in both Regions and associate Regional web ACLs with an API stage',
      'Set up Shield in one Region and associate a Regional web ACL with an API stage',
    ],
    a: [0],
    x: 'AWS Firewall Manager permite gestionar de forma centralizada las reglas de WAF (que protegen contra SQL injection y XSS) en multiples regiones y cuentas a la vez, minimizando el esfuerzo administrativo frente a configurar WAF manualmente en cada region. Shield protege contra DDoS (no ataques a nivel de aplicacion como estos), y configurar WAF por separado en cada region es mas trabajo administrativo que hacerlo de forma centralizada con Firewall Manager.',
  },
  {
    n: 51,
    d: 'SEC',
    m: 0,
    q: 'Demographic data from image analysis is saved to a CSV file in another S3 bucket, requiring encryption at rest and SQL querying, in a fully serverless solution. What actions should be taken to encrypt and query the data?',
    o: [
      'Use KMS encryption keys for the bucket and use Kinesis Data Analytics to query the data',
      'Use KMS encryption keys for the bucket and use Athena to query the data',
      'Use S3 server-side encryption and use Redshift Spectrum to query the data',
      'Use S3 server-side encryption and Amazon QuickSight to query the data',
    ],
    a: [1],
    x: 'KMS cifra los datos en reposo en el bucket, y Athena permite consultarlos directamente con SQL sin gestionar infraestructura, cumpliendo el requisito de solucion totalmente serverless con cifrado y consultas. Kinesis Data Analytics esta pensado para streams en tiempo real (no archivos CSV estaticos), Redshift Spectrum exige un cluster Redshift (no es serverless), y QuickSight es una herramienta de visualizacion, no de consultas SQL directas.',
  },
  {
    n: 52,
    d: 'RES',
    m: 0,
    q: 'A financial firm needs a DR solution for an on-premises PostgreSQL VM with an RPO under a minute and an RTO within two hours, keeping costs minimal. Which solution meets these requirements?',
    o: [
      'Use AWS Elastic Disaster Recovery with continuous replication as a pilot light solution',
      'Set up a warm standby RDS for PostgreSQL database, configuring DMS to use change data capture (CDC)',
      'Use third-party backup software for daily backups, storing a secondary set in S3',
      'Configure an active-active multi-site setup between on-premises and AWS using a third-party HA solution',
    ],
    a: [1],
    x: 'Un RDS PostgreSQL en modo warm standby, alimentado por AWS DMS con change data capture, mantiene una replica casi en tiempo real que puede promoverse rapidamente, cumpliendo el RPO de menos de un minuto y el RTO de dos horas de forma coste-efectiva. Los backups diarios con software de terceros implican perdida de datos de hasta 24 horas (incumple el RPO), un setup activo-activo con HA de terceros es mas caro y complejo de lo necesario, y Elastic Disaster Recovery esta mas orientado a servidores completos que a bases de datos especificas con estas garantias.',
  },
];
