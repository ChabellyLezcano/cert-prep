import type { RawQuestion } from '@/quiz/quiz.types';

/**
 * AWS SAA-C03 practice set 8 (54 questions), authored by Chabe.
 */
export const exam109: RawQuestion[] = [
  {
    n: 1,
    d: 'SEC',
    m: 0,
    q: 'Malicious traffic is coming from different IP addresses under the same CIDR range. What should be recommended to the security team?',
    o: [
      'Add a rule in the inbound table of the security group to deny traffic from that CIDR range',
      'Add a rule in the outbound table of the security group to deny traffic from that CIDR range',
      'Add a deny rule in the inbound table of the network ACL with a lower rule number than other rules',
      'Add a deny rule in the outbound table of the network ACL with a lower rule number than other rules',
    ],
    a: [2],
    x: 'Solo las network ACL admiten reglas deny, y se evaluan en orden numerico, asi que colocar la regla con un numero mas bajo asegura que se aplique antes que otras reglas permisivas. Los security groups no soportan reglas deny, y una regla outbound solo bloquearia el trafico saliente, no el entrante malicioso.',
  },
  {
    n: 2,
    d: 'PERF',
    m: 0,
    q: 'An e-commerce web application needs a highly scalable key-value database. Which AWS database service should be used?',
    o: ['Amazon DynamoDB', 'Amazon Redshift', 'Amazon ElastiCache', 'Amazon RDS'],
    a: [0],
    x: 'DynamoDB es una base de datos NoSQL clave-valor totalmente gestionada, con rendimiento rapido y predecible que escala sin limites practicos, ideal para este caso. RDS es relacional (no clave-valor), Redshift es un data warehouse para analitica, y ElastiCache es una cache en memoria, no una base de datos principal persistente.',
  },
  {
    n: 3,
    d: 'RES',
    m: 0,
    q: 'An app on ECS performs data processing in two parts; the second part takes much longer. How can the data processing be decoupled from the backend application component?',
    o: [
      'Process both parts in the same ECS task and create a Kinesis Firehose stream',
      'Create a DynamoDB table and save the output of the first part to the table',
      'Process each part with a separate ECS task and create an SQS queue',
      'Process each part with a separate ECS task and create an SNS topic to notify when processing completes',
    ],
    a: [2],
    x: 'Separar cada parte en su propia tarea ECS y usar una cola SQS entre ellas desacopla el procesamiento, permitiendo que la segunda parte se ejecute a su propio ritmo sin bloquear al backend, que recoge los mensajes cuando estan listos. Firehose es para streaming continuo (no coordina pasos secuenciales), DynamoDB no esta pensada como mecanismo de desacoplo, y SNS solo notifica sin ofrecer la cola/buffer que da SQS.',
  },
  {
    n: 4,
    d: 'PERF',
    m: 0,
    q: 'A gaming company collects real-time data on-premises and wants an in-memory database that supports data replication after migrating to AWS. Which database should be recommended?',
    o: [
      'Amazon RDS for MySQL',
      'Amazon RDS for PostgreSQL',
      'Amazon ElastiCache for Memcached',
      'Amazon ElastiCache for Redis',
    ],
    a: [3],
    x: 'ElastiCache for Redis soporta replicacion de datos y clustering para alta disponibilidad, a diferencia de Memcached que no ofrece replicacion nativa entre nodos. RDS MySQL y PostgreSQL no son bases de datos en memoria.',
  },
  {
    n: 5,
    d: 'RES',
    m: 0,
    q: 'An app produces data that must be processed in the order it is received, decoupled from the processing layer, with minimal operational overhead. How can this be met?',
    o: [
      'Create an SQS standard queue, with a Lambda function processing messages independently',
      'Create an SNS topic to decouple the app, with an SQS queue as a subscriber',
      'Create an SNS topic to decouple the app, with a Lambda function as a subscriber',
      'Create an SQS FIFO queue to decouple the app, with a Lambda function processing messages from the queue',
    ],
    a: [3],
    x: 'Una cola FIFO preserva estrictamente el orden de los mensajes, y Lambda puede procesarlos desde ella (via polling o destino asincrono), cumpliendo el requisito de orden con minimo overhead. Una cola standard solo ofrece orden "best-effort" (no garantizado), y SNS entrega para procesamiento inmediato, sin las garantias de cola y orden que da SQS FIFO.',
  },
  {
    n: 6,
    d: 'RES',
    m: 0,
    q: 'Staff connect from home offices to administer applications using bastion hosts in a single Region, requiring a resilient bastion host architecture with minimal ongoing operational overhead. What is best?',
    o: [
      'A Network Load Balancer backed by Reserved Instances in a cluster placement group',
      'A Network Load Balancer backed by an Auto Scaling group with instances in multiple AZs',
      'A Network Load Balancer backed by the existing servers in different AZs',
      'A Network Load Balancer backed by an Auto Scaling group with instances in multiple Regions',
    ],
    a: [1],
    x: 'Un Auto Scaling group con instancias distribuidas en varias AZs detras de un NLB da resiliencia dentro de una unica region con minima gestion continua, reemplazando automaticamente instancias que fallen. Un ASG no puede abarcar multiples regiones, un cluster placement group esta pensado para baja latencia dentro de una AZ (no resiliencia), y usar servidores existentes sin ASG no reemplaza automaticamente instancias caidas.',
  },
  {
    n: 7,
    d: 'RES',
    m: 0,
    q: 'A solution needs a relational database with disaster recovery across multiple Regions, supporting an RPO of 1 second and an RTO of 1 minute. Which AWS solution achieves this?',
    o: [
      'DynamoDB global tables',
      'Aurora Global Database',
      'RDS with a cross-Region replica',
      'RDS with Multi-AZ enabled',
    ],
    a: [1],
    x: 'Aurora Global Database replica entre regiones con latencia tipica por debajo de un segundo y permite promover una region secundaria en menos de un minuto ante un fallo, cumpliendo exactamente esos objetivos de RPO/RTO. DynamoDB no es relacional (rompe el requisito), Multi-AZ solo cubre fallos dentro de una region, y un cross-region replica de RDS tarda mucho mas en promoverse a primario.',
  },
  {
    n: 8,
    d: 'PERF',
    m: 0,
    q: 'A web app on an ASG behind an ALB processes images and needs to dynamically scale based on the number of images being uploaded, decoupled from the front end. How can this be achieved?',
    o: [
      'Create a scheduled policy that scales the ASG at times of expected peak load',
      'Create a target tracking policy that keeps the ASG at 70% CPU utilization',
      'Create an SQS queue and a custom CloudWatch metric measuring the number of messages, scaling the ASG based on it',
      'Create an SNS topic to generate a notification per uploaded image, scaling the ASG based on the number of SNS messages',
    ],
    a: [2],
    x: 'SQS desacopla el front end del procesamiento, y una metrica personalizada de CloudWatch que mide el numero de mensajes en cola permite escalar el ASG directamente en funcion de la carga real de trabajo pendiente. Escalar por CPU no refleja directamente el volumen de imagenes, un calendario fijo no se adapta a la demanda real, y SNS no ofrece un almacen de mensajes donde acumular trabajo pendiente como SQS.',
  },
  {
    n: 9,
    d: 'RES',
    m: 0,
    q: 'Microservices on EC2 behind API Gateway are not designed to elastically scale during large demand increases. Which solution addresses this concern?',
    o: [
      'Use CloudWatch alarms to notify operations staff of high CPU utilization',
      'Spread the microservices across multiple AZs and configure Data Lifecycle Manager for regular snapshots',
      'Create an SQS queue to store incoming requests, with the microservices retrieving requests from the queue for processing',
      'Use an ELB to distribute traffic between microservices, with CloudWatch metrics to monitor traffic',
    ],
    a: [2],
    x: 'Una cola SQS amortigua los picos de peticiones almacenandolas hasta que los microservicios (que no escalan elasticamente) puedan procesarlas a su propio ritmo, evitando la saturacion. Las alarmas de CloudWatch solo notifican (no resuelven la sobrecarga), el multi-AZ y los snapshots mejoran disponibilidad/backup (no elasticidad), y un ELB reparte trafico pero no evita la sobrecarga si los servicios no pueden absorber el pico.',
  },
  {
    n: 10,
    d: 'RES',
    m: 1,
    q: 'Sales performance data lives in a DynamoDB table, and a web app must display it using managed services with minimal operational maintenance. Which two architectures meet these requirements? (Choose two.)',
    o: [
      'An API Gateway REST API invokes a Lambda function that reads data from the DynamoDB table',
      'A Route 53 hosted zone routes requests to a Lambda function URL that reads data from the DynamoDB table',
      'An ELB forwards requests to a target group with the DynamoDB table configured as the target',
      'An ELB forwards requests to a target group of EC2 instances running an app that reads from DynamoDB',
      'An API Gateway REST API directly accesses the sales performance data in the DynamoDB table',
    ],
    a: [0, 1],
    x: 'API Gateway invocando Lambda, y Route 53 apuntando a una URL de funcion Lambda, son ambas arquitecturas serverless totalmente gestionadas que leen DynamoDB con minimo mantenimiento operativo. DynamoDB no puede ser un target directo de un ELB, usar instancias EC2 exige gestion continua de servidores, y API Gateway no puede acceder directamente a DynamoDB sin una integracion intermedia (como Lambda o integracion de servicio de AWS).',
  },
  {
    n: 11,
    d: 'PERF',
    m: 0,
    q: 'An ASG behind an ALB becomes unacceptably slow when running month-end reports at a specific day and time each month, with CPU hitting 100%. What should be recommended to handle the workload and avoid downtime?',
    o: [
      'Configure a simple scaling policy based on CPU utilization',
      'Configure ElastiCache to remove some workload from the EC2 instances',
      'Configure a CloudFront distribution in front of the ALB',
      'Configure a scheduled scaling policy based on the monthly schedule',
    ],
    a: [3],
    x: 'El scheduled scaling permite anadir capacidad justo antes del momento predecible en que se ejecutan los reportes cada mes, evitando la ralentizacion en vez de reaccionar despues de que ya ocurra. Un simple scaling policy reacciona con retraso una vez la CPU ya esta al 100%, CloudFront ayuda con contenido cacheable global (no con carga de procesamiento de reportes), y ElastiCache no sustituye la capacidad de computo necesaria para generar los reportes.',
  },
  {
    n: 12,
    d: 'RES',
    m: 0,
    q: 'A new reporting tool will access the same RDS MySQL data as a production application, needing high availability without impacting production performance. How can this be achieved?',
    o: [
      'Create a Multi-AZ RDS Read Replica of the production instance',
      'Create a cross-region Multi-AZ deployment and create a read replica in the second region',
      'Use Data Lifecycle Manager to automatically create and manage snapshots',
      'Create a Single-AZ read replica, then a second Single-AZ read replica from that replica',
    ],
    a: [0],
    x: 'Un read replica Multi-AZ ofrece tanto escalado de lecturas para el reporting como resiliencia ante fallos de AZ, sin afectar la carga de la instancia de produccion. Los snapshots no sirven trafico en vivo, un cross-region replica anade latencia/coste innecesarios para un caso local, y encadenar replicas Single-AZ no da alta disponibilidad.',
  },
  {
    n: 13,
    d: 'PERF',
    m: 0,
    q: 'An app requires decoupled components, receiving around 10,000 requests daily with processing taking up to 2 days, and failed requests must be retained. Which solution meets these requirements most efficiently?',
    o: [
      'Use a Kinesis data stream to decouple components and integrate the processing component with the KCL',
      'Create a DynamoDB table with streams enabled, processing requests from the stream',
      'Decouple with an SNS topic, with the receiving component subscribing to the topic',
      'Decouple with an SQS queue, configuring a dead-letter queue to collect requests that failed to process',
    ],
    a: [3],
    x: 'SQS retiene mensajes hasta 14 dias (cubriendo el procesamiento de hasta 2 dias), y una dead-letter queue captura los que fallan repetidamente para su analisis o reprocesado. Kinesis tiene retencion limitada (24h por defecto, hasta 7 dias) insuficiente para 2 dias, DynamoDB Streams no esta pensado para colas de solicitudes, y SNS no ofrece la retencion duradera ni la funcion de dead-letter queue que da SQS.',
  },
  {
    n: 14,
    d: 'PERF',
    m: 0,
    q: 'A containerized app on EKS needs to collect, aggregate, and summarize metrics/logs with a centralized dashboard showing CPU/memory for namespaces, services, and pods. What solution meets this?',
    o: [
      'Migrate the containers to ECS and enable CloudWatch Container Insights',
      'Run the CloudWatch agent in the existing EKS cluster and view metrics/logs in the console',
      'Configure CloudWatch Container Insights in the existing EKS cluster and view metrics/logs in the console',
      'Configure X-Ray to enable tracing for the EKS microservices, querying trace data with Elasticsearch',
    ],
    a: [2],
    x: 'CloudWatch Container Insights soporta EKS de forma nativa, dando dashboards especificos por namespace, servicio y pod para CPU/memoria sin necesitar migrar a ECS. Ejecutar solo el agente de CloudWatch no aporta la misma agregacion y dashboards especializados que Container Insights, y X-Ray esta pensado para trazado de peticiones, no para metricas de utilizacion de recursos por pod/namespace.',
  },
  {
    n: 15,
    d: 'SEC',
    m: 0,
    q: 'A company has developer, test, and pre-production AWS accounts and has received large bills for underutilized large EC2 instances. How can the ability to launch large instances be restricted in all accounts with the LEAST operational overhead?',
    o: [
      'Create an Organizations org with all accounts and an SCP denying the launch of large EC2 instances',
      'Create a service-linked role for EC2 with a policy that denies large instance launches',
      'Create an IAM role in each account denying large instance launches, granting the developer group access',
      'Create a resource-based policy denying large instance launches and attach it to EC2 in each account',
    ],
    a: [0],
    x: 'Una SCP aplicada desde AWS Organizations restringe centralmente el permiso maximo disponible en todas las cuentas de una sola vez, incluso para usuarios con permisos de administrador dentro de cada cuenta. No se pueden crear roles vinculados a servicio de forma manual, EC2 no admite politicas basadas en recursos directamente, y crear un rol por cuenta es mucho menos eficiente que una unica SCP centralizada.',
  },
  {
    n: 16,
    d: 'RES',
    m: 0,
    q: 'An Architect needs to automatically and repeatably create many member accounts within an AWS Organization, moved into an OU, with VPCs and subnets created. What is the best way to achieve this?',
    o: [
      'Use the AWS Organizations API',
      'Use the AWS Management Console',
      'Use the AWS CLI',
      'Use CloudFormation with scripts',
    ],
    a: [3],
    x: 'CloudFormation, combinado con scripts de automatizacion (por ejemplo integrandose con la API de Organizations), permite definir la creacion de cuentas, su ubicacion en OUs y el aprovisionamiento de VPCs/subnets como codigo repetible y consistente. La API de Organizations por si sola no aprovisiona infraestructura de red, la consola es manual y no escalable, y la CLI sola exige mucho mas scripting propio sin la consistencia que da CloudFormation.',
  },
  {
    n: 17,
    d: 'SEC',
    m: 0,
    q: 'A financial application requires immutable storage out of the box with encryption at rest and ACID properties for the storage layer, plus a containerized compute layer. Which solution meets this with the LEAST operational overhead?',
    o: [
      'ECS behind an ALB on Fargate, using Amazon QLDB to manage the storage layer',
      'An Auto Scaling group of EC2 instances behind an ALB, using S3 for storage',
      'ECS on EC2 behind an ALB in an ASG, storing data using DynamoDB',
      'A cluster of ECS instances on Fargate within an ASG behind an ALB, using S3 for storage',
    ],
    a: [0],
    x: 'Amazon QLDB tiene un journal inmutable por diseno (append-only) con propiedades ACID, cumpliendo el requisito de almacenamiento inmutable de forma nativa, y ECS en Fargate es la opcion de contenedores serverless con menor overhead operativo. S3 no es inmutable por defecto ni ofrece propiedades ACID transaccionales, y ejecutar ECS sobre EC2 exige gestionar instancias, mas overhead que Fargate.',
  },
  {
    n: 18,
    d: 'PERF',
    m: 0,
    q: 'An app deployed across multiple Regions exposes static public IPs, and some users experience poor performance over the internet. What should be recommended to reduce internet latency?',
    o: [
      'Set up a CloudFront distribution to access the application',
      'Set up Direct Connect locations in multiple Regions',
      'Set up a Route 53 geoproximity routing policy to route traffic',
      'Set up AWS Global Accelerator and add endpoints',
    ],
    a: [3],
    x: 'Global Accelerator ofrece direcciones IP estaticas y dirige el trafico a traves de la red global de AWS hacia el endpoint optimo, reduciendo la latencia de internet para usuarios globales. CloudFront no puede exponer IPs publicas estaticas, Direct Connect es solo para conectividad on-premises, y el geoproximity routing de Route 53 mejora el enrutamiento DNS pero no reduce la latencia de red de la misma forma que la red global de AWS usada por Global Accelerator.',
  },
  {
    n: 19,
    d: 'PERF',
    m: 0,
    q: 'A company needs to migrate a large quantity of data from on-premises to S3 over an existing Direct Connect connection, wanting a fully managed service that keeps data private and accelerates replication. Which solution should be recommended?',
    o: [
      'Deploy a DataSync agent on-premises, configuring a task connected to a VPC endpoint',
      'Deploy a DataSync agent on-premises, configuring a task connected to a public endpoint',
      'Deploy a Storage Gateway volume gateway in stored volume mode, taking point-in-time copies with AWS Backup',
      'Deploy a Storage Gateway file gateway with a local cache, storing the primary data set in S3',
    ],
    a: [0],
    x: 'DataSync automatiza y acelera la replicacion hacia S3, y conectarlo a un VPC endpoint mantiene el trafico privado sobre la conexion Direct Connect en vez de salir a internet. Un endpoint publico enviaria los datos por internet (contra el requisito de privacidad), y Storage Gateway esta pensado para acceso hibrido continuo, no para acelerar una migracion masiva puntual.',
  },
  {
    n: 20,
    d: 'SEC',
    m: 0,
    q: 'Security compliance requires that all personally identifiable information be encrypted at rest on EC2/EBS-backed instances and an RDS database. Which solution should be chosen?',
    o: [
      'Configure SSL/TLS encryption using KMS CMKs to encrypt database volumes',
      'Deploy CloudHSM, generate keys, and use the CMK to encrypt database volumes',
      'Configure EBS encryption and RDS encryption with KMS keys to encrypt instance and database volumes',
      'Enable encryption on RDS during creation and use Macie to identify sensitive data',
    ],
    a: [2],
    x: 'Cifrar tanto los volumenes EBS como la instancia RDS con claves KMS cubre el requisito de cifrado en reposo tanto para el almacenamiento de las instancias EC2 como para la base de datos. SSL/TLS solo cifra datos en transito (no en reposo), CloudHSM no es necesario para este caso, y Macie identifica datos sensibles en S3 (no cifra RDS ni ayuda con este requisito).',
  },
  {
    n: 21,
    d: 'SEC',
    m: 0,
    q: 'Access to financial reports in S3 must be logged, and any modifications to the log files must be detected. What actions should be taken?',
    o: [
      'Use S3 server access logging with read/write management events and log file validation enabled',
      'Use CloudTrail to log read/write management events on the bucket, storing logs in a new bucket with log file validation enabled',
      'Use CloudTrail to log read/write data events on the bucket, storing logs in a new bucket with log file validation enabled',
      'Use S3 server access logging with read/write data events and log file validation enabled',
    ],
    a: [2],
    x: 'Los data events de CloudTrail capturan actividad a nivel de objeto (lectura/escritura de los reportes), y la validacion de integridad de logs de CloudTrail detecta cualquier modificacion posterior a los archivos de log. Los management events no cubren el acceso a objetos individuales, y los server access logs de S3 no ofrecen validacion de integridad de logs ni el mismo nivel de detalle que CloudTrail data events.',
  },
  {
    n: 22,
    d: 'RES',
    m: 0,
    q: 'A Production VPC connects via VPN to a single on-premises device, and a Pre-Production VPC connects via two Direct Connect connections through a virtual private gateway. Both VPCs are linked by a single VPC peering connection. How can the single point of failure be removed?',
    o: [
      'Add a second virtual private gateway attached to the Production VPC',
      'Add additional VPNs to the Production VPC from a second customer gateway device',
      'Add an additional VPC peering connection between the two VPCs',
      'Add a set of VPNs between the Production and Pre-Production VPCs',
    ],
    a: [1],
    x: 'El unico punto de fallo real es el dispositivo customer gateway on-premises unico; anadir VPNs adicionales desde un segundo dispositivo customer gateway elimina esa dependencia de un solo equipo fisico. El virtual private gateway del lado de AWS ya es redundante por diseno, el peering ya es redundante sin necesitar duplicarlo, y no se pueden crear VPNs de AWS directamente entre dos VPCs.',
  },
  {
    n: 23,
    d: 'COST',
    m: 0,
    q: 'A new application will launch on EC2 with an EBS volume; usage is infrequent with peaks a couple of hours morning and evening, with variable disk I/O peaking at 3,000 IOPS. Which solution should be recommended?',
    o: [
      'EBS Cold HDD (sc1)',
      'EBS Throughput Optimized HDD (st1)',
      'EBS General Purpose SSD (gp2)',
      'EBS Provisioned IOPS SSD (io1)',
    ],
    a: [2],
    x: 'General Purpose SSD (gp2) permite hacer burst hasta 3,000 IOPS durante periodos prolongados a un coste mucho menor que Provisioned IOPS, encajando con un patron de uso variable e infrecuente. Provisioned IOPS SSD es mas caro de lo necesario para este perfil, y las opciones HDD (sc1/st1) no ofrecen la baja latencia ni el burst de IOPS que necesita esta carga.',
  },
  {
    n: 24,
    d: 'PERF',
    m: 0,
    q: 'Statistical data in S3 must be downloaded worldwide with low latency, using Route 53 for DNS hosting, with no database involved. Which solution meets these requirements?',
    o: [
      'A CloudFront web distribution with an S3 origin, and an ALIAS record in Route 53 pointing to it',
      'A CloudFront web distribution with an S3 origin, and a CNAME record in Route 53 pointing to it',
      'An A record in Route 53 with a geolocation traffic policy and health checks',
      'An A record in Route 53 with a geoproximity traffic policy and health checks',
    ],
    a: [0],
    x: 'Una distribucion de CloudFront con S3 como origen cachea el contenido globalmente con baja latencia, y un ALIAS record en Route 53 es la forma recomendada de apuntar el dominio de la aplicacion hacia la distribucion. Un CNAME funciona de forma similar pero no es la practica recomendada para apuntar a recursos de AWS como CloudFront, y las politicas de geolocation/geoproximity no tienen sentido cuando solo hay un unico origen (el bucket S3).',
  },
  {
    n: 25,
    d: 'PERF',
    m: 0,
    q: 'An API in RDS must allow sporadic access, ranging from no requests to large bursts of traffic. Which solution should be suggested?',
    o: [
      'Set up API Gateway and use Lambda functions',
      'Set up API Gateway and use ECS',
      'Set up API Gateway and use Elastic Beanstalk',
      'Set up API Gateway and use EC2 with Auto Scaling',
    ],
    a: [0],
    x: 'Lambda escala automaticamente a cero cuando no hay peticiones y responde de inmediato ante picos, cobrando solo por invocacion, encajando perfectamente con un patron esporadico e impredecible. ECS y Elastic Beanstalk requieren capacidad reservada o gestion de escalado mas lenta, y EC2 con Auto Scaling tarda en arrancar instancias frente a la escala instantanea de Lambda.',
  },
  {
    n: 26,
    d: 'SEC',
    m: 1,
    q: 'An API deployed in a VPC behind an internal NLB must be consumed by a client app in private subnets of a second account, without using the public internet. Which two configurations achieve this? (Choose two.)',
    o: [
      'Configure an AWS RAM connection between the two accounts, accessing the API using its private address',
      'Configure a VPC peering connection between the two VPCs, accessing the API using its private address',
      'Configure a Direct Connect connection between the two VPCs, accessing the API using its private address',
      'Configure a PrivateLink connection for the API into the client VPC, accessing the API using the PrivateLink address',
      'Configure a ClassicLink connection for the API into the client VPC, accessing the API using the ClassicLink address',
    ],
    a: [1, 3],
    x: 'El VPC peering conecta las dos VPCs con direcciones privadas de forma directa, y PrivateLink permite acceder al servicio a traves de un endpoint de interfaz sin exponerlo publicamente, ambas opciones evitando el internet publico. Direct Connect conecta on-premises con AWS (no dos VPCs entre si), RAM comparte recursos (no da acceso directo a una API), y ClassicLink es una funcion obsoleta para instancias EC2-Classic, sin relacion con este escenario.',
  },
  {
    n: 27,
    d: 'SEC',
    m: 0,
    q: 'Documents will be encrypted at rest in S3, without spending time managing/rotating the keys, but wanting control over who can access those keys. What should be used?',
    o: [
      'Server-Side Encryption with Customer-Provided Keys (SSE-C)',
      'Server-Side Encryption with AWS KMS-Managed Keys (SSE-KMS)',
      'Server-Side Encryption with keys stored in an S3 bucket',
      'Server-Side Encryption with Amazon S3-Managed Keys (SSE-S3)',
    ],
    a: [1],
    x: 'SSE-KMS con una CMK gestionada por el cliente deja que KMS maneje la clave de datos (sin gestion manual) mientras el cliente controla el acceso via politicas de la clave. SSE-C exige que el cliente aporte y gestione la clave en cada peticion (contra el requisito de no gestionarla), SSE-S3 no permite controlar el acceso a la clave subyacente, y no existe una funcion de guardar claves directamente dentro de un bucket S3.',
  },
  {
    n: 28,
    d: 'PERF',
    m: 0,
    q: 'A web app suffers from insufficient swap space on its EC2 instances. What should be recommended to monitor this?',
    o: [
      'Enable detailed monitoring and create a CloudWatch SwapUtilization custom metric',
      'Configure a CloudWatch SwapUsage metric dimension for the EC2 metrics',
      'Use EC2 metadata to collect information and publish it to CloudWatch custom metrics',
      'Install a CloudWatch agent on the instances and run a script on a set schedule',
    ],
    a: [3],
    x: 'Por defecto CloudWatch no expone metricas de swap; hay que instalar el agente unificado de CloudWatch en las instancias, que recopila metricas del sistema operativo (incluido swap) y las publica de forma nativa. La monitorizacion detallada solo aumenta la frecuencia de metricas por defecto (no anade swap), no existe una dimension SwapUsage nativa en EC2, y la metadata de instancia no expone datos de swap.',
  },
  {
    n: 29,
    d: 'RES',
    m: 0,
    q: 'A large quantity of data must be migrated to a versioning-enabled S3 bucket in us-west-1, replicated to another Region for disaster recovery. How should the replication be configured?',
    o: [
      'Create an additional bucket in another Region and configure CORS',
      'Create an additional bucket in another Region and configure cross-Region replication',
      'Create an additional bucket with versioning in another Region and configure CORS',
      'Create an additional bucket with versioning in another Region and configure cross-Region replication',
    ],
    a: [3],
    x: 'La replicacion cross-region de S3 exige que tanto el bucket origen como el destino tengan el versionado habilitado, asi que hay que crear el bucket destino con versionado activado y configurar la replicacion entre ambos. CORS no tiene relacion con la replicacion, y sin versionado en el bucket destino la replicacion no funciona.',
  },
  {
    n: 30,
    d: 'PERF',
    m: 0,
    q: '250TB of survey data copied onto multiple Snowball Edge Storage Optimized devices needs to be sent back to AWS, requiring consistent low-latency, high-throughput access for an HPC cluster. What is the solution?',
    o: [
      'Create an FSx for Lustre file system and import the data directly, accessing it from the HPC cluster instances',
      'Set up an EFS file system and an S3 bucket, uploading data to S3 and copying it into EFS',
      'Create a bucket in S3, import the data, and set up a Storage Gateway file gateway to access the bucket',
      'Set up an S3 bucket, configure an FSx for Lustre file system integrated with the bucket after importing, then access it',
    ],
    a: [3],
    x: 'Snowball solo puede entregar datos a un bucket de S3; una vez alli, FSx for Lustre se puede vincular a ese bucket via data repository association, presentando los objetos como archivos con el rendimiento de baja latencia y alto throughput requerido por el cluster HPC. No se puede importar directamente de Snowball a FSx for Lustre sin pasar por S3 primero, EFS no alcanza el rendimiento de Lustre para HPC, y File Gateway esta pensado para acceso hibrido continuo, no para rendimiento HPC de baja latencia.',
  },
  {
    n: 31,
    d: 'RES',
    m: 0,
    q: 'An on-premises SMB file server has files accessed frequently for the first few days, rarely after 30 days, with capacity running out. The solution must increase storage without sacrificing access to recent files, plus lifecycle management. What should be recommended?',
    o: [
      'A custom program on each user\u2019s computer granting S3 access, with a lifecycle policy transitioning data to S3 Glacier Flexible Retrieval after 30 days',
      'Use DataSync to take data older than 30 days from the SMB file server to AWS',
      'Create an FSx for Windows File Server file system to extend the file space',
      'Create an S3 File Gateway extending the storage space into the cloud, with a lifecycle policy transitioning data to S3 Glacier Deep Archive after 30 days',
    ],
    a: [3],
    x: 'Un S3 File Gateway se integra con el servidor SMB existente, dando acceso local a los archivos recientes mientras los datos se guardan de forma duradera en S3, y una lifecycle policy archiva automaticamente los archivos con mas de 30 dias a Glacier Deep Archive, la clase mas barata para retencion a largo plazo. Instalar un programa custom en cada equipo anade complejidad innecesaria, DataSync sirve para transferencias puntuales (no gestion continua del ciclo de vida), y FSx for Windows resuelve la capacidad pero no incluye politicas de ciclo de vida automaticas hacia clases de menor coste.',
  },
  {
    n: 32,
    d: 'SEC',
    m: 0,
    q: 'RDS MySQL automated daily backups are unencrypted, and the requirement is to enable encryption for future backups, destroying unencrypted ones after the first encrypted backup completes. What should be done to enable encryption for future backups?',
    o: [
      'Modify the backup section of the database configuration to toggle the "Enable encryption" checkbox',
      'Create a snapshot of the database, copy it to an encrypted snapshot, and restore the database from the encrypted snapshot',
      'Enable an encrypted read replica, then promote it to primary, removing the original instance',
      'Enable default encryption for the S3 bucket where backups are stored',
    ],
    a: [1],
    x: 'RDS no permite activar el cifrado directamente sobre una instancia existente; hay que tomar un snapshot, copiarlo cifrandolo, y restaurar una nueva instancia a partir de ese snapshot cifrado para que todos sus backups futuros esten cifrados. No existe un checkbox para activar cifrado en una instancia ya existente, y los backups de RDS no se gestionan como objetos directos en un bucket S3 propio del usuario.',
  },
  {
    n: 33,
    d: 'RES',
    m: 1,
    q: 'A Lambda function ingesting data via SNS occasionally fails to ingest data due to network connectivity issues, requiring manual re-runs. Which two actions ensure the data is ingested even during an outage? (Choose two.)',
    o: [
      'Modify the Lambda function so it reads from an SQS queue',
      'Set up an SQS queue and subscribe it to the SNS topic',
      'Deploy the Lambda function across multiple Availability Zones within the Region',
      'Attach the Lambda function to a VPC and deploy a NAT gateway, enabling multi-AZ for the VPC',
      'Increase throughput for the Lambda function and increase the provisioned CPU and memory',
    ],
    a: [0, 1],
    x: 'Suscribir una cola SQS al topic de SNS retiene los mensajes de forma duradera, y modificar Lambda para leer de esa cola en vez de procesar directamente el evento de SNS asegura que ningun mensaje se pierda si falla la primera invocacion. Lambda ya se ejecuta de forma redundante entre AZs automaticamente, no existe "habilitar multi-AZ para una VPC" como funcion, y aumentar throughput/CPU no soluciona la perdida de datos ante un fallo de red puntual.',
  },
  {
    n: 34,
    d: 'PERF',
    m: 0,
    q: 'An app runs across multiple AZs behind an ASG and ALB, performing best when CPU utilization stays around 40%. What should be recommended to maintain the desired performance across all instances?',
    o: [
      'Use a target tracking policy to dynamically scale the ASG',
      'Use a Lambda function to update the desired ASG capacity',
      'Use scheduled scaling actions to scale up and down the ASG',
      'Use a simple scaling policy to dynamically scale the ASG',
    ],
    a: [0],
    x: 'Una politica de target tracking ajusta automaticamente la capacidad del ASG para mantener la metrica (CPU) cerca del valor objetivo del 40%, reaccionando de forma continua a los cambios de carga. Un Lambda personalizado anade complejidad de desarrollo evitable, el escalado programado no se adapta a fluctuaciones reales, y el simple scaling reacciona con retrasos frente al ajuste continuo de target tracking.',
  },
  {
    n: 35,
    d: 'COST',
    m: 0,
    q: '200TB of video files must be moved from on-premises to AWS within four weeks, with around 50 Mbps of available bandwidth. What is the most cost-effective solution?',
    o: [
      'Use S3 Transfer Acceleration to securely upload the data',
      'Create a virtual private gateway and connect a VPN to upload the data',
      'Order multiple Snowball devices to migrate the data to AWS',
      'Use Snowmobile to migrate the data to AWS',
    ],
    a: [2],
    x: 'Con solo 50 Mbps de ancho de banda, transferir 200TB por internet tardaria meses, asi que Snowball es la opcion practica y coste-efectiva para migraciones de decenas o cientos de TB dentro del plazo de cuatro semanas. Transfer Acceleration y VPN siguen limitados por el ancho de banda local disponible, y Snowmobile esta pensado para escala de exabytes, excesivo para 200TB.',
  },
  {
    n: 36,
    d: 'RES',
    m: 0,
    q: 'A Direct Connect connection needs a low-cost, short-term backup option. What is the most cost-effective solution?',
    o: [
      'Configure Transit Gateway with an IPSec VPN backup',
      'Implement a second AWS Direct Connect connection',
      'Configure an IPSec VPN connection over the Direct Connect link',
      'Implement an IPSec VPN connection advertised via BGP alongside Direct Connect',
    ],
    a: [3],
    x: 'Una VPN IPSec activa simultaneamente con Direct Connect, anunciada por BGP, sirve de respaldo de bajo coste: el trafico prefiere siempre Direct Connect y solo pasa por la VPN si este falla, sin costes recurrentes de infraestructura adicional. Una segunda conexion Direct Connect es cara y lenta de aprovisionar, un Transit Gateway anade coste innecesario para un respaldo temporal, y una VPN sobre el propio enlace Direct Connect no anade redundancia real ante un fallo del enlace en si.',
  },
  {
    n: 37,
    d: 'SEC',
    m: 0,
    q: 'A systems administrator wants to detect and remediate the compromise of services such as EC2 instances and S3 buckets. Which AWS service can be used?',
    o: ['Amazon Macie', 'Amazon GuardDuty', 'Amazon Cognito', 'Amazon Inspector'],
    a: [1],
    x: 'GuardDuty analiza CloudTrail, VPC Flow Logs y DNS logs para detectar amenazas continuas como reconocimiento, compromiso de instancias, de cuentas y de buckets, con la capacidad de disparar respuestas automatizadas. Macie detecta datos sensibles (no compromiso de recursos), Inspector busca vulnerabilidades (no compromiso activo), y Cognito es autenticacion de usuarios, sin relacion con deteccion de amenazas.',
  },
  {
    n: 38,
    d: 'COST',
    m: 0,
    q: 'A website is hosted for a development team, consisting of HTML, CSS, client-side JavaScript, and images. Which solution is most cost-effective?',
    o: [
      'Launch an EC2 instance and host the website there',
      'Create an S3 bucket and host the website there',
      'Use a Docker container to host the website on Fargate',
      'Create an ALB with a Lambda target',
    ],
    a: [1],
    x: 'Al ser contenido puramente estatico (sin logica de servidor), S3 como sitio web estatico es la opcion mas economica, ya que no requiere servidores ni computo en ejecucion. EC2 y Fargate implican coste de computo innecesario, y un ALB con target Lambda anade complejidad y coste sin necesidad para servir simplemente archivos estaticos.',
  },
  {
    n: 39,
    d: 'PERF',
    m: 0,
    q: 'A company wants a centralized data repository for Machine Learning purposes, including data from RDS MySQL. Which service can be used to build this?',
    o: ['Amazon S3', 'Amazon Quantum Ledger Database (QLDB)', 'AWS Lake Formation', 'Amazon Neptune'],
    a: [2],
    x: 'AWS Lake Formation permite importar datos desde bases relacionales como MySQL en RDS (con carga masiva e incremental) hacia un data lake centralizado, curado y seguro, pensado para ML. S3 por si solo no orquesta la ingesta desde RDS, QLDB es un ledger inmutable (no un data lake), y Neptune es una base de grafos, sin relacion con este caso de uso.',
  },
  {
    n: 40,
    d: 'RES',
    m: 0,
    q: 'A DR solution has a second copy of an app behind a second ALB in another Region, running in warm standby mode, with failover currently manual (updating a DNS alias record). How can the failover process be automated?',
    o: [
      'Create a CNAME record on Route 53 pointing to the ALB endpoint',
      'Create a latency-based routing policy on Route 53',
      'Enable a Route 53 health check',
      'Enable an ALB health check',
    ],
    a: [2],
    x: 'Un health check de Route 53 puede monitorizar el ALB primario y, si falla, actualizar automaticamente la resolucion DNS hacia el ALB secundario (por ejemplo con failover routing), automatizando por completo el proceso. Un CNAME estatico solo apunta a un unico ALB sin capacidad de failover, el enrutamiento por latencia no garantiza el failover ante un fallo, y un health check de ALB solo monitoriza las instancias detras de el (no puede disparar cambios de DNS entre regiones).',
  },
  {
    n: 41,
    d: 'RES',
    m: 0,
    q: 'A retail organization needs a DR strategy with regions kept up to date with the lowest latency, running at reduced capacity but able to handle traffic immediately. Which solution meets this with the LOWEST RTO?',
    o: [
      'Aurora global database with a warm standby DR strategy',
      'RDS Multi-AZ DB instance with a warm standby DR strategy',
      'RDS Multi-AZ DB instance with a pilot light DR strategy',
      'Aurora global database with a pilot light DR strategy',
    ],
    a: [0],
    x: 'Aurora Global Database mantiene la base de datos actualizada entre regiones con muy baja latencia, y con warm standby la aplicacion puede atender trafico de inmediato (aunque a capacidad reducida), dando el menor RTO posible. Con pilot light hace falta intervencion y tiempo para que la aplicacion acepte trafico, y RDS Multi-AZ no esta pensado para escenarios de DR multi-region.',
  },
  {
    n: 42,
    d: 'COST',
    m: 0,
    q: 'A company wants to reduce S3 spend as quickly as possible, given buckets with items that have different access patterns. What is the quickest way to reduce S3 spend with the LEAST operational overhead?',
    o: [
      'Create a Lambda function to scan buckets, check which objects are in the appropriate buckets, and move them there',
      'Place all objects in S3 Glacier Instant Retrieval',
      'Automate the move of objects to the best storage class with Trusted Advisor',
      'Transition objects to the appropriate storage class using an S3 Lifecycle configuration',
    ],
    a: [3],
    x: 'Una S3 Lifecycle configuration mueve automaticamente los objetos a la clase de almacenamiento adecuada segun su antiguedad, sin necesitar desarrollo propio ni intervencion manual. Trusted Advisor no transiciona objetos automaticamente (solo revisa recomendaciones de coste), un Lambda propio anade desarrollo innecesario cuando existe la funcion nativa, y poner todo en Glacier Instant Retrieval ignora que distintos items tienen distintos patrones de acceso.',
  },
  {
    n: 43,
    d: 'RES',
    m: 0,
    q: 'A DynamoDB item must be modified with a record retained for compliance every time it changes. What is the most efficient solution to record this information?',
    o: [
      'Enable DynamoDB Streams, with a Lambda function polling the stream and recording modified item data to S3',
      'Enable DynamoDB Global Tables, with streams saving output directly to S3',
      'Enable CloudWatch Logs, with a Lambda function monitoring the logs and recording deleted item data to S3',
      'Enable CloudTrail, with an EC2 instance monitoring the log files and recording changed items in another DynamoDB table',
    ],
    a: [0],
    x: 'DynamoDB Streams captura cambios a nivel de item en tiempo real, y una funcion Lambda que consume el stream puede registrar cada modificacion en S3 de forma escalable y serverless. Global Tables resuelve replicacion multi-region (no auditoria de cambios), CloudWatch Logs no captura cambios de items de DynamoDB, y CloudTrail registra llamadas a la API, no el contenido de los cambios de datos en si.',
  },
  {
    n: 44,
    d: 'PERF',
    m: 0,
    q: 'EC2 instances fail due to insufficient swap space, and monitoring is required for Linux instances. What should be recommended?',
    o: [
      'Enable detailed monitoring and create a CloudWatch SwapUtilization custom metric',
      'Create a custom metric in CloudWatch that monitors Swap Usage',
      'Install and configure the unified CloudWatch agent on the EC2 instances',
      'Use EC2 metadata to collect information and publish it to CloudWatch custom metrics',
    ],
    a: [2],
    x: 'El agente unificado de CloudWatch recopila metricas a nivel de sistema operativo como el uso de swap y las publica de forma nativa en CloudWatch, siendo la practica recomendada por AWS. La monitorizacion detallada no anade metricas de swap, y publicar metricas personalizadas manualmente exige desarrollo evitable cuando el agente ya soporta swap de forma directa.',
  },
  {
    n: 45,
    d: 'SEC',
    m: 0,
    q: 'Support staff need to securely reach EC2 Linux instances in private subnets without opening inbound SSH ports or managing key pairs, with all access logged for auditing. What should be recommended?',
    o: [
      'Deploy a bastion host in a public subnet with a broad security group rule for SSH',
      'Distribute individual SSH key pairs to each support engineer',
      'Use AWS Systems Manager Session Manager, granting IAM permissions to start sessions on the instances',
      'Open port 22 to 0.0.0.0/0 on the instances\u2019 security group and rely on OS-level authentication',
    ],
    a: [2],
    x: 'Session Manager permite conectarse a instancias EC2 sin abrir el puerto 22 ni distribuir pares de claves, gestionando el acceso via permisos IAM y registrando cada sesion para auditoria. Un bastion host anade infraestructura que mantener, distribuir claves SSH complica la rotacion y revocacion de acceso, y abrir el puerto 22 a cualquier IP expone las instancias innecesariamente.',
  },
  {
    n: 46,
    d: 'PERF',
    m: 0,
    q: 'A computer scientist building a weather prediction app using telemetry data wants managed services and access to third-party datasets. Which combination of services delivers the best solution?',
    o: [
      'A TensorFlow AMI from the Marketplace for ML, and DataSync to access third-party telemetry data',
      'SageMaker to build the ML part, and DataSync to access third-party telemetry data',
      'A TensorFlow AMI from the Marketplace for ML, and AWS Data Exchange to access third-party telemetry data',
      'SageMaker to build the ML part, and AWS Data Exchange to access third-party telemetry data',
    ],
    a: [3],
    x: 'SageMaker es un servicio totalmente gestionado para construir, entrenar y desplegar modelos de ML, y AWS Data Exchange da acceso a conjuntos de datos de terceros de multiples industrias, cubriendo ambos requisitos con servicios gestionados. Usar una AMI de TensorFlow implica gestionar una instancia EC2 propia (no es un servicio gestionado), y DataSync mueve datos entre almacenamiento propio, no da acceso a datasets de terceros.',
  },
  {
    n: 47,
    d: 'PERF',
    m: 0,
    q: 'Fortnightly data analyst reports against an Aurora database take a long time and degrade performance for the online retail application. ReadIOPS and CPUUtilization spike when reports run. What is the MOST cost-effective solution?',
    o: [
      'Migrate the fortnightly reporting to EMR',
      'Migrate the fortnightly reporting to an Aurora Replica',
      'Increase the Provisioned IOPS on the Aurora instance',
      'Migrate the Aurora database to a larger instance class',
    ],
    a: [1],
    x: 'Dirigir los reportes al endpoint de lectura de un Aurora Replica descarga esa carga de la instancia principal, siendo la solucion mas coste-efectiva sin cambiar de plataforma ni ampliar innecesariamente el tamano de la instancia. EMR es innecesario para simplemente descargar consultas de reporting, aumentar Provisioned IOPS mejora el storage pero no la CPU, y ampliar el tamano de instancia es mas caro sin resolver el problema de fondo de raiz (contencion entre carga transaccional y de reporting).',
  },
  {
    n: 48,
    d: 'SEC',
    m: 0,
    q: 'A Lambda function\u2019s execution role IAM policy must allow put, update, and delete on a DynamoDB table named "Orders" with no other actions allowed, giving the LEAST privileged access. Which policy statement should be included?',
    o: [
      'Allow dynamodb:PutItem, UpdateItem, DeleteItem on Resource "arn:aws:dynamodb:us-east-1:227392126428:table/*"',
      'Allow dynamodb:PutItem, UpdateItem, DeleteItem on Resource "arn:aws:dynamodb:us-east-1:227392126428:table/Orders"',
      'Allow dynamodb:* on Resource "arn:aws:dynamodb:us-east-1:227392126428:table/Orders"',
      'Deny dynamodb:* on Resource "arn:aws:dynamodb:us-east-1:227392126428:table/Orders"',
    ],
    a: [1],
    x: 'Especificar exactamente las tres acciones necesarias (PutItem, UpdateItem, DeleteItem) y el ARN concreto de la tabla Orders cumple el principio de minimo privilegio de forma precisa. Usar un comodin "*" en el recurso abarcaria todas las tablas de la cuenta, y usar "dynamodb:*" como accion concede todas las operaciones del servicio, ambas demasiado amplias; una regla deny tampoco concede el acceso requerido.',
  },
  {
    n: 49,
    d: 'SEC',
    m: 0,
    q: 'A web application behind CloudFront and an ALB is receiving a flood of malicious requests from a small set of IP addresses, and the team wants to block them at the edge before they reach the origin. What should be recommended?',
    o: [
      'Add a deny rule for those IPs in the security group attached to the ALB',
      'Attach AWS WAF to the CloudFront distribution with an IP set match rule blocking those addresses',
      'Add a deny rule for those IPs in the network ACL of the ALB\u2019s subnet',
      'Contact AWS Support to block the IPs at the network level',
    ],
    a: [1],
    x: 'AWS WAF asociado a la distribucion de CloudFront puede bloquear peticiones de IPs especificas en el edge, antes de que lleguen siquiera al ALB o al origen. Los security groups no admiten reglas deny, las network ACL del ALB no cubren el trafico que ya paso por CloudFront, y AWS Support no gestiona el filtrado de seguridad de la aplicacion del cliente.',
  },
  {
    n: 50,
    d: 'PERF',
    m: 0,
    q: 'A financial modelling workload needs a POSIX-compliant, high-performance shared file system on Linux, with data seamlessly integrated with an existing S3 bucket. Which storage solution fits?',
    o: [
      'Amazon FSx for Lustre',
      'Amazon FSx for Windows File Server',
      'Amazon Elastic Block Store',
      'Amazon S3 Standard-IA',
    ],
    a: [0],
    x: 'FSx for Lustre ofrece un sistema de archivos POSIX de alto rendimiento e integra de forma nativa con S3, presentando los objetos como archivos para cargas exigentes como el modelado financiero. FSx for Windows esta pensado para SMB/Windows (no Linux/POSIX), EBS es almacenamiento de bloque para una sola instancia, y S3 Standard-IA no es un sistema de archivos montable.',
  },
  {
    n: 51,
    d: 'SEC',
    m: 0,
    q: 'S3 buckets are used by different applications, each with a designated prefix, and applications must be limited to their own prefix with detailed access control. Which solution offers the LEAST administrative burden?',
    o: [
      'Set up specific S3 access points and policies for each application',
      'Use an S3 Batch Operations task to configure ACL permissions for every object in the bucket',
      'Duplicate the bucket\u2019s objects into separate buckets per application using prefix-based replication',
      'Duplicate the bucket\u2019s objects into distinct buckets per application and set up specific access points for each',
    ],
    a: [0],
    x: 'Los S3 access points permiten definir politicas de acceso propias por prefijo dentro del mismo bucket, sin duplicar datos ni gestionar ACLs objeto por objeto. Configurar ACLs para cada objeto es muy laborioso a escala, y duplicar los datos en buckets separados aumenta el coste de almacenamiento y la complejidad de mantenimiento innecesariamente.',
  },
  {
    n: 52,
    d: 'COST',
    m: 0,
    q: 'A 15-person team logs hire dates in DynamoDB and wants to email employees on their work anniversary. Which method ensures maximum operational efficiency?',
    o: [
      'A script examining the DynamoDB table using SNS to email staff, run via a cron job on EC2 daily',
      'A script examining the DynamoDB table using SQS to email staff, run via a cron job on EC2 daily',
      'A Lambda function that checks the table and uses SNS to dispatch emails, scheduled to execute daily',
      'A Lambda function that checks the table and uses SQS to dispatch emails, scheduled to execute daily',
    ],
    a: [2],
    x: 'Una funcion Lambda programada a diario (via EventBridge) que consulta la tabla y usa SNS para enviar los correos es totalmente serverless, sin instancias que mantener, y SNS esta pensado precisamente para el envio de notificaciones. Usar EC2 con cron anade gestion de servidor innecesaria, y SQS no envia correos directamente (es una cola, no un servicio de notificacion).',
  },
  {
    n: 53,
    d: 'PERF',
    m: 0,
    q: 'A content management system on EC2 behind an ALB, in an ASG across multiple AZs, needs all EC2 instances to access the latest content (files, articles, resources) with minimal delay. What solution fulfills these requirements?',
    o: [
      'Adjust ASG lifecycle policies to update EC2 user data so new instances copy website resources, routing updates only to the newest instance via the ALB',
      'Move the website resources to EFS, mounted locally by every EC2 instance, and update the app to use the EFS-hosted resources',
      'Place the resources in S3, having each instance sync them to its own EBS volume hourly via an S3 sync command',
      'Use an EBS snapshot of the resources, attaching it as an additional volume when a new instance launches',
    ],
    a: [1],
    x: 'EFS es un sistema de archivos compartido que todas las instancias en varias AZs pueden montar simultaneamente, dando acceso consistente y en tiempo real al mismo contenido actualizado. Dirigir actualizaciones solo a la instancia mas reciente deja desactualizadas al resto, sincronizar por hora introduce retraso, y un snapshot de EBS solo captura un momento puntual, sin sincronizacion continua entre instancias.',
  },
  {
    n: 54,
    d: 'SEC',
    m: 0,
    q: 'A set of EC2 instances will interact with an Aurora DB cluster, needing to authenticate securely without managing static credentials, with the least maintenance required. What is the best method?',
    o: [
      'Create a database user with a username and password, embedding these in the CloudFormation template',
      'Create a database user with a username and password, stored in Systems Manager Parameter Store',
      'Configure the DB cluster to use IAM database authentication, creating a database user for it, and assign a role to the EC2 instances',
      'Configure the DB cluster to use IAM database authentication with an IAM user, linking that IAM user directly to the EC2 instances',
    ],
    a: [2],
    x: 'La autenticacion de base de datos con IAM permite que las instancias EC2 se autentiquen usando credenciales temporales via un rol IAM, sin gestionar ni rotar contrasenas estaticas. Embeber credenciales en CloudFormation o guardarlas en Parameter Store sigue exigiendo gestion manual de rotacion, y vincular un usuario IAM individual directamente a las instancias no es el patron recomendado (se usan roles, no usuarios, para EC2).',
  },
];
