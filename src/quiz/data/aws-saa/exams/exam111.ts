import type { RawQuestion } from '@/quiz/quiz.types';

/**
 * AWS SAA-C03 practice set 10 (56 questions), authored by Chabe.
 */
export const exam111: RawQuestion[] = [
  {
    n: 1,
    d: 'SEC',
    m: 1,
    q: 'A company wants to authenticate access to multiple new AWS accounts using its centralized corporate directory. Which two combinations of steps should be suggested? (Choose two.)',
    o: [
      'Create a new AWS Organizations entity with all features enabled, creating the new accounts within it',
      'Set up a Cognito identity pool and configure IAM Identity Center to accept Cognito authentication',
      'Establish a Transit Gateway for centralized network management, linking the accounts',
      'Deploy AWS Directory Service integrated with the corporate directory, setting up IAM Identity Center for cross-account authentication',
      'Install AWS Control Tower and use IAM Identity Center only to manage identity',
    ],
    a: [0, 3],
    x: 'AWS Organizations es la base para una estructura multi-cuenta con gobernanza centralizada, y AWS Directory Service integrado con IAM Identity Center permite autenticar contra el directorio corporativo existente en todas las cuentas. Cognito esta pensado para autenticacion de usuarios de aplicaciones (no acceso empresarial a cuentas AWS), y un Transit Gateway resuelve conectividad de red, no autenticacion.',
  },
  {
    n: 2,
    d: 'PERF',
    m: 0,
    q: 'A traffic law enforcement company has thousands of edge devices generating 1TB of ~2KB status alerts daily, needing high availability, minimal cost, no infrastructure management, 14 days of immediate data, with older data archived. What is the MOST operationally efficient solution?',
    o: [
      'EC2 instances across two AZs behind an ELB, scripted to store alerts in S3, with a lifecycle rule to Glacier after 14 days',
      'A Kinesis Data Firehose delivery stream to S3, with a lifecycle rule to Glacier after 14 days',
      'A Kinesis Data Firehose delivery stream to OpenSearch, with manual daily snapshots and deletion after 14 days',
      'An SQS standard queue with a 14-day retention, consumers copying old messages to S3 before deleting them',
    ],
    a: [1],
    x: 'Kinesis Data Firehose es serverless y totalmente gestionado, ingiere el TB diario sin aprovisionar infraestructura y entrega directo a S3, donde una lifecycle policy archiva a Glacier tras 14 dias automaticamente. EC2 exige gestionar servidores, OpenSearch con snapshots manuales anade complejidad operativa, y SQS no esta pensado para almacenar 1TB diario ni para archivado a largo plazo.',
  },
  {
    n: 3,
    d: 'SEC',
    m: 0,
    q: 'A 3-tier web app has a front end on EC2 in a public subnet, middleware on EC2 in a private subnet, and RDS MySQL in a private subnet. The database must only allow incoming connections from the application. Which option ensures this?',
    o: [
      'A route table excluding the route to the public subnets\u2019 CIDR, associated with the database subnets',
      'A security group denying inbound traffic from the security group assigned to the public subnet instances',
      'A new peering connection between public and private subnets, plus another between private and database subnets',
      'A security group allowing inbound traffic from the security group assigned to the private subnet instances',
    ],
    a: [3],
    x: 'Referenciar el security group de la capa de middleware como origen permite que solo esas instancias se conecten, sin importar cuantas veces cambien. Los security groups no admiten reglas deny, una route table modificada solo rompe el enrutamiento dentro de la VPC, y el peering se usa para conectar VPCs entre si, no subnets dentro de la misma VPC.',
  },
  {
    n: 4,
    d: 'SEC',
    m: 0,
    q: 'An app in a private subnet must have outbound internet access for downloading updates without being exposed to inbound connection attempts. Which steps should be taken?',
    o: [
      'Attach an internet gateway to the private subnet and create a NAT gateway',
      'Attach an internet gateway to the VPC but do not create a NAT gateway',
      'Create a NAT gateway but do not attach an internet gateway to the VPC',
      'Create a NAT gateway and attach an internet gateway to the VPC',
    ],
    a: [3],
    x: 'El NAT gateway se crea en una subnet publica y necesita un internet gateway adjunto a la VPC para dar salida a internet a las instancias privadas, sin permitir conexiones entrantes iniciadas desde fuera. Un internet gateway se adjunta a la VPC (no a una subnet), y sin el, el NAT gateway no puede funcionar.',
  },
  {
    n: 5,
    d: 'PERF',
    m: 0,
    q: 'A database layer being migrated to AWS uses a multi-threaded, in-memory caching layer to improve performance for repeated queries. Which service would be the most suitable replacement for the database cache?',
    o: [
      'Amazon RDS MySQL',
      'Amazon DynamoDB DAX',
      'Amazon ElastiCache Redis',
      'Amazon ElastiCache Memcached',
    ],
    a: [3],
    x: 'ElastiCache for Memcached soporta multiples nucleos e hilos y nodos grandes, encajando con una cache multi-hilo en memoria para consultas repetidas. Redis no soporta multiples nucleos de la misma forma, DAX es especifico de DynamoDB, y RDS MySQL no es una base de datos en memoria.',
  },
  {
    n: 6,
    d: 'SEC',
    m: 0,
    q: 'An app migrated to EC2 must communicate with an on-premises server using private IP addresses. Which action should be taken to quickly provision the necessary connectivity?',
    o: [
      'Configure a Virtual Private Gateway',
      'Create a Transit Gateway',
      'Set up a Direct Connect connection',
      'Create a CloudFront distribution',
    ],
    a: [0],
    x: 'Un virtual private gateway es el endpoint necesario en el lado de AWS para establecer una VPN Site-to-Site gestionada rapidamente hacia el servidor on-premises. Direct Connect tarda demasiado en aprovisionarse, Transit Gateway es para conectar multiples VPCs/redes, y CloudFront es un CDN, sin relacion con este requisito.',
  },
  {
    n: 7,
    d: 'RES',
    m: 0,
    q: 'An organization plans a DR solution running a scaled-down version of a fully functional environment always active, minimizing recovery time. Which DR strategy should be recommended?',
    o: ['Pilot light', 'Warm standby', 'Backup and restore', 'Multi-site'],
    a: [1],
    x: 'Warm standby mantiene una version reducida pero completamente funcional del entorno siempre activa en la nube, minimizando el tiempo de recuperacion. Pilot light solo mantiene un nucleo minimo de servicios, backup and restore es el enfoque de mayor tiempo de recuperacion, y multi-site implica un entorno activo-activo a plena capacidad.',
  },
  {
    n: 8,
    d: 'SEC',
    m: 0,
    q: 'A product manager without admin access needs to view CloudWatch logs linked to a past incident, following least privilege, and the account is managed by a third party. Which option fulfills the requirement?',
    o: [
      'Share the dashboard via email from the CloudWatch console with a shareable link',
      'Create a dedicated IAM user with CloudWatchReadOnlyAccess attached, sharing the new login credentials and dashboard URL',
      'Deploy a bastion server in a public subnet, sharing RDP credentials with the browser configured with cached AWS credentials',
      'Create an IAM user with a ViewOnlyAccess policy, sharing new credentials and dashboard name',
    ],
    a: [1],
    x: 'Crear un usuario IAM dedicado con la politica CloudWatchReadOnlyAccess da exactamente el acceso minimo necesario para ver los dashboards, compartiendo la URL directa. No existe una funcion de compartir dashboards por email/enlace publico, un bastion host con credenciales en cache es innecesariamente complejo e inseguro, y no existe una politica gestionada llamada "ViewOnlyAccess" con exactamente ese alcance.',
  },
  {
    n: 9,
    d: 'RES',
    m: 0,
    q: 'A company has several AWS accounts each with multiple VPCs, needing routing between all private subnets with a simple architecture allowing transitive routing. How should the network connectivity be configured?',
    o: [
      'A hub-and-spoke topology with App Mesh, using RAM to share route tables',
      'A Transit Gateway shared with each account using AWS Resource Access Manager',
      'An AWS Managed VPN between each VPC with route tables configured',
      'A transitive VPC peering connection between each VPC with route tables configured',
    ],
    a: [1],
    x: 'Transit Gateway soporta de forma nativa el enrutamiento transitivo entre muchas VPCs con una topologia en estrella simple, y AWS RAM permite compartirlo entre cuentas. VPC peering no admite enrutamiento transitivo, una VPN gestionada entre cada par de VPCs es mucho mas compleja de mantener, y App Mesh es para networking a nivel de aplicacion en microservicios, no para VPCs.',
  },
  {
    n: 10,
    d: 'SEC',
    m: 1,
    q: 'A travel agency wants individual, secure URLs for each travel agent accessing a REST API on API Gateway, using Route 53 for DNS, with the LEAST operational complexity. Which three steps meet this? (Choose three.)',
    o: [
      'Request a wildcard ACM certificate matching the custom domain name in the same Region as API Gateway',
      'Request a wildcard ACM certificate for the custom domain in a different Region than API Gateway',
      'Establish a custom domain name in API Gateway for the REST API, importing the corresponding ACM certificate',
      'Establish separate API endpoints in API Gateway for each travel agent',
      'Register the domain, set up a wildcard custom domain in a Route 53 hosted zone, and create a record pointing to the API Gateway endpoint',
    ],
    a: [0, 2, 4],
    x: 'Un certificado ACM comodin en la misma region que API Gateway, un dominio personalizado en API Gateway usando ese certificado, y un registro comodin en Route 53 apuntando al endpoint cubren subdominios individuales por agente con una sola configuracion. El certificado debe estar en la misma region que API Gateway, y crear un endpoint de API separado por cada agente anade complejidad operativa innecesaria.',
  },
  {
    n: 11,
    d: 'SEC',
    m: 0,
    q: 'A manufacturing company migrating apps into many AWS accounts under Organizations needs SSO integrated with its existing Active Directory. Which solution will meet these requirements?',
    o: [
      'Deploy an IdP on-premises and enable IAM Identity Center from the console',
      'Use AWS Directory Service to create a two-way trust relationship with the self-managed Active Directory',
      'Enable IAM Identity Center and create a two-way forest trust connecting the self-managed AD via AWS Directory Service for Microsoft AD',
      'Enable IAM Identity Center and create a one-way domain trust connecting the self-managed AD via AWS Directory Service for Microsoft AD',
    ],
    a: [2],
    x: 'IAM Identity Center es el servicio de SSO recomendado para multiples cuentas, y un two-way forest trust con AWS Directory Service for Microsoft AD conecta con el AD existente para autenticar con las credenciales actuales. Un trust de una sola direccion no permite la autenticacion/autorizacion completa necesaria, y desplegar solo un IdP on-premises sin el trust no logra la integracion requerida.',
  },
  {
    n: 12,
    d: 'PERF',
    m: 0,
    q: 'A telemarketing company wants multi-speaker recognition and transcript generation from customer call center audio, plus querying the transcripts to analyze business patterns. Which solution meets these requirements?',
    o: [
      'Amazon Translate for speaker recognition, storing transcripts in Redshift and querying with SQL',
      'Amazon Rekognition for speaker recognition, storing transcripts in S3 and analyzing with ML models',
      'Amazon Transcribe for multiple speaker recognition, using Amazon Athena for transcript file analysis',
      'Amazon Rekognition for speaker recognition, storing transcripts in S3 and using Amazon Textract for analysis',
    ],
    a: [2],
    x: 'Amazon Transcribe convierte audio a texto e identifica multiples hablantes, y Athena permite consultar esas transcripciones directamente en S3 con SQL estandar sin gestionar infraestructura. Rekognition es para imagenes/video (no audio), Translate es para traduccion, y Textract extrae texto de documentos escaneados, no transcribe audio.',
  },
  {
    n: 13,
    d: 'COST',
    m: 0,
    q: 'Log files up to 1GB in size, generated at varying frequency, take around 40 seconds each to process. Which solution is the most cost-effective?',
    o: [
      'Write the log files to an EC2 instance with an attached EBS volume, saving processed files to S3',
      'Write the log files to an SQS queue, using Lambda to process the files from the queue and save to S3',
      'Write the log files to an S3 bucket, with an event notification invoking a Lambda function to process the files',
      'Write the log files to an S3 bucket, with an event notification invoking an ECS task to process the files',
    ],
    a: [2],
    x: 'Una notificacion de evento de S3 que invoca directamente una funcion Lambda es la solucion serverless mas economica, ya que solo se paga por el tiempo de ejecucion real. Usar EC2 no es serverless, SQS no es necesario para archivos de hasta 1GB (supera el limite de mensaje de SQS), y S3 no puede invocar tareas de ECS directamente via notificaciones de evento.',
  },
  {
    n: 14,
    d: 'RES',
    m: 0,
    q: 'A medical records app across three AZs (ALB, EC2 session tier, PostgreSQL on EC2) anticipates a surge in traffic and must scale for future capacity demands while ensuring high availability across all three AZs. Which solution meets these requirements?',
    o: [
      'Migrate PostgreSQL to RDS Multi-AZ, use ElastiCache for Redis with a replication group for sessions, migrate the app server to an ASG across three AZs',
      'Keep PostgreSQL on EC2, use ElastiCache for Redis for sessions, migrate the app server to an ASG across three AZs',
      'Migrate to Aurora PostgreSQL single-AZ, use ElastiCache for Memcached for sessions, migrate the app server to an ASG across three AZs',
      'Migrate to DynamoDB, use DAX to cache reads, store sessions in DynamoDB, migrate the app server to an ASG across three AZs',
    ],
    a: [0],
    x: 'RDS Multi-AZ da failover automatico gestionado, ElastiCache Redis con grupo de replicacion soporta sesiones con durabilidad, y el ASG en tres AZs completa la alta disponibilidad de toda la pila. Mantener PostgreSQL en EC2 pierde los beneficios de RDS, single-AZ incumple el requisito de alta disponibilidad, y migrar a DynamoDB exigiria reescribir la logica de la aplicacion innecesariamente.',
  },
  {
    n: 15,
    d: 'SEC',
    m: 0,
    q: 'Sensitive configuration data in Kubernetes ConfigMaps and Secrets on EKS must be encrypted at rest. Which solution meets these requirements?',
    o: [
      'Create the EKS cluster with default options and use the EFS CSI driver as an add-on',
      'Implement Secrets Manager to manage, rotate, and store all sensitive data, integrated with EKS',
      'Create a new KMS key and enable EKS KMS secrets encryption on the cluster',
      'Use S3 to store all sensitive data with server-side encryption using a new KMS key',
    ],
    a: [2],
    x: 'EKS soporta cifrado envelope de los Secrets de Kubernetes usando una clave de KMS, cifrando directamente lo almacenado en etcd al habilitarlo en el cluster. Secrets Manager no cifra por si solo lo guardado en etcd, el driver CSI de EFS no aporta cifrado de Secrets, y guardar los datos en S3 no resuelve el cifrado de los ConfigMaps/Secrets en si.',
  },
  {
    n: 16,
    d: 'SEC',
    m: 0,
    q: 'A large multi-Region enterprise needs to track all configuration changes affecting AWS resources and have detailed records of who accessed the environment, including logins and API calls made. What actions should be taken?',
    o: [
      'Use CloudWatch to track configuration changes and Config to record API calls',
      'Use Macie to track configuration changes and CloudTrail to record API calls',
      'Use Config to track configuration changes and CloudTrail to record API calls',
      'Use Config to track configuration changes and EventBridge to record API calls',
    ],
    a: [2],
    x: 'Config rastrea y evalua cambios de configuracion de recursos, mientras CloudTrail registra en detalle la actividad de la cuenta incluidas las llamadas a la API y quien las hizo. CloudWatch no rastrea configuraciones, Macie detecta datos sensibles (no cambios de configuracion), y EventBridge es un bus de eventos, no un servicio de auditoria de llamadas a la API.',
  },
  {
    n: 17,
    d: 'PERF',
    m: 0,
    q: 'An online game platform is launching a significant API Gateway update and wants to minimize impact on existing users, gradually rolling out changes while monitoring for issues. What should the company do?',
    o: [
      'Update the existing API directly and immediately direct all traffic to the updated API',
      'Create a new version of the API and use Route 53 to gradually shift DNS queries to it',
      'Create a completely new API and redirect half of user traffic to it',
      'Use an API Gateway canary release deployment, promoting the canary stage after verification',
    ],
    a: [3],
    x: 'Un despliegue canary de API Gateway dirige inicialmente un pequeno porcentaje de trafico a la nueva version, permitiendo verificarla antes de promoverla a produccion completa. Actualizar directamente expone a todos los usuarios de golpe, crear una API nueva con la mitad del trafico no es gradual, y usar Route 53 para cambiar DNS es mas lento e impredecible que el canary nativo.',
  },
  {
    n: 18,
    d: 'SEC',
    m: 0,
    q: 'A company must ensure that only encrypted EBS volumes can be created going forward, with existing unencrypted volumes flagged for remediation, applied consistently to all new EC2 launches. What should be recommended?',
    o: [
      'Manually check each new volume in the console and delete any that are unencrypted',
      'Enable EBS encryption by default at the account/Region level, and use a Config rule to flag existing unencrypted volumes',
      'Attach an IAM policy denying ec2:CreateVolume entirely for all users',
      'Use AWS Backup to automatically encrypt existing volumes during the next scheduled backup',
    ],
    a: [1],
    x: 'Habilitar el cifrado de EBS por defecto asegura que todos los volumenes nuevos se creen cifrados automaticamente, y una regla de AWS Config detecta y senala los volumenes existentes que no cumplen, dando visibilidad para remediarlos. Revisar manualmente no escala, denegar CreateVolume por completo bloquearia tambien la creacion legitima de volumenes cifrados, y AWS Backup no cifra volumenes existentes durante el backup en si.',
  },
  {
    n: 19,
    d: 'PERF',
    m: 0,
    q: 'A music streaming company must expose a Lambda function for a third-party song feed webhook, with the LEAST operational complexity. Which solution should be recommended?',
    o: [
      'Generate an API Gateway endpoint for the Lambda function and share it with the third party',
      'Create an SNS topic linked to the Lambda function, sharing the topic ARN',
      'Deploy an NLB to distribute requests to the Lambda function, sharing the NLB URL',
      'Create an SQS queue connected to the Lambda function, sharing the ARN of the queue',
    ],
    a: [0],
    x: 'API Gateway crea un endpoint HTTP publico que puede invocar la funcion Lambda directamente, la forma mas simple de exponerla para un webhook externo. SNS y SQS no exponen endpoints publicos invocables desde internet, y un NLB delante de Lambda anade complejidad innecesaria frente a la integracion nativa de API Gateway.',
  },
  {
    n: 20,
    d: 'COST',
    m: 0,
    q: 'A DynamoDB table used by an application on EC2 continuously grows, but the application only requires data from the most recent 30 days. What solution minimizes cost and effort?',
    o: [
      'Run a monitoring app on EC2 using DynamoDB Streams to store timestamps and script the deletion of old items',
      'Deploy the entire solution using CloudFormation, redeploying the stack every 30 days',
      'Add an attribute with a value of the current timestamp plus 30 days, configuring it as the TTL attribute',
      'Have DynamoDB Streams invoke a Lambda function on every new item to delete items older than 30 days',
    ],
    a: [2],
    x: 'DynamoDB Time to Live elimina automaticamente los items al pasar la marca de tiempo definida, sin coste adicional ni consumo de capacidad de escritura. Las demas opciones exigen infraestructura o codigo adicional para conseguir lo mismo con mucho mas esfuerzo.',
  },
  {
    n: 21,
    d: 'COST',
    m: 0,
    q: 'An observed increase in EC2 costs is traced to unwanted vertical scaling of instance types. A solutions architect must graph the last 2 months of costs and analyze the root cause. How should the information be generated with the LEAST operational overhead?',
    o: [
      'Use AWS Budgets to create a budget report comparing EC2 costs by instance type',
      'Use Cost Explorer\u2019s granular filtering to perform an in-depth analysis by instance type',
      'Use Cost and Usage Reports sent to S3, with QuickSight generating an interactive graph',
      'Use graphs from the Billing and Cost Management dashboard to compare EC2 costs',
    ],
    a: [1],
    x: 'Cost Explorer permite filtrar y graficar costes con el nivel de detalle necesario directamente desde la consola, sin configuracion adicional. AWS Budgets esta pensado para alertas de umbral, el dashboard de Billing no ofrece esa granularidad sin el CUR, y montar QuickSight sobre el CUR en S3 exige mucho mas esfuerzo operativo del necesario.',
  },
  {
    n: 22,
    d: 'RES',
    m: 1,
    q: 'New functionality converts uploaded images to thumbnails automatically upon upload: store the image in S3, process/compress with Lambda, store the result in a different S3 bucket, requiring a stateless, durable solution. Which combination of two actions meets these requirements? (Choose two.)',
    o: [
      'Launch an EC2 instance connected to an SQS queue, logging the file name to a text file and invoking Lambda',
      'Configure the Lambda function to use the SQS queue as its event source, resizing the image and storing it in a separate bucket',
      'Configure an EventBridge event to monitor the S3 bucket, sending an alert to SNS with the owner\u2019s email for manual processing',
      'Configure the S3 bucket as an event source for Lambda, writing the processed file name to a text file in memory to track processed images',
      'Create an SQS queue, configuring an event notification to add a message when an image is uploaded to S3',
    ],
    a: [1, 4],
    x: 'Una notificacion de evento de S3 que anade un mensaje a SQS al subir una imagen, junto con Lambda usando esa cola como event source para redimensionar y guardar el resultado, forma una solucion serverless y duradera. Guardar el progreso en memoria no es duradero, un unico EC2 no es resiliente, y enviar solo una alerta para procesamiento manual no automatiza el flujo.',
  },
  {
    n: 23,
    d: 'PERF',
    m: 1,
    q: 'A containerized multi-tier app runs on Linux EC2 instances connecting to a PostgreSQL database on separate dedicated EC2 instances, and the company wishes to optimize operational efficiency and performance. Which two actions should be taken? (Choose two.)',
    o: [
      'Set up ElastiCache between the web application and the PostgreSQL database',
      'Migrate the PostgreSQL database to Amazon Aurora',
      'Migrate the web application to the same EC2 instances as the database',
      'Set up a CloudFront distribution for the web application content',
      'Migrate the web application to be hosted on AWS Fargate with ECS',
    ],
    a: [1, 4],
    x: 'Migrar a Aurora reduce la carga operativa y mejora el rendimiento frente a PostgreSQL autogestionado en EC2, y mover la app a Fargate con ECS elimina la gestion del clustering de contenedores. Consolidar en las mismas instancias que la base de datos no aporta beneficios reales, CloudFront ayuda con contenido cacheable pero no reduce la carga operativa, y ElastiCache solo ayuda si hay datos calientes repetidos.',
  },
  {
    n: 24,
    d: 'RES',
    m: 0,
    q: 'A digital marketing agency wants all AWS resources, including untagged ones, backed up properly to minimize data loss risks, with the LEAST operational overhead. Which solution should be recommended?',
    o: [
      'Use Lambda to periodically scan for untagged resources, add tags, then set up AWS Backup',
      'Rely on each account owner to identify their untagged resources and use AWS Backup',
      'Manually search for untagged resources per service, tag them, and set up AWS Backup separately per service',
      'Use AWS Config to identify all untagged resources and tag them programmatically, then use AWS Backup to automate backups based on tags',
    ],
    a: [3],
    x: 'AWS Config detecta recursos sin las etiquetas requeridas y puede corregirlos programaticamente, y AWS Backup automatiza la copia de seguridad basada en esas etiquetas, cubriendo el ciclo completo con minimo esfuerzo manual. Depender de cada propietario de cuenta es inconsistente, buscar manualmente por servicio es muy laborioso, y un Lambda personalizado anade complejidad de desarrollo evitable frente a Config.',
  },
  {
    n: 25,
    d: 'PERF',
    m: 0,
    q: 'A batch process spins up hundreds of EC2 worker instances that all need read access to a shared configuration file stored centrally, with updates propagated to all workers within seconds. What is the simplest solution?',
    o: [
      'Store the file on an EBS volume attached to one worker and have others fetch it via SCP',
      'Store the file in SSM Parameter Store and have each worker fetch it via the Systems Manager API on startup and periodically',
      'Bake the file into a new AMI every time it changes and relaunch all workers',
      'Store the file in an SQS queue and have each worker consume it as a message',
    ],
    a: [1],
    x: 'SSM Parameter Store permite guardar un valor centralizado que cualquier instancia con el permiso IAM adecuado puede leer bajo demanda, propagando cambios de forma casi inmediata sin infraestructura adicional. Un EBS solo accesible desde una instancia via SCP anade pasos manuales, reconstruir la AMI en cada cambio es lento y disruptivo, y SQS consume el mensaje al leerlo (no esta pensado para configuracion compartida y persistente).',
  },
  {
    n: 26,
    d: 'PERF',
    m: 0,
    q: 'A recommendation engine needs to query relationships between millions of users and products (who bought what, who is connected to whom) with fast traversal of multi-hop relationships. Which database type fits best?',
    o: ['Amazon RDS MySQL', 'Amazon Neptune', 'Amazon ElastiCache Memcached', 'Amazon S3'],
    a: [1],
    x: 'Neptune es una base de datos de grafos gestionada, optimizada para almacenar y recorrer relaciones complejas de multiples saltos entre entidades, como las que necesita un motor de recomendaciones. RDS MySQL modela relaciones con JOINs costosos a esa escala, Memcached es solo una cache sin capacidad de consulta relacional, y S3 es almacenamiento de objetos sin motor de consultas de grafos.',
  },
  {
    n: 27,
    d: 'RES',
    m: 0,
    q: 'A legacy on-premises Oracle database needs to move to an open-source engine on AWS while preserving stored procedures, triggers, and complex views with the least manual rewriting effort. What should be recommended?',
    o: [
      'Use AWS DMS alone to migrate directly to Aurora PostgreSQL',
      'Use the AWS Schema Conversion Tool to convert the schema and code first, then use AWS DMS to migrate the data',
      'Export the database to CSV files and import them manually into Aurora PostgreSQL',
      'Use AWS Backup to take a snapshot of the on-premises database and restore it into RDS',
    ],
    a: [1],
    x: 'Para migraciones heterogeneas (motores distintos), el AWS Schema Conversion Tool convierte el esquema y el codigo, incluidos procedimientos almacenados y vistas complejas, antes de que DMS migre los datos manteniendo el origen operativo. DMS por si solo no convierte codigo entre motores distintos, exportar a CSV pierde toda la logica de procedimientos/triggers, y AWS Backup no realiza conversiones entre motores de base de datos diferentes.',
  },
  {
    n: 28,
    d: 'SEC',
    m: 0,
    q: 'A law firm has web servers in a public subnet, with app and database servers in private subnets, and deployed a third-party virtual firewall appliance in an inspection VPC that must inspect all traffic before it reaches the web server. Which solution meets this with the LEAST operational overhead?',
    o: [
      'Create an NLB in the public subnet of the application\u2019s VPC to route traffic to the appliance',
      'Deploy a Transit Gateway in the inspection VPC, configuring route tables to route incoming packets through it',
      'Deploy a Gateway Load Balancer in the inspection VPC, with a Gateway Load Balancer endpoint receiving and forwarding packets to the appliance',
      'Create an ALB in the public subnet of the application\u2019s VPC to route traffic to the appliance',
    ],
    a: [2],
    x: 'El Gateway Load Balancer esta pensado para desplegar y escalar appliances virtuales de inspeccion, combinando un gateway transparente con distribucion de trafico hacia el firewall. NLB y ALB no estan disenados para inspeccion de paquetes, y un Transit Gateway enruta trafico entre redes pero no realiza ni distribuye la inspeccion en si.',
  },
  {
    n: 29,
    d: 'COST',
    m: 0,
    q: 'An analytics team runs unpredictable, large ad-hoc SQL queries against data stored in S3 only a few times per month, and wants to avoid provisioning or paying for idle compute between queries. What should be recommended?',
    o: [
      'Provision a small always-on Redshift cluster and resize it manually before large queries',
      'Load the data into an RDS instance and scale it up before each query',
      'Use Amazon Athena to query the data directly in S3, paying only per query',
      'Provision an EMR cluster that stays running continuously to avoid startup delays',
    ],
    a: [2],
    x: 'Athena es serverless y cobra solo por los datos escaneados en cada consulta, sin infraestructura que aprovisionar ni mantener activa entre consultas esporadicas. Un cluster Redshift o EMR siempre activo genera coste constante aunque no se use, y cargar los datos en RDS anade un paso de ETL innecesario para consultas ad-hoc ocasionales sobre datos que ya estan en S3.',
  },
  {
    n: 30,
    d: 'COST',
    m: 0,
    q: 'An EC2 instance needs high throughput for a MapReduce process with large I/O sizes over a large dataset. Which EBS volume is the MOST cost-effective solution for these requirements?',
    o: [
      'EBS Provisioned IOPS SSD',
      'EBS Throughput Optimized HDD',
      'EBS General Purpose SSD',
      'EBS General Purpose SSD in a RAID 1 configuration',
    ],
    a: [1],
    x: 'Throughput Optimized HDD esta disenado para cargas de alto throughput con datasets grandes y E/S grande, siendo la opcion mas economica entre las que cumplen ese perfil. Las opciones SSD son mas caras y estan orientadas a IOPS altos con tamanos de E/S pequenos, no al throughput secuencial masivo.',
  },
  {
    n: 31,
    d: 'COST',
    m: 0,
    q: 'A company wants to move rarely-updated large binary files (over 500MB each) from an on-premises NAS to AWS, needing native NFS access from an EC2-based rendering farm with minimal management, and the files are accessed by many instances concurrently. What should be recommended?',
    o: [
      'Amazon EBS Multi-Attach volumes',
      'Amazon EFS',
      'Amazon S3 with a mounted FUSE client',
      'Amazon FSx for Windows File Server',
    ],
    a: [1],
    x: 'EFS ofrece un sistema de archivos NFS totalmente gestionado y accesible simultaneamente desde muchas instancias EC2 a la vez, encajando con archivos grandes accedidos concurrentemente por una granja de renderizado. EBS Multi-Attach solo funciona con volumenes Provisioned IOPS en la misma AZ y con soporte del sistema de archivos para acceso concurrente, S3 con un cliente FUSE no ofrece semantica de archivos consistente ni rendimiento fiable, y FSx for Windows esta pensado para SMB, no NFS.',
  },
  {
    n: 32,
    d: 'COST',
    m: 0,
    q: 'A critical Python application runs every 15 minutes for ~2 minutes, needing 1.5GB memory and heavy CPU. The company wants to minimize costs. Which solution meets these requirements?',
    o: [
      'Implement the app as a Lambda function with 1.5GB memory, using EventBridge to schedule it every 15 minutes',
      'Use App2Container to containerize the app, deploying it on ECS Fargate with 1 vCPU and 1.5GB memory',
      'Use App2Container to containerize the app on EC2, with a CloudWatch alarm to stop the instance when idle',
      'Deploy the app on an EC2 instance, manually starting/stopping it according to the schedule',
    ],
    a: [0],
    x: 'Lambda cobra solo por el tiempo real de ejecucion (unos 2 minutos cada 15 minutos), y EventBridge programa la invocacion de forma nativa, siendo la opcion mas economica para este job corto y periodico. Usar Fargate o EC2 implica pagar por capacidad reservada o gestionar arranques/paradas.',
  },
  {
    n: 33,
    d: 'PERF',
    m: 0,
    q: 'Large amounts of production data on EBS need to be cloned into a test environment in the same Region with consistently high I/O performance, minimizing the time required to clone. Which solution meets these requirements?',
    o: [
      'Snapshot the production volumes, create and initialize new EBS volumes, attach them, then restore from the snapshots',
      'Snapshot the production volumes, enable EBS fast snapshot restore, restore into new volumes, and attach them',
      'Snapshot the production volumes and restore onto EC2 instance store volumes in the test environment',
      'Configure the production volumes with EBS Multi-Attach, snapshot them, and attach the production volumes to the test instances',
    ],
    a: [1],
    x: 'EBS fast snapshot restore inicializa completamente el volumen al crearlo desde el snapshot, eliminando la latencia de la primera lectura de cada bloque y entregando el rendimiento aprovisionado desde el primer momento. No se puede restaurar un snapshot a un volumen instance store (efimero), y Multi-Attach solo comparte el mismo volumen entre instancias de una misma AZ.',
  },
  {
    n: 34,
    d: 'RES',
    m: 0,
    q: 'A company\u2019s DR budget is tight, and losing up to 24 hours of data with several hours of recovery time is acceptable in a true disaster, but they still want a way to rebuild the environment in another Region on demand. Which DR strategy fits best?',
    o: ['Multi-site active-active', 'Warm standby', 'Backup and restore', 'Pilot light'],
    a: [2],
    x: 'Backup and restore es la estrategia de menor coste, basada en crear copias de seguridad periodicas de datos y aplicaciones que se restauran en otra region solo cuando ocurre un desastre real, encajando con tolerancias de RPO/RTO mas relajadas. Multi-site y warm standby mantienen infraestructura activa de forma continua (mas cara), y pilot light exige mantener al menos un nucleo minimo de servicios corriendo, con mas coste que backup and restore puro.',
  },
  {
    n: 35,
    d: 'SEC',
    m: 0,
    q: 'A large company using Organizations wants to limit access to an S3 bucket to only accounts enrolled with Organizations, avoiding listing dozens of frequently-changing account IDs. Which strategy meets these requirements with the LEAST effort?',
    o: [
      'Add non-organizational accounts to an OU with an SCP denying access to the S3 bucket',
      'Use Config and Lambda to remediate the bucket policy whenever accounts change status',
      'Use Attribute Based Access Control referencing tags on accounts that are enrolled or not',
      'Use the aws:PrincipalOrgID global key within a bucket policy to allow access only to accounts in the Organization',
    ],
    a: [3],
    x: 'La clave global aws:PrincipalOrgID permite referenciar toda la organizacion en una sola condicion de la bucket policy, sin listar ni actualizar manualmente IDs de cuenta segun cambian. Mantener tags de forma manual es propenso a errores, un flujo con Config+Lambda es innecesariamente complejo, y las SCP solo aplican a cuentas que ya son miembros de la organizacion.',
  },
  {
    n: 36,
    d: 'SEC',
    m: 0,
    q: 'A newly created custom (non-default) security group is attached to an EC2 instance with no rules added yet. What is the default behavior for inbound and outbound traffic?',
    o: [
      'All inbound and outbound traffic is allowed by default',
      'All inbound traffic is denied by default; all outbound traffic is allowed by default',
      'All inbound traffic is allowed by default; all outbound traffic is denied by default',
      'All inbound and outbound traffic is denied by default',
    ],
    a: [1],
    x: 'Un security group personalizado recien creado deniega todo el trafico entrante por defecto (hay que anadir reglas explicitas para permitirlo), pero permite todo el trafico saliente por defecto. No es correcto que ambos esten permitidos o ambos denegados por defecto en un security group nuevo sin reglas.',
  },
  {
    n: 37,
    d: 'PERF',
    m: 0,
    q: 'A tightly-coupled scientific simulation runs across many EC2 instances within a single AZ, and node-to-node network latency is the main bottleneck to overall performance. What should be recommended?',
    o: [
      'Spread the instances across a spread placement group',
      'Launch the instances into a cluster placement group within the same AZ',
      'Attach an Elastic IP to each instance',
      'Enable Multi-AZ for the EC2 instances',
    ],
    a: [1],
    x: 'Un cluster placement group agrupa las instancias fisicamente cerca dentro de una unica AZ, dando la baja latencia y alto throughput de red entre nodos que necesitan las cargas fuertemente acopladas. El spread placement group prioriza tolerancia a fallos (no baja latencia), las Elastic IPs no afectan la latencia entre instancias, y "Multi-AZ" no es un concepto aplicable directamente a instancias EC2 sueltas.',
  },
  {
    n: 38,
    d: 'PERF',
    m: 0,
    q: 'A PowerShell script needs to run on a fleet of EC2 instances running Windows, already launched in a VPC. What tool can be run from the console to execute the script on all target instances?',
    o: ['AWS CodeDeploy', 'AWS Config', 'Run Command', 'AWS OpsWorks'],
    a: [2],
    x: 'Systems Manager Run Command ejecuta scripts o comandos de PowerShell ad-hoc en muchas instancias a la vez desde la consola, sin necesitar conexion individual. CodeDeploy es un servicio de despliegue de aplicaciones, Config evalua configuraciones, y OpsWorks gestiona instancias de Puppet/Chef.',
  },
  {
    n: 39,
    d: 'RES',
    m: 0,
    q: 'An app runs on EC2 in a private subnet behind an ALB in a public subnet, highly available and distributed across multiple AZs, and the EC2 instances must make API calls to an internet-based service. How can highly available internet connectivity be enabled?',
    o: [
      'Create a NAT instance in the private subnet of each AZ, updating each private route table',
      'Create a NAT gateway in the public subnet of each AZ, updating each private subnet\u2019s route table to point to the NAT gateway in its own AZ',
      'Configure an internet gateway and add a route to it in each private subnet route table',
      'Create a NAT gateway and attach it to the VPC, adding a route from each private subnet route table',
    ],
    a: [1],
    x: 'Un NAT gateway por AZ, colocado en la subnet publica correspondiente y referenciado desde la route table de la subnet privada de esa misma AZ, evita un punto unico de fallo por AZ. Los NAT gateways no se crean en subnets privadas ni se adjuntan directamente a la VPC, y un internet gateway no se puede enrutar desde una subnet privada sin exponer las instancias.',
  },
  {
    n: 40,
    d: 'PERF',
    m: 0,
    q: 'A company requires an ELB with extremely high throughput and extremely low latencies over TCP, supporting load balancing to multiple ports on an instance. Which ELB should the company use?',
    o: ['Application Load Balancer', 'Amazon Route 53', 'Classic Load Balancer', 'Network Load Balancer'],
    a: [3],
    x: 'El Network Load Balancer opera a nivel de conexion (capa 4), soporta millones de peticiones por segundo con latencia muy baja, y admite balanceo hacia multiples puertos en una misma instancia. El Classic Load Balancer no ofrece ese throughput/latencia ni ese balanceo multipuerto, el ALB opera solo a nivel HTTP/HTTPS, y Route 53 es DNS, no un load balancer.',
  },
  {
    n: 41,
    d: 'SEC',
    m: 0,
    q: 'An international software firm wants to centrally manage and distribute a standard set of solutions and tools for its clients\u2019 self-service needs. Which solution would best satisfy these requirements?',
    o: [
      'Create CloudFormation stacks for the clients',
      'Create Config rules for the clients',
      'Create Systems Manager documents for the clients',
      'Create Service Catalog portfolios for the clients',
    ],
    a: [3],
    x: 'AWS Service Catalog esta pensado para catalogar y distribuir de forma centralizada un conjunto estandar de herramientas aprobadas que los clientes pueden usar de forma self-service. CloudFormation por si solo no ofrece descubrimiento self-service, Config evalua cumplimiento, y los documentos de Systems Manager definen acciones de automatizacion, no un catalogo de autoservicio.',
  },
  {
    n: 42,
    d: 'PERF',
    m: 0,
    q: 'An e-commerce company\u2019s serverless web app must interact with numerous DynamoDB tables to fulfill user requests, and performance must remain consistent. Which method provides the MOST operationally efficient way to fulfill these requirements?',
    o: [
      'Amazon S3 with Lambda triggers',
      'AWS AppSync with multiple data sources and resolvers',
      'AWS Lambda with Step Functions',
      'AWS Glue with a DynamoDB connector',
    ],
    a: [1],
    x: 'AppSync ofrece una capa GraphQL con resolvers que mapean directamente hacia operaciones de DynamoDB, escalando automaticamente sin logica de orquestacion compleja. S3 con Lambda triggers procesa eventos de subida, Step Functions anade orquestacion innecesaria para consultas simples, y Glue es para ETL/analitica batch.',
  },
  {
    n: 43,
    d: 'SEC',
    m: 0,
    q: 'A solutions architect has logged into an EC2 Linux instance using SSH and needs to determine what IAM role is assigned, the instance ID, and the security group names attached. What would be the best source of this information?',
    o: ['User data', 'Metadata', 'Tags', 'Parameters'],
    a: [1],
    x: 'La metadata de instancia (via http://169.254.169.254/latest/meta-data) contiene informacion propia de la instancia como el rol IAM, el instance ID y los security groups asociados. Los tags categorizan recursos, el user data configura el sistema al arrancar, y los parametros no son un concepto relevante para EC2 en este contexto.',
  },
  {
    n: 44,
    d: 'RES',
    m: 1,
    q: 'The solutions architect for a critical application must ensure the EC2 instances are able to be launched in another AWS Region in the event of a disaster. Which two steps should be taken? (Choose two.)',
    o: [
      'Launch instances in the second Region using the S3 API',
      'Copy the snapshots using S3 cross-region replication',
      'Create AMIs of the instances and copy them to another Region',
      'Enable cross-region snapshots for the EC2 instances',
      'Launch instances in the second Region from the AMIs',
    ],
    a: [2, 4],
    x: 'Crear AMIs de las instancias y copiarlas a otra region da una copia puntual del estado de las instancias en la region remota, y desde esas AMIs se pueden lanzar nuevas instancias cuando haga falta. No existe una funcion de "snapshots cross-region" automatica, y los snapshots no se acceden ni replican via la API de S3.',
  },
  {
    n: 45,
    d: 'PERF',
    m: 1,
    q: 'A software development company is deploying a microservices-based application on EKS with fluctuating traffic and wants the cluster to scale up and down accordingly with MINIMAL operational overhead. Which combination of two steps satisfies these requirements? (Choose two.)',
    o: [
      'Integrate SQS and connect it to EKS for workload management',
      'Leverage X-Ray to track and analyze the application\u2019s network activity',
      'Utilize the Kubernetes Metrics Server to enable horizontal pod autoscaling based on resource utilization',
      'Employ the Kubernetes Cluster Autoscaler for dynamically managing the number of nodes in the EKS cluster',
      'Implement the Kubernetes Vertical Pod Autoscaler to adjust CPU and memory allocation for the pods',
    ],
    a: [2, 3],
    x: 'El Metrics Server alimenta al Horizontal Pod Autoscaler para escalar el numero de pods segun uso de recursos, y el Cluster Autoscaler ajusta el numero de nodos segun la demanda de esos pods, cubriendo el escalado completo de la aplicacion. El Vertical Pod Autoscaler ajusta recursos por pod (no el numero de pods/nodos), y SQS o X-Ray no gestionan directamente el autoescalado del cluster.',
  },
  {
    n: 46,
    d: 'RES',
    m: 0,
    q: 'An EC2 instance behind an ELB is in the process of being deregistered. Which ELB feature is used to allow existing connections to close cleanly?',
    o: ['Proxy Protocol', 'Sticky Sessions', 'Deletion Protection', 'Connection Draining'],
    a: [3],
    x: 'El connection draining (deregistration delay) da un periodo de tiempo para que las conexiones existentes hacia una instancia que se esta des-registrando terminen de forma ordenada antes de cortarlas. Sticky sessions vincula un cliente a una instancia via cookie, el Proxy Protocol transmite la IP original del cliente, y deletion protection solo evita que se elimine el propio load balancer.',
  },
  {
    n: 47,
    d: 'PERF',
    m: 0,
    q: 'An application makes calls to a REST API on EC2 instances behind an ALB. Most calls complete quickly, but a single endpoint requires much longer, introducing overall latency. What steps can be taken to minimize the effects of the long-running API calls?',
    o: [
      'Create an SQS queue and decouple the long-running API calls',
      'Change the EC2 instance to one with enhanced networking to reduce latency',
      'Change the ALB to an NLB and use SSL/TLS termination',
      'Increase the ALB idle timeout to allow the long-running requests to complete',
    ],
    a: [0],
    x: 'Desacoplar las llamadas de larga duracion mediante una cola SQS permite procesarlas de forma asincrona en instancias separadas, evitando que su latencia afecte al resto del sistema. El enhanced networking mejora la red (no la duracion de la llamada), cambiar a NLB con TLS no ataca el problema de fondo, y aumentar el idle timeout solo evita que se corte la conexion.',
  },
  {
    n: 48,
    d: 'RES',
    m: 0,
    q: 'A multinational organization\u2019s app runs on EC2 in an ASG behind an ALB, with an Aurora MySQL database spanning multiple AZs in one region, and plans to launch services in a new geographic area with maximum availability and minimal service interruption. Which strategy should the organization adopt?',
    o: [
      'Establish the application layer in the new region, use Aurora Global Database across primary and new regions, Route 53 health checks with a failover routing policy, promoting the secondary manually as needed',
      'Expand the existing Auto Scaling group into the new region, use Aurora Global Database, and Route 53 health checks with a failover routing policy',
      'Create a similar application layer in the new region with a new Aurora MySQL database, using DMS for ongoing replication and Route 53 health checks',
      'Replicate the application layer in the new region and implement an Aurora MySQL Read Replica there, promoting it to primary on failure',
    ],
    a: [0],
    x: 'Aurora Global Database replica entre regiones con latencia muy baja y permite una promocion manual rapida ante un fallo, y Route 53 con health checks de failover dirige el trafico correctamente, dando la maxima disponibilidad con minima interrupcion. Un Auto Scaling group no se puede extender entre regiones, DMS es mas apropiado para migracion puntual que para failover continuo, y un read replica tarda mas en promocionarse que una region secundaria de Aurora Global Database.',
  },
  {
    n: 49,
    d: 'COST',
    m: 0,
    q: 'A digital media company\u2019s RDS MySQL instance for a content management system is nearing storage capacity due to constant new data, needing sufficient storage without operational interruption or manual intervention. Which solution addresses this with the LEAST operational overhead?',
    o: [
      'Utilize ElastiCache to offload some read traffic and reduce database load',
      'Enable automatic storage scaling for the MySQL instance',
      'Implement a lifecycle policy to delete older data from the MySQL instance',
      'Migrate the database to a larger RDS MySQL instance',
    ],
    a: [1],
    x: 'El auto-escalado de almacenamiento de RDS incrementa automaticamente la capacidad al acercarse al limite asignado, sin intervencion manual ni interrupcion. Migrar a una instancia mayor no evita el problema a futuro, borrar datos antiguos puede no ser aceptable, y ElastiCache mejora lecturas pero no resuelve la falta de espacio en disco.',
  },
  {
    n: 50,
    d: 'COST',
    m: 0,
    q: 'A development team needs to run up a few lab servers on a weekend for a new project, needing to run uninterrupted for a few hours. Which EC2 pricing option would be most suitable?',
    o: ['Reserved', 'Dedicated instances', 'Spot', 'On-Demand'],
    a: [3],
    x: 'On-Demand garantiza que las instancias no se terminen inesperadamente y es economico para necesidades ad-hoc y de corta duracion. Spot puede ser interrumpido por AWS en cualquier momento, Reserved exige compromiso de 1-3 anos, y Dedicated instances son mas caras sin aportar beneficio aqui.',
  },
  {
    n: 51,
    d: 'COST',
    m: 0,
    q: 'A company runs a fleet of On-Demand EC2 instances with steady, predictable usage expected to continue for the next two years, and wants to reduce compute costs without committing to specific instance families or Regions. What should be recommended?',
    o: [
      'Purchase Standard Reserved Instances tied to a specific instance family',
      'Purchase a Compute Savings Plan',
      'Switch entirely to Spot Instances',
      'Purchase Convertible Reserved Instances tied to one Region',
    ],
    a: [1],
    x: 'Un Compute Savings Plan da un descuento significativo a cambio de un compromiso de gasto por hora, aplicandose automaticamente a traves de familias de instancia, tamanos, sistemas operativos y regiones, sin atarse a una configuracion especifica. Las Reserved Instances (estandar o convertibles) atan el descuento a una familia de instancia o region concreta, y depender solo de Spot es arriesgado para una carga con uso estable y predecible que no tolera interrupciones.',
  },
  {
    n: 52,
    d: 'PERF',
    m: 0,
    q: 'A telecommunication company\u2019s API experiences significant traffic spikes and needs consistently low latency response times to ensure a good user experience, minimizing operational overhead. Which solution would meet these requirements MOST efficiently?',
    o: [
      'Use API Gateway with Fargate tasks to handle the API requests',
      'Implement the API on an EC2 instance behind an ALB with manual scaling',
      'Use API Gateway along with Lambda functions with provisioned concurrency',
      'Implement the API using Elastic Beanstalk with auto-scaling groups',
    ],
    a: [2],
    x: 'La concurrencia aprovisionada en Lambda mantiene un pool de funciones ya "calientes" listas para responder de inmediato, garantizando baja latencia constante incluso durante picos, dentro de una arquitectura totalmente gestionada junto con API Gateway. Fargate anade gestion de ciclo de vida de contenedores, EC2 con escalado manual no reacciona con la rapidez necesaria, y Elastic Beanstalk no garantiza la misma baja latencia sostenida.',
  },
  {
    n: 53,
    d: 'RES',
    m: 0,
    q: 'A cloud architect observed that a web application experienced downtime during a scheduled failover on its RDS MySQL database as part of a scaling operation. The organization wants to mitigate such downtime in future scaling exercises with minimal operational overhead. Which solution will be the MOST effective?',
    o: [
      'Implement more RDS MySQL read replicas in the cluster to manage the load during the failover',
      'Configure an RDS Proxy for the database and modify the application to connect to the proxy endpoint',
      'Implement ElastiCache for Redis to manage the load during the failover',
      'Establish a secondary RDS MySQL cluster within the same Region, modifying the application to connect there during any future failover',
    ],
    a: [1],
    x: 'RDS Proxy mantiene un pool de conexiones y, durante un failover, se conecta automaticamente a la instancia standby preservando las conexiones de la aplicacion, reduciendo drasticamente el tiempo de interrupcion percibido. Los read replicas no aceleran el proceso de failover en si, ElastiCache no gestiona failovers de base de datos relacional, y mantener un segundo cluster con reconexion manual anade complejidad sin garantizar menos downtime.',
  },
  {
    n: 54,
    d: 'COST',
    m: 0,
    q: 'A web-based application uses EC2 for the front end and RDS for the back end, writing transaction log files to an S3 bucket that grows large over time. It is acceptable to retain the most recent 60 days of log files and permanently delete the rest. What action enables this automatically?',
    o: [
      'Use an S3 lifecycle policy to move the log files older than 60 days to the GLACIER storage class',
      'Use an S3 lifecycle policy with object expiration configured to remove objects older than 60 days',
      'Write a script that checks object age and deletes objects older than 60 days',
      'Use an S3 bucket policy that deletes objects older than 60 days',
    ],
    a: [1],
    x: 'Una lifecycle policy con una accion de expiracion configurada elimina automaticamente los objetos una vez superan los 60 dias, cumpliendo el requisito de eliminacion permanente sin scripts adicionales. Mover a Glacier reduce coste pero no borra los archivos, un script propio es innecesario, y las bucket policies no eliminan objetos por antiguedad.',
  },
  {
    n: 55,
    d: 'SEC',
    m: 0,
    q: 'A Solutions Architect has created an Organization with several accounts, and security policy requires specific API actions to be limited across all accounts, needing a method of centrally controlling these actions. What is the SIMPLEST method of achieving the requirements?',
    o: [
      'Create a network ACL that limits access to the services or actions and attach it to all relevant subnets',
      'Create an IAM policy in the root account and attach it to users and groups in each account',
      'Create a service control policy in the root organizational unit to deny access to the services or actions',
      'Create cross-account roles in each account to limit access to the services and actions that are allowed',
    ],
    a: [2],
    x: 'Una SCP aplicada en la OU raiz de la organizacion restringe de forma centralizada los permisos maximos disponibles en todas las cuentas de una sola vez. Las network ACL controlan trafico de red (no acciones de la API), replicar politicas IAM en cada cuenta no esta centralizado, y crear roles cross-account en cada cuenta es mucho mas complejo e ineficiente.',
  },
  {
    n: 56,
    d: 'PERF',
    m: 0,
    q: 'An application uses a MySQL database running on an EC2 instance, generating high I/O and constant writes to a single table. Which EBS volume type will provide the MOST consistent performance and low latency?',
    o: [
      'General Purpose SSD (gp2)',
      'Provisioned IOPS SSD (io1)',
      'Cold HDD (sc1)',
      'Throughput Optimized HDD (st1)',
    ],
    a: [1],
    x: 'Provisioned IOPS SSD (io1) se puede configurar con la cantidad exacta de IOPS necesaria y ofrece el rendimiento mas consistente y de menor latencia, ideal para escrituras constantes de alto I/O. Las opciones HDD no son adecuadas para cargas de baja latencia, y gp2 no iguala la consistencia de rendimiento de Provisioned IOPS bajo carga sostenida.',
  },
];
